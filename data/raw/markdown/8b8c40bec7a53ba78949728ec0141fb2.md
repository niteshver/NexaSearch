arXiv is now an independent nonprofit! Learn more ×
License: CC BY 4.0 
arXiv:2607.25299v1 [cs.LG] 28 Jul 2026
# Retraction-Free Optimization over the Stiefel Manifold for the LoRA Fine-Tuning
Yuan Zhang, Jiang Hu, Zhijian Lai, Lin Lin, and Zaiwen Wen Yuan Zhang is with Center for Data Science, Peking University, Beijing 100871, China Email: zy1002@stu.pku.edu.cn.Jiang Hu is with Yau Mathematical Sciences Center, Tsinghua University, Beijing 100084, China Email: jianghu@tsinghua.edu.cn.Zhijian Lai is with Beijing International Center for Mathematical Research, Peking University, Beijing 100871, China Email: lai_zhijian@pku.edu.cn.Lin Lin is with Department of Mathematics, University of California, Berkeley, CA, US. Email: linlin@math.berkeley.edu.Zaiwen Wen is with Beijing International Center for Mathematical Research, Center for Machine Learning Research and Changsha Institute for Computing and Digital Economy, Peking University, Beijing, China Email: wenzw@pku.edu.cn.
###### Abstract
Optimization over the Stiefel manifold plays a significant role in various machine learning tasks. Existing methods either use the retraction operators, requiring costly orthonormalization for large-scale matrices, or employ landing methods that rely on careful step size selection and penalty parameter tuning. To address these challenges, we propose a retraction-free and penalty parameter-free algorithm that directly lands on the manifold. By leveraging the strongly-convex-like property of the quadratic penalty function and the proximal smoothness of the Stiefel manifold, we establish global convergence guarantees with the best-known iteration complexities under both constant and diminishing step sizes. Then, we reformulate the low-rank adaptation (LoRA) fine-tuning problem for large language models as a manifold optimization problem, introducing Manifold-LoRA for geometry-accelerated adaptation. This approach employs the proposed landing technique and a carefully designed step size strategy to accelerate the training process. Numerical experiments on benchmark datasets demonstrate the efficiency and strong downstream performance of the proposed method.
##  Introduction
Optimization over the Stiefel manifold has attracted considerable attention in the context of machine learning, e.g., RNN , batch normalization , distributionally robust optimization , and vision transformer . The mathematical formulation of this class of problems is  
| \displaystyle\min_{X\in\mathbb{R}^{d\times r}}\quad  | =\displaystyle f(X)=\frac{1}{N}\sum_{i=1}^{N}f_{i}(X)  |  
| --- | --- |  
| X∈:={:},\displaystyle X\in{\rm St}(d,r):=\left\\{X\in\mathbb{R}^{d\times r}:X^{\top}X=I\right\\},  |  
where f_{i}:\mathbb{R}^{d\times r}\rightarrow\mathbb{R} are continuously differentiable functions and integers . The most popular methods for solving () are retraction-based algorithms, which have been extensively studied in the context of manifold optimization . Recently, to alleviate the possible computational burden of the retraction operator, some retraction-free methods have been developed in . Their ideas are based on a combination of the manifold geometry and a penalty function for the manifold constraint.
Due to their scalability and parallelization efficiency, retraction-free algorithms are well suited to certain large-scale machine learning applications. In such methods, one must simultaneously control both the constraint violation and the optimality of the loss function. Noting the similarity to decentralized optimization—where the consensus gradient step size is fixed by a known value and only the loss gradient step sizes require tuning —we seek to develop retraction-free algorithms with a known penalty parameter for solving ().
Another motivation for studying retraction-free methods arises from its application in the fine-tuning of large language models (LLMs). Recently, LLMs have revolutionized the field of natural language processing (NLP), achieving unprecedented performance in various applications . To tailor pretrained LLMs for specific downstream tasks, the most common approach is full fine-tuning, which requires prohibitively large computational resources due to the need to adapt all model weights, hindering the deployment of large models. Parameter-efficient fine-tuning (PEFT) has gained widespread attention as it requires few trainable parameters while delivering results comparable to or even superior to full fine-tuning. This paradigm involves inserting learnable modules or designating only a small portion of weights as trainable, keeping the main model frozen . Among fine-tuning methods, low-rank adaptation (LoRA) has become the de facto standard among parameter-efficient fine-tuning techniques. It assumes that the change in weights lies in a low intrinsic dimension, thereby modeling the update \Delta W\in\mathbb{R}^{d\times m} by two low-rank (not greater than a small integer ) matrices A\in\mathbb{R}^{r\times m} and B\in\mathbb{R}^{d\times r}, i.e., \Delta W=BA. Since , the requirements on both storage and computation are significantly reduced. Due to its decompositional nature, there is redundancy in the representation of . Traditional optimization methods for LoRA are unable to exploit this redundancy, which consequently undermines the performance of the models. Instead, we reformulate LoRA fine-tuning as an optimization problem over the product of Stiefel manifolds and Euclidean spaces.111During the review of the initial version of this manuscript, several concurrent studies emerged (see, e.g., ). They adopt closely related manifold constraints to address redundancy in LoRA and use retraction-based algorithms. Therefore, we propose an algorithmic framework called Manifold-LoRA to accelerate the fine-tuning process and enhance model performance. Moreover, by exploiting projected gradients and incorporating a parameter-free penalty, the overhead that our method incurs is relatively negligible. Our contributions are as follows:
  * •
We prove the existence of an explicit penalty parameter by establishing a strong-convexity-like condition for the nonconvex penalty problem associated with the Stiefel manifold constraint. Building on the concept of proximal smoothness for the Stiefel manifold, we then derive convergence results for retraction-free algorithms with an explicit penalty parameter in both stochastic and deterministic settings. Notably, we show that the iterates converge exactly under a constant step size in the deterministic setting, thereby improving upon the prior convergence-to-neighborhood result in . Furthermore, our explicit choice of the penalty parameter achieves a better iteration complexity for the constraint violation in the stochastic setting with a decaying step size than that reported in , since we use two-scale step sizes—decaying for the loss-gradient step but constant for the penalty-gradient step—whereas uses a single-scale decaying step size for both. Moreover, our analysis framework—motivated by decentralized optimization—employs a linear decay of the constraint violation and the descent property of the loss function, which contrasts with the augmented Lagrangian-based approach used in .
  * •
Building upon the established landing theory of retraction-free and penalty parameter-free method and the AdamW framework, we propose a new method, Manifold-LoRA, which employs a carefully designed step size strategy to accelerate the training process of fine-tuning. Compared with the conventional AdamW method, we use the penalized gradient instead of the usual gradient, and the computational overhead is negligible. Numerical experiments are conducted on a wide range of NLP tasks, demonstrating the efficiency of our algorithm. Specifically, compared to vanilla LoRA, our Manifold-LoRA with half the trainable parameters delivers fast convergence and competitive downstream performance. In particular, our method converges twice as fast as baseline methods on several typical datasets, including the SQuAD 2.0 dataset and the CoLA dataset.


###  Related Work
#### Optimization over the Stiefel manifold
Optimization over the Stiefel manifold has attracted lots of attention due to its broad applications. Through the use of retraction, known as the generalization of the exponential map, the Riemannian gradient descent is proposed , where all iterations lie in the manifold. When such retraction is computationally costly, the authors develop a retraction-free algorithm based on the augmented Lagrangian method. More recently, by defining the constraint dissolving operator and adding a sufficiently large penalty term, the authors convert the manifold constrained problem () into an unconstrained problem and then apply unconstrained optimization algorithms. A closely related paper to ours is , which first connects an augmented Lagrangian-based merit function with the landing field and then establishes the convergence by exploring the descent property of the merit function. Although the parameter associated with the penalty term could be arbitrarily chosen, an additional search is still needed when conducting landing algorithms. Inspired by the convergence of Oja’s flow, a retraction-free method is developed in for the squared Stiefel manifold (i.e., ), where the landing flow consists of the projected gradient and the gradient of the penalty function. All of these methods rely on an unknown penalty parameter to ensure the convergence. This motivates us to design penalty parameter-free algorithms, which could significantly reduce the need for tuning parameters in practical implementations.
#### LoRA
There are numerous variants of LoRA aiming to improve performance or reduce memory usage. AdaLoRA , a well-known successor, introduces the idea of adaptively adjusting the rank of different layers by incorporating an additional vector 𝒈\boldsymbol{g} to serve as the diagonal of a singular value matrix. This approach leverages a revised sensitivity-based importance measure to decide whether to disable the entries in the vector 𝒈\boldsymbol{g} and in the matrices and . A similar work, SoRA , adopts the same model architecture as AdaLoRA, but proposes a different way to update the vector 𝒈\boldsymbol{g} after training. This update rule is the proximal gradient of \mathcal{L}_{1} loss, acting as a post-pruning method. Additionally, based on the idea that networks with random initialization contain subnetworks that are optimal, VeRA is proposed in to reduce memory overhead. Although LoRA has gained significant popularity and various variants have been developed, the potential for efficient training through leveraging the manifold geometry to reduce redundancy has not been well-explored.
###  Notation
For a matrix X\in\mathbb{R}^{d\times r}, we use to denote its Frobenius norm. For a squared matrix A\in\mathbb{R}^{r\times r}, we define {\rm sym}(A)=\frac{A+A^{\top}}{2} and use {\rm diag}(A)\in\mathbb{R}^{r} to denote its diagonal part. For two matrices X,Y\in\mathbb{R}^{d\times r}, we use :=\left\langle X,Y\right\rangle:=\sum_{i=1}^{d}\sum_{j=1}^{r}X_{ij}Y_{ij} to denote their Euclidean inner product. For a differential function f:\mathbb{R}^{d\times r}\rightarrow\mathbb{R}, we use \nabla f(X) to denote its usual Euclidean gradient at . We define ={∣dist​}U_{{\rm St}(d,r)}(\frac{1}{8})=\\{X\in\mathbb{R}^{d\times r}\mid{\rm dist}(X,{\rm St}(d,r))<\frac{1}{8}\\} and ​={∣dist​≤}\bar{U}_{{\rm St}(d,r)}(\frac{1}{8})=\\{X\in\mathbb{R}^{d\times r}\mid{\rm dist}(X,{\rm St}(d,r))\leq\frac{1}{8}\\} with dist​:=min⁡{\rm dist}(X,{\rm St}(d,r)):=\min_{Y\in{\rm St}(d,r)}\|Y-X\|. Let 𝟏\mathbf{1} represent the all-ones vector.
##  II Manifold Optimization for LoRA Fine-tuning
In this section, we begin by reformulating LoRA fine-tuning as a manifold optimization problem, incorporating an additional constraint on the matrix , which serves as the basis matrix in our reformulation. We further introduce retraction operators and review classical retraction-based methods.
###  Manifold Optimization Formulation of LoRA Fine-tuning
One possible drawback in the current LoRA fine-tuning framework is that the low-rank decomposition into product is not unique. Specifically, for any invertible matrix , it holds that =BA=(BC)(C^{-1}A). Note that shares the same column space with . This suggests optimizing the subspace generated by instead of itself. Numerous studies in the field of low-rank optimization, e.g., , investigate the manifold geometry of the low-rank decomposition and develop efficient algorithms. However, such geometry has not been explored in the LoRA fine-tuning.
To address such redundancy (i.e., the non-uniqueness of representations), we regard as the basis through the manifold constraint and as the coordinate of under . Hence, the optimization problem can be formulated as  
| min\displaystyle\min_{A\in\mathbb{R}^{r\times m},\;B\in\mathbb{R}^{d\times r}}\quad  | \displaystyle\mathcal{L}(BA),  |  
| --- | --- |  
| B∈St​​or​B∈,\displaystyle B\in{\rm St}(d,r){\rm~or~}B\in{\rm Ob}(d,r),  |  
where :={:}{\rm Ob}(d,r):=\\{B\in\mathbb{R}^{d\times r}:{\rm diag}(B^{\top}B)=\mathbf{1}\\} and ℒ\mathcal{L} represents the loss function. Compared to the Stiefel manifold {\rm St}(d,r), the Oblique manifold {\rm Ob}(d,r) necessitates that the matrix has unit norms in its columns, without imposing requirements for orthogonality between the columns. Problem () is an optimization problem over the product of manifolds and Euclidean spaces.
###  Retraction-based Manifold Optimization
Manifold optimization has attracted much attention in the past few decades, as evident in works such as . For the Stiefel manifold {\rm St}(d,r), its tangent space at is denoted by :={:}T_{X}{\rm St}(d,r):=\\{\xi\in\mathbb{R}^{d\times r}:X^{\top}\xi+\xi^{\top}X=0\\}, which is defined as the subspace consisting of all tangent vectors. For a differentiable function , the Riemannian gradient ∈{\rm grad}f(X)\in T_{X}{\rm St}(d,r) is the unique tangent vector satisfying  
| ⟨,ξ⟩X=,∈,\left\langle{\rm grad}f(X),\xi\right\rangle_{X}={\rm d}f(X)[\xi],~~\forall\xi\in T_{X}{\rm St}(d,r),  |  
| --- |  
where \left\langle\cdot,\cdot\right\rangle_{X} is the Riemannian metric and denotes the differential of function . Since {\rm St}(d,r) is a submanifold embedded in \mathbb{R}^{d\times r}, by setting the Riemannian metric as the Euclidean metric, the Riemannian gradient of at is  
| =−X​sym​,{\rm grad}f(X)=\nabla f(X)-X{\rm sym}(X^{\top}\nabla f(X)),  |  
| --- |  
which is the projection of \nabla f(X) onto the tangent space T_{X}{\rm St}(d,r). The normal space N_{X}{\rm St}(d,r) is defined as the orthogonal complement of T_{X}{\rm St}(d,r) in \mathbb{R}^{d\times r}. In the design of Riemannian algorithms, an essential concept is the so-called retraction operator. A retraction operator ℛ\mathcal{R} at , denoted as \mathcal{R}_{X}, is a mapping from T_{X}{\rm St}(d,r) to {\rm St}(d,r) that satisfies the following two properties:
  * •
\mathcal{R}_{X}(0_{X})=X and is the zero element of T_{X}{\rm St}(d,r);
  * •
=ξ\frac{\rm d}{{\rm d}t}\mathcal{R}_{X}(t\xi)\mid_{t=0}=\xi for any \xi\in T_{X}{\rm St}(d,r).


It is well-known that the retraction operator is a generalization of the exponential map . The iterative scheme of a Riemannian gradient descent method is usually given by  
| =​(​grad​f​),X_{k+1}=\mathcal{R}_{X_{k}}(t_{k}{\rm grad}f(X_{k})),  |  
| --- |  
where is a step size. For the Stiefel manifold {\rm St}(d,r), there are several choices for the retraction ℛ\mathcal{R}, such as the exponential map, the Cayley transform, the QR decomposition, and the polar decomposition, see for details. Among them, the Cayley transformation proposed by is popularly used. It can be expressed as, for any \eta\in T_{X}{\rm St}(d,r),  
| =X−U​​​X,\mathcal{R}_{X}^{\rm Cayley}(-\eta)=X-U\left(I_{2r}+\frac{1}{2}V^{\top}U\right)^{-1}V^{\top}X,  |  
| --- |  
where concatenated matrices U:=[,X]∈U:=[(I_{d}-\frac{1}{2}XX^{\top})\eta,X]\in\mathbb{R}^{d\times(2r)} and V:=[X,−]∈V:=[X,-(I_{d}-\frac{1}{2}XX^{\top})\eta]\in\mathbb{R}^{d\times(2r)}. This needs to invert a -by- matrix and the total computational flops are 4dr^{2}+\frac{40}{3}r^{3} , which could be calculated fast for small .
##  III Retraction-free and Penalty Parameter-free Optimization
In this section, we focus on the design of retraction-free and penalty parameter-free algorithms for solving problem (). We will first present the retraction-free algorithm and then show how the penalty parameter can be explicitly determined by characterizing the landscape of the penalty function.
###  Proximal Smoothness
The concept of proximal smoothness, as introduced by , refers to the characteristic of a closed set whereby the nearest-point projection becomes a singleton when the point is close enough to the set. This property facilitates algorithmic and theoretical advancements by endowing nonconvex sets with convex-like structures. Specifically, for any positive real number , we define the -tube around ℳ\mathcal{M} as :={X:}U_{\mathcal{M}}(\gamma):=\\{X:{\rm dist}(X,\mathcal{M})<\gamma\\}. We say a closed set ℳ\mathcal{M} is -proximally smooth if the projection operator :=⁡\mathcal{P}_{\mathcal{M}}(X):=\operatorname*{\text{argmin}}_{Y\in\mathcal{M}}\|Y-X\|^{2} is a singleton whenever X\in U_{\mathcal{M}}(\gamma). Indeed, as stated in [15, Corollary 4.6], a closed set ℳ\mathcal{M} is convex if and only if it is -proximally smooth for arbitrary \gamma\in(0,\infty). The Stiefel manifold \mathcal{M}={\rm St}(d,r) of interest is 1-proximally smooth .
On the other hand, it is well known that for any closed convex set \mathcal{M}\subset\mathbb{R}^{d\times r}, the projection operator \mathcal{P}_{\mathcal{M}} is 1-Lipschitz continuous over \mathbb{R}^{d\times r}. Similarly, following the proof in [15, Theorem 4.8], we obtain the corresponding result for the Stiefel manifold: for any ∈​X,Y\in\bar{U}_{{\rm St}(d,r)}(\frac{1}{2}),  
| ‖−‖≤.\left\|\mathcal{P}_{{\rm St}(d,r)}(X)-\mathcal{P}_{{\rm St}(d,r)}(Y)\right\|\leq 2\|X-Y\|.  |  
| --- |  
The above properties ensure the Stiefel manifold locally behaves like a convex set, serving as a useful auxiliary result for our subsequent analysis.
###  Retraction-free Algorithms
Inspired by retraction-free algorithms , we consider the following retraction-free gradient descent method for problem ():  
| =−​​−μ​​,X_{k+1}=X_{k}-\alpha_{k}\mathcal{P}_{T_{X_{k}}{\rm St}(d,r)}(g_{k})-\mu X_{k}(X_{k}^{\top}X_{k}-I),  |  
| --- |  
where \alpha_{k},\mu>0 are step sizes, is a stochastic estimate of \nabla f(X_{k}), e.g., mini-batch stochastic gradient, and the mapping  
| ​:=g−​sym​.\mathcal{P}_{T_{X_{k}}{\rm St}(d,r)}(g):=g-X_{k}{\rm sym}(X_{k}^{\top}g).  |  
| --- |  
In contrast to retraction-based algorithms, does not remain on {\rm St}(d,r) at each iteration. Note that when X_{k}\in{\rm St}(d,r), \mathcal{P}_{T_{X_{k}}{\rm St}(d,r)} becomes the projection operator onto the tangent space. Thus, for X_{k}\in{\rm St}(d,r) and g_{k}=\nabla f(X_{k}), it holds ​=\mathcal{P}_{T_{X_{k}}{\rm St}(d,r)}(g_{k})={\rm grad}f(X_{k}). Moreover, the term =X_{k}(X_{k}^{\top}X_{k}-I)=\nabla\varphi(X_{k}) in () is exactly the gradient of the following quadratic penalty function  
| :=.\varphi(X):=\frac{1}{4}\|X^{\top}X-I\|^{2}.  |  
| --- |  
We present the detailed description of retraction-free algorithm in Algorithm .
SQuADv2.0 Train Loss
SQuADv2.0 Eval Exact Match
SQuADv2.0 Eval F1
The figures compare the training loss, evaluation exact match, and evaluation F1 metrics against epochs for the SQuADv2.0 dataset. It can be clearly seen that our proposed Manifold-LoRA method almost achieves a 2x speed-up in training epochs compared to the vanilla LoRA.
Initial point , \alpha_{k},\mu>0, and .
while _Stopping conditions not met_ do
Compute the stochastic gradient \nabla f(X_{k})
Compute ←−​​−μ​​.X_{k+1}\leftarrow X_{k}-\alpha_{k}\mathcal{P}_{T_{X_{k}}{\rm St}(d,r)}(g_{k})-\mu X_{k}(X_{k}^{\top}X_{k}-I).
Update k\leftarrow k+1.
Retraction-free algorithm for solving ()
As will be shown in our theorem, the negative penalty gradient -\nabla\varphi(X_{k}) pulls the iterate back to the manifold, while the use of the projected stochastic gradient ​\mathcal{P}_{T_{X_{k}}{\rm St}(d,r)}(g_{k}) is crucial for ensuring its asymptotic orthogonality with \nabla\varphi(X_{k}), resulting in landing on the manifold and convergence to a stationary point simutaneously. This differs from the usual penalty method, which optimizes f(X)+\mu\varphi(X) using the update =−−μ​​X_{k+1}=X_{k}-\alpha_{k}g_{k}-\mu X_{k}(X_{k}^{\top}X_{k}-I), and requires \mu\rightarrow\infty to guarantee the feasibility.
A key distinction from existing works is that our approach allows for a constant step size in the penalty term while ensuring convergence in both deterministic and stochastic settings. Notably, we set by requiring is not far away from {\rm St}(d,r). It is worth highlighting that similar explicit choices for constraint violation have been explored in decentralized optimization , where a fixed step size—typically set to 1—is commonly employed for enforcing consensus constraints. The theoretical foundation supporting our approach relies on the restricted strong convexity of the penalty function and the proximal smoothness of the Stiefel manifold, which together facilitate convergence guarantees in Section .
Compared with the popularly used Cayley transformation-based retraction-type algorithms, the computational cost therein is 4dr^{2}+\frac{40}{3}r^{3}, which is more than twice the cost of our method at 2dr^{2} for any . Moreover, retractions on the Stiefel manifold involve complex orthogonalization procedures, such as matrix inversion in the Cayley transformation, which are difficult to scale and parallelize. In contrast, the landing update () can be executed using scalable BLAS3 operations.
###  Manifold-LoRA
The retraction-free method is well-suited to address (), simultaneously minimizing the loss function \mathcal{L}(BA) and constraint violation of . To control the constraint violation, we use the quadratic penalties :=R_{\mathrm{St}}(B):=\|B^{\top}B-I\|^{2} and :=‖‖2R_{\mathrm{Ob}}(B):=\|{\rm diag}(B^{\top}B)-\mathbf{1}\|^{2} for the Stiefel manifold and Oblique manifold, respectively. By the retraction-free method, Algorithm , we use the projected gradient of the loss part instead of the Euclidean gradient. For simplicity, we write \nabla_{B}\mathcal{L}=\nabla_{B}\mathcal{L}(BA), and similarly \nabla_{A}\mathcal{L}. For B\in\mathrm{St}(d,r) or B\in\mathrm{Ob}(d,r), the respective projected gradients are  
| ​=−\mathcal{P}_{T_{B}{\rm St}(d,r)}(\nabla_{B}\mathcal{L})=\nabla_{B}\mathcal{L}-B{\rm sym}(B^{\top}\nabla_{B}\mathcal{L})  |  
| --- |  
and  
| ​=−B​ddiag​\mathcal{P}_{T_{B}{\rm Ob}(d,r)}(\nabla_{B}\mathcal{L})=\nabla_{B}\mathcal{L}-B{\rm ddiag}(B^{\top}\nabla_{B}\mathcal{L})  |  
| --- |  
where {\rm ddiag}(Z) denotes with all off-diagonal entries set to . Thus, the gradients of our retraction-free method for and are \nabla_{A}\mathcal{L} and ​+μ​​​(or​​+)\mathcal{P}_{T_{B}{\rm St}(d,r)}(\nabla_{B}\mathcal{L})+\mu\nabla R_{\mathrm{St}}(B)({\rm~or~}\mathcal{P}_{T_{B}{\rm Ob}(d,r)}(\nabla_{B}\mathcal{L})+\mu\nabla R_{\mathrm{Ob}}(B)). Note that and represent the basis and the coordinate of , respectively. This results in different magnitudes and different Lipschitz constants of their gradient function. In fact, let . It follows  
| =,=.\nabla_{A}\mathcal{L}(BA)=B^{\top}\nabla_{X}\mathcal{L}(X),\quad\nabla_{B}\mathcal{L}(BA)=\nabla_{X}\mathcal{L}(X)A^{\top}.  |  
| --- |  
Then, it holds that for any ,A_{1},A_{2}\in\mathbb{R}^{r\times m},B_{1},B_{2}\in\mathbb{R}^{d\times r},  
| ‖−‖\displaystyle\|\nabla_{A}\mathcal{L}(BA_{1})-\nabla_{A}\mathcal{L}(BA_{2})\|  | ≤​​,\displaystyle\leq\|B\|_{2}L_{g}\|A_{1}-A_{2}\|,  |  
| --- | --- |  
| ‖−‖\displaystyle\|\nabla_{B}\mathcal{L}(B_{1}A)-\nabla_{B}\mathcal{L}(B_{2}A)\|  | ≤​​,\displaystyle\leq\|A\|_{2}L_{g}\|B_{1}-B_{2}\|,  |  
where is the Lipschitz constant of \nabla_{X}\mathcal{L}(X) and \|\cdot\|_{2} represents the matrix norm (i.e., the largest singular value). Note that the step size should generally be proportional to the reciprocal of Lipschitz constant for gradient-type algorithms . Hence, we schedule the learning rates for the two matrices based on their respective norms. Having prepared the above, we incorporate the SGD or AdamW optimizer with our manifold-accelerated technique to enhance the LoRA fine-tuning, as presented in Algorithm , which can be seen as a generalization of Algorithm to solve optimization problems over the product of Stiefel or Oblique manifold and Euclidean space. Note that the Oblique manifold {\rm Ob}(d,r) is the product of Stiefel manifolds {\rm St}(d,1), which is the sphere in \mathbb{R}^{d}.
Loss curves on CoLA dataset.
Loss curves on QQP dataset.
Loss curves on STSB dataset.
The figures illustrate that both sphere constrained and Stiefel constrained manifold-LoRA achieve a faster convergence rate and attain a lower training loss within same optimization steps compared to LoRA method on three distinct datasets CoLA, QQP, STS-B.
Initial A_{0},B_{0}, choice update_type \in\\{\texttt{SGD},\,\texttt{AdamW}\\}, \alpha_{k},\mu>0, \beta_{1}=0.9, \beta_{2}=0.999, {\rm ub}\geq{\rm lb}>0, \epsilon=10^{-8}, \lambda>0, ==0m(C_{-1})=v(C_{-1})=0, .
while _Stopping conditions not met_ do
Compute the stochastic gradients g(A_{k}) and g(B_{k})\nabla_{A}\mathcal{L}(B_{k}A_{k}) and \nabla_{B}\mathcal{L}(B_{k}A_{k}), respectively. 
Let \hat{g}(A_{k})\leftarrow g(A_{k})
Let ←​\hat{g}(B_{k})\leftarrow\mathcal{P}_{T_{B_{k}}\mathrm{St}(d,r)}\left(g(B_{k})\right) as in (), or ←​\hat{g}(B_{k})\leftarrow\mathcal{P}_{T_{B_{k}}\mathrm{Ob}(d,r)}\left(g(B_{k})\right) as in (). 
for _C\in\\{A,B\\}_ do
/* Projected grad update for A, B */
if _update_type == SGD_then
←C_{k+1}\leftarrow C_{k}-\alpha_{k}\,\hat{g}(C_{k})
if _update_type == AdamW_then
←+m(C_{k})\leftarrow\beta_{1}\,m(C_{k-1})+(1-\beta_{1})\,\hat{g}(C_{k})
←+⊙​v(C_{k})\leftarrow\beta_{2}\,v(C_{k-1})+(1-\beta_{2})\,\hat{g}(C_{k})\odot\hat{g}(C_{k})
←\hat{m}(C_{k})\leftarrow\tfrac{m(C_{k})}{1-\beta_{1}^{k}}
←\hat{v}(C_{k})\leftarrow\tfrac{v(C_{k})}{1-\beta_{2}^{k}}
←clip​\alpha(C_{k})\leftarrow\text{clip}(\|C_{k}\|_{2},\text{ub},\text{lb})
←−α​​−C_{k+1}\leftarrow C_{k}-\alpha(C_{k})\,\tfrac{\hat{m}(C_{k})}{\sqrt{\hat{v}(C_{k})}+\epsilon}-\lambda C_{k}
/* Penalty grad update for B */
←−μ​​​()C_{k+1}\leftarrow C_{k}-\mu\nabla R_{\mathrm{St}}(C_{k})({\rm or~}\nabla R_{\mathrm{Ob}}(C_{k}))
Update k\leftarrow k+1
Manifold-LoRA for solving ()
##  IV Convergence Analysis
In this section, we analyze the convergence of our retraction-free gradient descent method (). We first justify the choice of \mu=\frac{1}{3} under an appropriate initialization. Subsequently, we establish convergence results for () in both deterministic and stochastic settings.
###  Explicit Choice for the Penalty Parameter
It is known that a large penalty parameter yields better feasibility [40, Chapter 17]. To make the iterative scheme () be penalty parameter-free, we need a careful investigation on the landscape of the following optimization problem:  
| ​=.\min_{X\in\mathbb{R}^{d\times r}}\;\;\varphi(X)=\frac{1}{4}\left\|X^{\top}X-I\right\|^{2}.  |  
| --- |  
It can be easily verified that problem () is nonconvex and its optimal solution set is {\rm St}(d,r). The key of obtaining an explicit formula of is to establish certain strong convexity-type inequality and show that the gradient descent method with step size has linear convergence.
For any X\in\mathbb{R}^{d\times r}, let us denote :=\bar{X}:=\mathcal{P}_{{\rm St}(d,r)}(X). Let X=USV^{\top} be the singular value decomposition with orthogonal matrices U\in\mathbb{R}^{d\times r},V\in\mathbb{R}^{r\times r} and diagonal matrix S\in\mathbb{R}^{r\times r}, then \bar{X}=UV^{\top}. Building on these notations, we demonstrate that problem () satisfies the restricted secant inequality (RSI) , which serves as an alternative to the strong convexity in the linear convergence analysis of gradient-type methods.
######  (RSI).
For any X\in\mathbb{R}^{d\times r} with \|X-\bar{X}\|\leq\frac{1}{8}, we have  
| ≥.\left\langle\nabla\varphi(X),X-\bar{X}\right\rangle\geq\|X-\bar{X}\|^{2}.  |  
| --- |  
With the given RSI, applying the gradient descent (GD) update to (), i.e.,  
| =−,X^{\rm pen}_{k+1}=X^{\rm pen}_{k}-\mu\nabla\varphi(X^{\rm pen}_{k}),  |  
| --- |  
yields the following linear convergence result.
######  (Linear convergence of GD for ()).
Let the sequence \\{X_{k}^{\rm pen}\\} be generated by () with \mu=\frac{1}{3}. Suppose that ‖‖≤\|X^{\rm pen}_{1}-\overline{X^{\rm pen}_{1}}\|\leq\frac{1}{8}, then we have  
| ‖−‖2≤​.\|X_{k+1}^{\rm pen}-\overline{X_{k+1}^{\rm pen}}\|^{2}\leq\frac{2}{3}\|X_{k}-\overline{X_{k}^{\rm pen}}\|^{2}.  |  
| --- |  
Note that the linear convergence of gradient descent follows from the established RSI. It would also be interesting to investigate Newton–Schulz-type updates for nearly orthogonal matrices and to exploit their superlinear local convergence in the design and analysis of retraction-free algorithms for problem (). The proofs of Lemmas and are provided in Appendix .
###  Landing on the Stiefel Manifold
Building on the established linear convergence of gradient descent for problem (), we are now able to show that the iterates generated by () will land on the Stiefel manifold eventually, and the limiting point is a stationary point of (), i.e., X_{\infty}\in{\rm St}(d,r), and =0{\rm grad}f(X_{\infty})=0.
######  .
Suppose the following smoothness and stochasticity conditions hold:
  * •
For each , the component function  is continuously differentiable, and its Euclidean gradient \nabla f_{i}-Lipschitz continuous over the convex hull of ​\bar{U}_{\mathrm{St}(d,r)}\bigl(\tfrac{1}{8}\bigr).
  * •
The stochastic gradient  is an unbiased estimator of \nabla f(X_{k}) with uniformly bounded variance, namely, for all k=1,2,\dots,  
| ,𝔼​[]≤,\mathbb{E}[\,g_{k}\,]\;=\;\nabla f(X_{k}),\qquad\mathbb{E}\bigl[\|\,g_{k}-\nabla f(X_{k})\|^{2}\bigr]\;\leq\;\sigma^{2},  |  
| --- |  
where  is a constant.


For simplicity, define =​\hat{\nabla}f_{i}(X)=\mathcal{P}_{T_{X}{\rm St}(d,r)}(\nabla f_{i}(X)). Note that =\hat{\nabla}f_{i}(X)={\rm grad}f_{i}(X) whenever X\in{\rm St}(d,r). We first have the following quadratic upper bound on and Lipschitz continuity of {\rm grad}f_{i}.
######  (Quadratic upper bound).
Suppose that Assumption holds. There exists a constant such that for any X,Y\in{\rm St}(d,r), and any , the following quadratic upper bound holds:  
| ≤+⟨,⟩+.f_{i}(Y)\leq f_{i}(X)+\left\langle{\rm grad}f_{i}(X),Y-X\right\rangle+\frac{L}{2}\|Y-X\|^{2}.  |  
| --- |  
In addition, there exists a constant \hat{L}>0 such that for any ,Y∈​X\in{\rm St}(d,r),Y\in\bar{U}_{{\rm St}(d,r)}(\frac{1}{8}), and any ,  
| ‖−‖≤.\|{\rm grad}f_{i}(X)-\hat{\nabla}f_{i}(Y)\|\leq\hat{L}\|X-Y\|.  |  
| --- |  
CoLA evaluation matthews correlation
QQP evaluation accuracy
STS-B evaluation pearson
Performance on the validation sets across three datasets. The COLA dataset is evaluated using the matthews correlation metric, QQP is measured by accuracy, and STS-B is evaluated by Pearson correlation, all plotted against the number of epochs. 
By the linear convergence result in Lemma , we have the following decay on the feasibility error.
######  (Error bound of feasibility).
Let \\{X_{k}\\} be the sequence generated by () with \mu=\frac{1}{3}. If \|X_{k}-\bar{X}_{k}\|\leq\frac{1}{8}, then  
| ≤+.\|X_{k+1}-\bar{X}_{k+1}\|\leq\sqrt{\tfrac{2}{3}}\|X_{k}-\bar{X}_{k}\|+\frac{5}{2}\alpha_{k}\|g_{k}\|.  |  
| --- |  
The following one-step descent lemma on is crucial in establishing the convergence. Detailed proofs of Lemmas , , and are provided in Appendix .
######  (One-step descent for ).
Suppose that Assumption holds. Let \\{X_{k}\\} be the sequence generated by Algorithm with \mu=\frac{1}{3}. If \|X_{k}-\bar{X}_{k}\|\leq\frac{1}{8}, we have  
| −\displaystyle\mathbb{E}_{k}[f(\bar{X}_{k+1})]-f(\bar{X}_{k})  |  
| --- |  
| ≤\displaystyle\leq  | −(−​)​+\displaystyle-(\alpha_{k}-(4\hat{L}^{2}+4L+1)\alpha_{k}^{2})\|\hat{\nabla}f(X_{k})\|^{2}+  |  
| ​(+++3)​+\displaystyle\frac{1}{2}\left(4\hat{D}_{f}+9\hat{L}^{2}+8L+3\right)\|X_{k}-\bar{X}_{k}\|^{2}+  |  
| ​+7​​​,\displaystyle\frac{1}{2}\|X_{k+1}-\bar{X}_{k+1}\|^{2}+7(4\hat{L}^{2}+4L+1)\alpha_{k}^{2}\sigma^{2},  |  
where :=maxX∈​⁡\hat{D}_{f}:=\max_{X\in\bar{U}_{{\rm St}(d,r)}(\frac{1}{8})}\|\nabla f(X)\| and we use \mathbb{E}_{k}[\cdot] to denote an expected value taken with respect to the distribution of the random variable in the estimation of given .
From the above lemma, the one-step decrease on is related to both the gradient norm of and the feasibility error. Regarding convergence, we need both {\rm grad}f(X_{k}) and \|X_{k}^{\top}X_{k}-I\| converge to 0. The following theorem shows that the retraction-free and penalty parameter-free update () converges.
######  (Convergence of Algorithm ).
Suppose that Assumption holds. Let \\{X_{k}\\} be the sequence generated by Algorithm with \mu=\frac{1}{3} and \|X_{1}-\bar{X}_{1}\|\;\leq\;\tfrac{1}{8}. Denote the total expectation by :=.\mathbb{E}[\cdot]:=\mathbb{E}_{1}\,\mathbb{E}_{2}\cdots\mathbb{E}_{k}[\cdot]. Then the following statements hold:
  * •
(Deterministic case) If the full gradient is used (i.e.  in Assumption ) and the step size is kept constant as \alpha_{k}\equiv\alpha\in\bigl(0,\tfrac{1}{2c_{1}}\bigr], for some sufficiently large , then  
| ​[+]=.\min_{k\leq K}\;\;\mathbb{E}\left[\|\hat{\nabla}f(X_{k})\|^{2}+\|X_{k}^{\top}X_{k}-I\|^{2}\right]=\mathcal{O}\left(\frac{1}{K}\right).  |  
| --- |  
  * •
(Stochastic case) If the step size decays as \alpha_{k}=\frac{\alpha_{0}}{\sqrt{k}}, with \alpha_{0}\in\bigl(0,\tfrac{1}{2c_{1}}\bigr], for some sufficiently large , then  
| ​[+]=\min_{k\leq K}\;\;\mathbb{E}\left[\|\hat{\nabla}f(X_{k})\|^{2}+\|X_{k}^{\top}X_{k}-I\|^{2}\right]=\mathcal{O}\left(\frac{\log K}{\sqrt{K}}\right)  |  
| --- |  
| ⁡=.\min_{k\leq K}\|X_{k}^{\top}X_{k}-I\|^{2}=\mathcal{O}(\frac{\log K}{K}).  |  
| --- |  


The proof of Theorem is presented in Appendix .
Compared with the landing algorithm , which targets only the squared Stiefel manifold and requires tuning both parameters and , our approach handles general Stiefel manifolds and necessitates the tuning of only , as established in Theorem . In addition, the landing algorithm in converges only to a neighborhood whose size depends on the step size, as discussed in the paragraph following Proposition 10 of their paper. Moreover, our iteration complexity of \mathcal{O}(1/K) is on par with retraction-based algorithms .
Our penalty-parameter-free analysis establishes an improved convergence rate of \mathcal{O}\left(\frac{\log K}{K}\right) for constraint violation in the stochastic setting with decaying step sizes, improving upon the \mathcal{O}\left(\frac{\log K}{\sqrt{K}}\right) rate reported in . This improvement stems from our two-scale step-size scheme—decaying for the loss-gradient step while remaining constant for the penalty-gradient step—whereas employs a single-scale decaying step size for both steps, despite allowing an arbitrary but finite penalty parameter. Furthermore, the proof of Theorem relies on the linear decay of the constraint violation and the descent property of , in contrast to the augmented Lagrangian-based framework of . We also remark that our penalty-parameter-free algorithm design and analysis are motivated by insights from multi-agent decentralized optimization : the stepsize for the _consensus (constraint violation)_ term—which directly measures and penalizes the discrepancy across agents—is typically fixed to , while only the stepsizes for the local loss-gradient updates require tuning. In this literature, the consensus error (i.e., the constraint violation) is also known to converge faster than the loss gradient in stochastic settings with decaying step sizes. Related two-time-scale ideas have also been explored in minimax optimization; see, e.g., .
Now, since Algorithm with SGD-type update can be seen as a generalization of Algorithm to solve problems over the products of the Stiefel manifold or the Oblique manifold and the Euclidean space, we immediately have the following convergence result. The proof of Corollary is given in Appendix .
######  (Convergence of Algorithm ).
Suppose that the gradients \nabla_{A}\mathcal{L}(BA) and \nabla_{B}\mathcal{L}(BA) are Lipschitz continuous, and that \hat{g}(A_{k}) and \hat{g}(B_{k}) are unbiased, bounded-variance estimators of \nabla_{A}\mathcal{L}(B_{k}A_{k}) and \nabla_{B}\mathcal{L}(B_{k}A_{k}), respectively. Let \\{(A_{k},B_{k})\\} be the sequence generated by Algorithm with \mu=\frac{1}{3} and \|B_{0}-\bar{B}_{0}\|\leq 1/8, using an SGD-type update. Then, if the step size is chosen as \alpha_{k}=\frac{\alpha_{0}}{\sqrt{k}} with a small \alpha_{0}>0, we have  
| \displaystyle\min_{k\leq K}  | 𝔼[∥ℒ+∥ℒ\displaystyle\mathbb{E}\bigg[\|\nabla_{A}\mathcal{L}(B_{k}A_{k})\|^{2}+\|\hat{\nabla}_{B}\mathcal{L}(B_{k}A_{k})\|^{2}  |  
| --- | --- |  
| +∥−I]≤𝒪().\displaystyle\quad+\frac{\alpha_{0}}{\sqrt{k}}\|B_{k}^{\top}B_{k}-I\|^{2}\bigg]\leq\mathcal{O}\left(\frac{\log K}{\sqrt{K}}\right).  |  
The proof of the above corollary is based on Theorem and the geometry of the product manifolds. The original Adam method may fail to converge ; nevertheless, Euclidean Adam-type corrections can yield convergence guarantees for Algorithm with Adam-type updates, e.g., AdaShift ; see Appendix .
##  Experiments
In this section, we present comprehensive experimental results to evaluate the performance of Manifold-LoRA, i.e., Algorithm with AdamW update, across various tasks, including natural language understanding (NLU), question answering (QA), and natural language generation (NLG). We highlight the method’s advantages in terms of convergence speed, downstream performance, and memory efficiency. All experiments follow a consistent setup to ensure fair comparison.
###  Baselines and Implementation Details
We compare Manifold-LoRA with several parameter-efficient fine-tuning (PEFT) baselines, including full fine-tuning, Adapter , BitFit , and LoRA . Variants of the Adapter method are omitted due to similar performance trends. Note that, although retraction or projection onto the oblique manifold is inexpensive, we still adopt the retraction-free update to validate our theoretical predictions, in particular the effectiveness of the landing property.
Our implementation is based on PyTorch , Huggingface Transformers , and OpenDelta . For a fair comparison, we ensure that all methods (including Adapter and LoRA) have approximately the same number of trainable parameters by aligning the bottleneck dimensions (e.g., 16 or 32). LoRA updates are scaled by a fixed hyperparameter (typically 16 or 32, as in ), and AdamW is used as the optimizer with default exponential moving average parameters \beta_{1}=0.9, \beta_{2}=0.999. All experiments are conducted on NVIDIA A800 GPUs. 222The hyperparameters used for the GLUE benchmark, question-answering tasks, and E2E benchmark are reported in Tables , , and , respectively.  To make a fair comparison, all hyperparameters such as batch size and learning rate scheduler, remain the same across experiments, except the additional parameters introduced by the Manifold-LoRA.
###  Natural Language Understanding
We evaluate Manifold-LoRA using the DeBERTaV3-base model on the GLUE benchmark , which contains nine subdatasets including MNLI, SST-2, CoLA, QQP, QNLI, RTE, MRPC, and STS-B. For all GLUE tasks, we employ a consistent training configuration: a warmup ratio of 0.06, linear learning rate scheduling, a maximum sequence length of 256, weight decay set to 0.1, and a batch size of 32. The LoRA modules are applied to the query and value projection matrices ( and ).
#### Performance Comparison
Table presents the GLUE results. Manifold-LoRA consistently outperforms LoRA and other baselines across most tasks. In particular, on the RTE and STS-B datasets, both sphere-constrained and Stiefel-constrained variants of Manifold-LoRA with rank outperform LoRA with , indicating superior memory efficiency under equal memory budgets. Overall, the proposed algorithm achieves the best average performance across all methods, with the Oblique-constrained variant under rank attaining the highest average score of 88.63%. Notably, even with a reduced rank of , both the Stiefel- and Oblique-constrained Manifold-LoRA variants outperform the LoRA baseline with , demonstrating superior parameter efficiency.
#### Convergence speed
To further assess the convergence speed of our proposed algorithm, we compare the training loss trajectories throughout the optimization process. As shown in Figure , Manifold-LoRA reaches the same training loss as the standard Adam optimizer in nearly half the number of epochs. In particular, on the CoLA dataset (Figure ), it exhibits almost 2× faster convergence.
We also track the evolution of validation metrics during training. As illustrated in Figure , Manifold-LoRA consistently outperforms vanilla LoRA. Notably, on the STS-B dataset (Figure ), our method achieves a significantly larger performance margin. For the CoLA and QQP datasets, Manifold-LoRA shows slight improvements over LoRA, demonstrating steady performance gains across tasks.
#### Stability across random seeds
Results are averaged over five random seeds, with shaded areas indicating variance. As shown in Figure , Manifold-LoRA exhibits smaller variance compared to LoRA, confirming its stability.
The focus of this work is to use manifold geometry to accelerate LoRA fine-tuning. A systematic study of how such geometric constraints affect generalization is beyond the scope of this paper and is left for future work.
###  Question Answering
We further fine-tune DeBERTaV3-base on SQuAD v1.1 and SQuADv2.0 using Manifold-LoRA. The evaluation primarily focuses on F1 score and exact match accuracy to assess both the completeness and preciseness of predicted answers. For both SQuADv1.1 and SQuADv2.0, we adopt a consistent training setup: a warmup ratio of 0.06, a linear learning rate schedule, a weight decay of 0.1, and a batch size of 64. The learning rate is set to 3e-3, and training is conducted for 4 epochs. Following the LoRA framework, all low-rank modules are inserted into , , , , , and .
#### Performance comparison
Table summarizes experimental results when we fine-tune DeBERTaV3-base under different rank settings. For Adapter, we set the rank to 16 and 32, and for LoRA and our method, the rank is set to 8 and 16. It can be observed that Manifold-LoRA achieves superior performance with fewer trainable parameters. For instance, with the Stiefel constraint, Manifold-LoRA achieves an F1 score of 89.22 and an Exact Match score of 86.41 on the SQuADv2.0 dataset, significantly surpassing all other baselines.
#### Training efficiency and manifold consistency
We also evaluate training loss, validation Exact Match, and validation F1 score across training epochs on the SQuADv2.0 dataset, as shown in Figure . Across the three metrics, Manifold-LoRA exhibits a significantly faster convergence speed, nearly twice that of LoRA, demonstrating a faster loss reduction and a faster improvement in evaluation performance. Additionally, we observe that the models with Stiefel and Oblique constraints follow similar trends in both the loss and evaluation metrics, suggesting that both geometric constraints can achieve comparable effectiveness on question answering tasks.
To further verify whether the learned low-rank matrix effectively lies on the target manifold, we visualize its structure by plotting the heatmaps of B^{\top}B in Figure . Specifically, we extract the checkpoint from the DeBERTa-base model after the second training epoch on the SQuADv2.0 dataset, and select several representative layers from the second and third transformer blocks for analysis. The resulting heatmaps reveal that B^{\top}B closely adheres to the expected manifold geometry, validating the effectiveness of our manifold-constrained design.
###  Natural Language Generation
Having demonstrated superior performance on natural language understanding and question answering tasks, we further extend our experiments to evaluate the effectiveness of our proposed method on a natural language generation task. Specifically, we conduct experiments on the E2E NLG Challenge dataset , using GPT-2 Medium and GPT-2 Large as backbone models. The E2E dataset comprises approximately 50K examples with 8 distinct semantic fields. It provides multiple reference outputs for each input table, with an average output length of 22.9 tokens. For both GPT-2 Medium and Large models on the E2E benchmark, we use a linear learning rate schedule with 500 warmup steps, a weight decay of 0.01, and no LoRA dropout. The models are trained for 5 epochs with a batch size of 8 and a learning rate of 2e-4.
#### Performance comparison
To ensure a fair comparison, we follow the same hyperparameter settings as in the original LoRA paper, except for the additional parameters introduced by Manifold-LoRA. The numerical results are summarized in Table . It is evident that Manifold-LoRA achieves superior performance across all five metrics with limited trainable parameters.
###  Scaling Experiments
#### Experimental Settings
We largely follow the experimental protocol of prior work on LoRA-based fine-tuning with manifold-constrained optimization to ensure a fair and consistent comparison . In particular, we adopt the same model architectures (LLaMA3.2-1B/3B and LLaMA3-8B ), downstream benchmarks (SQuAD, QuAC, GSM8K , and MATH), and evaluation metrics as in the reference study. For all experiments, LoRA is applied to the same set of layers with identical rank and scaling configurations, and models are fine-tuned under the same training budget.
#### Reading Comprehension
Table reports results on SQuAD and QuAC when scaling our method from 1B to 8B models, where the method “Stiefel” is from . Across all model sizes, Manifold-LoRA consistently outperforms other baselines on both datasets. Notably, the performance advantage on SQuAD and QuAC is maintained as the model size increases, indicating that our optimization approach scales well for extractive reading comprehension. On QuAC, which emphasizes semantic understanding and contextual coherence across conversational turns, the oblique manifold-constrained optimization demonstrates more consistent gains than Stiefel manifold-based alternatives.
#### Mathematical Reasoning
Results on GSM8K and MATH are summarized in Table . Stiefel-based Manifold-LoRA yields consistent improvements over AdamW across all model scales, with particularly strong gains on the more challenging MATH benchmark. The improvements become more pronounced for larger models, especially LLaMA3-8B, indicating that Stiefel manifold-constrained optimization is effective at preserving and enhancing complex mathematical reasoning capabilities as model capacity increases.
Reading comprehension results on SQuAD and QuAC (F1/EM).  
 |  
|  |  
| LLaMA3.2-1B  |  
 |  
| Manifold-LoRA (Stiefel)  |  
| Manifold-LoRA (Sphere)  |  
| LLaMA3.2-3B  |  
 |  
| Manifold-LoRA (Stiefel)  |  
| Manifold-LoRA (Sphere)  |  
 |  
 |  
| Manifold-LoRA (Stiefel)  |  
| Manifold-LoRA (Sphere)  |  
Mathematical reasoning accuracy on GSM8K and MATH.  
 |  
|  |  
| LLaMA3.2-1B  |  
| Manifold-LoRA (Stiefel)  |  
| Manifold-LoRA (Sphere)  |  
| LLaMA3.2-3B  |  
| Manifold-LoRA (Stiefel)  |  
| Manifold-LoRA (Sphere)  |  
 |  
| Manifold-LoRA (Stiefel)  |  
| Manifold-LoRA (Sphere)  |  
Manifold-specific hyperparameters of Manifold-LoRA for the E2E benchmark.  
| Hyperparameter  |  
| --- |  
 |  
 |  
Manifold-specific hyperparameters of Manifold-LoRA for question answering tasks.  
| Hyperparameter  |  
| --- |  
|  Sphere ()  |  
|  Sphere ()  |  
|  Stiefel ()  |  
|  Stiefel ()  |  
Hyperparameter configurations of Manifold-LoRA for GLUE benchmark.  
| Hyperparameter  |  
| --- |  
 |  
| Learning Rate  |  
|  Sphere ()  |  
 |  
|  Sphere ()  |  
 |  
|  Stiefel ()  |  
 |  
|  Stiefel ()  |  
 |  
We present results using DeBERTaV3-base on the GLUE benchmark. For MNLI, we report the accuracy (combining matched and mismatched sets), with the left panel representing matched subset and the right panel representing mismatched subset. For CoLA, we report Matthew’s correlation, and for STS-B, we report Pearson correlation. For all other tasks, we report accuracy. All metrics are same as the original LoRA paper . Higher values are better for all metrics. The best results are highlighted in bold.  
 |  
|  |  
 |  
 |  
 |  
 |  
 |  
 |  
 |  
 |  
 |  
 |  
Results with DeBERTaV3-base on SQuAD v1.1 and SQuADv2.0. We report F1 and Exact Match (EM). The best results in each setting are shown in bold.  
 |  
|  |  
| Exact Match  | Exact Match  |  
 |  
 |  
 |  
 |  
 |  
 |  
 |  
 |  
 |  
 |  
Figure 4:  The heat map of B^{\top}B with the Stiefel manifold (the first and second rows) and the Oblique manifold (the third and fourth rows) at the end of training on SQuADv2.0 dataset. GPT-2 medium (M) and large (L) models evaluated on the E2E NLG Challenge. * denotes results from previously published works.  
| Parameters  |  
| --- |  
 |  
|  GPT-2 M (Adapter)  |  
|  GPT-2 M (Adapter)  |  
|  GPT-2 M (FT)  |  
|  GPT-2 M (PreLayer)  |  
| GPT-2 M (LoRA)  |  
| GPT-2 M (Stiefel)  |  
| GPT-2 M (Sphere)  |  
 |  
|  GPT-2 L (Adapter)  |  
|  GPT-2 L (PreLayer)  |  
| GPT-2 L (LoRA)  |  
| GPT-2 L (Stiefel)  |  
| GPT-2 L (Sphere)  |  
##  VI Conclusion
Optimization over the Stiefel manifold has been widely used in machine learning tasks. In this work, we develop a retraction-free and penalty parameter-free gradient method, and prove that the generated iterates eventually land on the manifold and achieve the optimality simultaneously. Moreover, our convergence theory enables the use of a constant step size, improving on previous results that only ensured convergence to a neighborhood. We then apply this landing theory to avoid the possible redundancy of LoRA fine-tuning in LLMs. Specifically, we reformulate the LoRA fine-tuning as an optimization problem over the product of the Stiefel or Oblique manifold and Euclidean space, and propose a new algorithm, Manifold-LoRA, which incorporates a careful analysis of step sizes to enable fast training using the landing properties. Extensive experimental results demonstrate that our approach not only accelerates the training process but also yields significant performance improvements.
Our study suggests several potential directions for future research. Although the established landing theory focuses on the Stiefel manifold, e.g., the generalized Stiefel manifold , extending this theory to general manifolds, is one potential direction. Additionally, evaluating the performance of Manifold-LoRA on LLMs with billions of parameters would be valuable. A systematic understanding of the generalization behavior induced by manifold-constrained adaptation is also an interesting direction for future research. Due to the heterogeneity of different layers, incorporating adaptive ranks for across different layers is another possible direction. This may be achievable by adding sparsity regularization to the coordinate matrix .
## Acknowledgement
We thank the Associate Editor and the two reviewers for their constructive comments and suggestions, which have substantially improved the manuscript.
## References
  * [1] P. Ablin and G. Peyré (2022) Fast and accurate optimization on the orthogonal manifold without retraction.  In International Conference on Artificial Intelligence and Statistics,  pp. 5636–5657. 
  * [2] P. Ablin, S. Vary, B. Gao, and P. Absil (2024) Infeasible deterministic, stochastic, and variance-reduction algorithms for optimization under orthogonality constraints.  Journal of Machine Learning Research 25 (389),  pp. 1–38. 
  * [3] P. Absil, R. Mahony, and R. Sepulchre (2008) Optimization algorithms on matrix manifolds.  Princeton University Press. 
  * [4] M. Arjovsky, A. Shah, and Y. Bengio (2016) Unitary evolution recurrent neural networks.  In International conference on machine learning,  pp. 1120–1128. 
  * [5] M. Balashov and R. Kamalov (2021) The gradient projection method with armijo’s step size on manifolds.  Computational Mathematics and Mathematical Physics 61,  pp. 1776–1786. 
  * [6] V. Bogachev, V. Aletov, A. Molozhavenko, D. Bobkov, V. Soboleva, A. Alanov, and M. Rakhuba (2025) LoRA meets riemannion: muon optimizer for parametrization-independent low-rank adapters.  arXiv preprint arXiv:2507.12142. 
  * [7] V. Bogachev, V. Aletov, A. Molozhavenko, D. Bobkov, V. Soboleva, A. Alanov, and M. Rakhuba (2025) Riemannlora: a unified riemannian framework for ambiguity-free lora optimization.  arXiv e-prints,  pp. arXiv–2507. 
  * [8] L. Bottou, F. E. Curtis, and J. Nocedal (2018) Optimization methods for large-scale machine learning.  SIAM review 60 (2),  pp. 223–311. 
  * [9] N. Boumal, P. Absil, and C. Cartis (2019) Global rates of convergence for nonconvex optimization on manifolds.  IMA Journal of Numerical Analysis 39 (1),  pp. 1–33. 
  * [10] N. Boumal and P. Absil (2011) RTRMC: a Riemannian trust-region method for low-rank matrix completion.  Advances in neural information processing systems 24. 
  * [11] An introduction to optimization on smooth manifolds.  Cambridge University Press. 
  * [12] R. S. Chen, B. Lucier, Y. Singer, and V. Syrgkanis (2017) Robust optimization for non-convex objectives.  Advances in Neural Information Processing Systems 30. 
  * [13] S. Chen, A. Garcia, M. Hong, and S. Shahrampour (2021) Decentralized Riemannian gradient descent on the Stiefel manifold.  In International Conference on Machine Learning,  pp. 1594–1605. 
  * [14] M. Cho and J. Lee (2017) Riemannian approach to batch normalization.  Advances in Neural Information Processing Systems 30. 
  * [15] F. H. Clarke, R. J. Stern, and P. R. Wolenski (1995) Proximal smoothness and the lower-C2 property.  Journal of Convex Analysis 2 (1-2),  pp. 117–144. 
  * [16] K. Cobbe, V. Kosaraju, M. Bavarian, M. Chen, H. Jun, L. Kaiser, M. Plappert, J. Tworek, J. Hilton, R. Nakano, et al. (2021) Training verifiers to solve math word problems.  arXiv preprint arXiv:2110.14168. 
  * [17] W. Dai, E. Kerman, and O. Milenkovic (2012) A geometric approach to low-rank matrix completion.  IEEE Transactions on Information Theory 58 (1),  pp. 237–247. 
  * [18] W. Dai, O. Milenkovic, and E. Kerman (2011) Subspace evolution and transfer (SET) for low-rank matrix completion.  IEEE Transactions on Signal Processing 59 (7),  pp. 3120–3132. 
  * [19] K. Deng and J. Hu (2023) Decentralized projected Riemannian gradient method for smooth optimization on compact submanifolds.  arXiv preprint arXiv:2304.08241. 
  * [20] N. Ding, X. Lv, Q. Wang, Y. Chen, B. Zhou, Z. Liu, and M. Sun (2023) Sparse low-rank adaptation of pre-trained language models.  arXiv preprint arXiv:2311.11696. 
  * [21] A. Dubey, A. Jauhri, A. Pandey, A. Kadian, A. Al-Dahle, A. Letman, A. Mathur, A. Schelten, A. Yang, A. Fan, et al. (2024) The llama 3 herd of models.  arXiv e-prints,  pp. arXiv–2407. 
  * [22] J. Frankle and M. Carbin (2018) The lottery ticket hypothesis: finding sparse, trainable neural networks.  arXiv preprint arXiv:1803.03635. 
  * [23] B. Gao, G. Hu, Y. Kuang, and X. Liu (2022) An orthogonalization-free parallelizable framework for all-electron calculations in density functional theory.  SIAM Journal on Scientific Computing 44 (3),  pp. B723–B745. 
  * [24] B. Gao, X. Liu, X. Chen, and Y. Yuan (2018) A new first-order algorithmic framework for optimization problems with orthogonality constraints.  SIAM Journal on Optimization 28 (1),  pp. 302–332. 
  * [25] P. He, J. Gao, and W. Chen (2021) Debertav3: improving deberta using electra-style pre-training with gradient-disentangled embedding sharing.  arXiv preprint arXiv:2111.09543. 
  * [26] N. J. Higham (2008) Functions of matrices: theory and computation. 
  * [27] N. Houlsby, A. Giurgiu, S. Jastrzebski, B. Morrone, Q. De Laroussilhe, A. Gesmundo, M. Attariyan, and S. Gelly (2019) Parameter-efficient transfer learning for NLP.  In International conference on machine learning,  pp. 2790–2799. 
  * [28] E. J. Hu, Y. Shen, P. Wallis, Z. Allen-Zhu, Y. Li, S. Wang, L. Wang, and W. Chen (2021) Lora: low-rank adaptation of large language models.  arXiv preprint arXiv:2106.09685. 
  * [29] J. Hu, X. Liu, Z. Wen, and Y. Yuan (2020) A brief introduction to manifold optimization.  Journal of the Operations Research Society of China 8,  pp. 199–248. 
  * [30] S. Hu, N. Ding, W. Zhao, X. Lv, Z. Zhang, Z. Liu, and M. Sun (2023) OpenDelta: a Plug-and-play Library for Parameter-efficient Adaptation of Pre-trained Models.  arXiv preprint arXiv:2307.03084. 
  * [31] B. Jiang and Y. Dai (2015) A framework of constraint preserving update schemes for optimization on Stiefel manifold.  Mathematical Programming 153 (2),  pp. 535–575. 
  * [32] L. Kong, Y. Wang, and M. Tao (2023) Momentum Stiefel Optimizer, with Applications to Suitably-Orthogonal Attention, and Optimal Transport.  In International Conference on Learning Representations, 
  * [33] D. J. Kopiczko, T. Blankevoort, and Y. M. Asano (2023) Vera: vector-based random matrix adaptation.  arXiv preprint arXiv:2310.11454. 
  * [34] On the computation of the matrix k-th root.  ZAMM-Journal of Applied Mathematics and Mechanics/Zeitschrift für Angewandte Mathematik und Mechanik: Applied Mathematics and Mechanics 78 (3),  pp. 167–172. 
  * [35] X. L. Li and P. Liang (2021) Prefix-tuning: optimizing continuous prompts for generation.  arXiv preprint arXiv:2101.00190. 
  * [36] T. Lin, C. Jin, and M. I. Jordan (2025) Two-timescale gradient descent ascent algorithms for nonconvex minimax optimization.  Journal of Machine Learning Research 26 (11),  pp. 1–45.  External Links: 
  * [37] T. Lin, C. Jin, and M. Jordan (2020-13–18 Jul) On gradient descent ascent for nonconvex-concave minimax problems.  In Proceedings of the 37th International Conference on Machine Learning, H. D. III and A. Singh (Eds.),  Proceedings of Machine Learning Research, Vol. 119,  pp. 6083–6093.  External Links: 
  * [38] I. Loshchilov and F. Hutter (2017) Decoupled weight decay regularization.  arXiv preprint arXiv:1711.05101. 
  * [39] A. Nedic and A. Ozdaglar (2009) Distributed subgradient methods for multi-agent optimization.  IEEE Transactions on Automatic Control 54 (1),  pp. 48. 
  * [40] J. Nocedal and S. J. Wright (1999) Numerical optimization. 
  * [41] J. Novikova, O. Dušek, and V. Rieser (2017) The E2E dataset: new challenges for end-to-end generation.  arXiv preprint arXiv:1706.09254. 
  * [42] J. Park, M. Kang, S. Lee, H. Lee, S. Kim, and J. Lee (2025-11) Riemannian optimization for LoRA on the stiefel manifold.  In Findings of the Association for Computational Linguistics: EMNLP 2025, C. Christodoulopoulos, T. Chakraborty, C. Rose, and V. Peng (Eds.),  Suzhou, China,  pp. 20971–20985.  External Links: ISBN 979-8-89176-335-7
  * [43] A. Paszke, S. Gross, F. Massa, A. Lerer, J. Bradbury, G. Chanan, T. Killeen, Z. Lin, N. Gimelshein, L. Antiga, et al. (2019) Pytorch: an imperative style, high-performance deep learning library.  Advances in neural information processing systems 32. 
  * [44] C. Qin, A. Zhang, Z. Zhang, J. Chen, M. Yasunaga, and D. Yang (2023) Is chatgpt a general-purpose natural language processing task solver?.  arXiv preprint arXiv:2302.06476. 
  * [45] G. Qu and N. Li (2017) Harnessing smoothness to accelerate distributed optimization.  IEEE Transactions on Control of Network Systems 5 (3),  pp. 1245–1260. 
  * [46] A. Radford, J. Wu, R. Child, D. Luan, D. Amodei, I. Sutskever, et al. (2019) Language models are unsupervised multitask learners.  OpenAI blog 1 (8),  pp. 9. 
  * [47] P. Rajpurkar, R. Jia, and P. Liang (2018) Know what you don’t know: unanswerable questions for SQuAD.  arXiv preprint arXiv:1806.03822. 
  * [48] P. Rajpurkar, J. Zhang, K. Lopyrev, and P. Liang (2016) Squad: 100,000+ questions for machine comprehension of text.  arXiv preprint arXiv:1606.05250. 
  * [49] S. J. Reddi, S. Kale, and S. Kumar (2018) On the convergence of adam and beyond.  In International Conference on Learning Representations, 
  * [50] W. Shi, Q. Ling, G. Wu, and W. Yin (2015) EXTRA: an exact first-order algorithm for decentralized consensus optimization.  SIAM Journal on Optimization 25 (2),  pp. 944–966. 
  * [51] S. Vary, P. Ablin, B. Gao, and P. Absil (2024) Optimization without retraction on the random generalized stiefel manifold.  In International Conference on Machine Learning,  pp. 49226–49248. 
  * [52] A. Wang, A. Singh, J. Michael, F. Hill, O. Levy, and S. R. Bowman (2018) GLUE: a multi-task benchmark and analysis platform for natural language understanding.  arXiv preprint arXiv:1804.07461. 
  * [53] Z. Wen and W. Yin (2013) A feasible method for optimization with orthogonality constraints.  Mathematical Programming 142 (1),  pp. 397–434. 
  * [54] T. Wolf, L. Debut, V. Sanh, J. Chaumond, C. Delangue, A. Moi, P. Cistac, T. Rault, R. Louf, M. Funtowicz, J. Davison, S. Shleifer, P. von Platen, C. Ma, Y. Jernite, J. Plu, C. Xu, T. L. Scao, S. Gugger, M. Drame, Q. Lhoest, and A. M. Rush (2020-10) Transformers: state-of-the-Art Natural Language Processing.  In Proceedings of the 2020 Conference on Empirical Methods in Natural Language Processing: System Demonstrations,  Online,  pp. 38–45. 
  * [55] N. Xiao, X. Liu, and K. Toh (2024) Dissolving constraints for Riemannian optimization.  Mathematics of Operations Research 49 (1),  pp. 366–397.  Cited by: , , , . 
  * [56] E. B. Zaken, S. Ravfogel, and Y. Goldberg (2021) Bitfit: simple parameter-efficient fine-tuning for transformer-based masked language-models.  arXiv preprint arXiv:2106.10199. 
  * [57] H. Zhang and W. Yin (2013) Gradient methods for convex minimization: better rates under weaker conditions.  arXiv preprint arXiv:1303.4645. 
  * [58] Q. Zhang, M. Chen, A. Bukharin, P. He, Y. Cheng, W. Chen, and T. Zhao (2023) Adaptive budget allocation for parameter-efficient fine-tuning.  In The Eleventh International Conference on Learning Representations, 
  * [59] Z. Zhou, Q. Zhang, G. Lu, H. Wang, W. Zhang, and Y. Yu (2019) AdaShift: decorrelation and convergence of adaptive learning rate methods.  In International Conference on Learning Representations, 


##  Appendix A Proofs for the Retraction-Free Convergence Analysis
This appendix provides the detailed arguments supporting the convergence results in Section . Throughout, denotes a closest point to on the Stiefel manifold.
### 
###### Proof.
Let the singular-value decomposition of be X=USV^{\top}, and write \bar{X}=UV^{\top}. Then the distance from to the Stiefel manifold is  
| dist​==,\mathrm{dist}(X,\mathrm{St}(d,r))\;=\;\|X-\bar{X}\|\;=\;\|s-\mathbf{1}\|_{2},  |  
| --- |  
where s=\operatorname{diag}(S) is the vector of singular values of . Under the assumption \|X-\bar{X}\|\leq\frac{1}{8}, each singular value satisfies  
| for all ​i.\frac{7}{8}\;\leq\;s_{i}\;\leq\;\frac{9}{8}\quad\text{for all }i.  |  
| --- |  
We now compute the inner product between the gradient of =\varphi(X)=\tfrac{1}{4}\|X^{\top}X-I\|_{F}^{2} and the deviation X-\bar{X}. First observe  
| =,\nabla\varphi(X)=X\bigl(X^{\top}X-I\bigr),  |  
| --- |  
so  
| =⟨,⟩.\bigl\langle\nabla\varphi(X),\,X-\bar{X}\bigr\rangle=\bigl\langle X(X^{\top}X-I),\,X-\bar{X}\bigr\rangle.  |  
| --- |  
Substitute X=USV^{\top} and \bar{X}=UV^{\top}, then  
| \displaystyle\bigl\langle\nabla\varphi(X),\,X-\bar{X}\bigr\rangle  | =⟨U​S​​,⟩\displaystyle=\bigl\langle USV^{\top}\bigl(VS^{2}V^{\top}-I\bigr),\,USV^{\top}-UV^{\top}\bigr\rangle  |  
| --- | --- |  
| =⟨,⟩=tr​.\displaystyle=\bigl\langle U\,(S^{3}-S)\,V^{\top},\;U\,(S-I)\,V^{\top}\bigr\rangle=\mathrm{tr}\bigl((S^{3}-S)(S-I)\bigr).  |  
Since (S^{3}-S)(S-I) is diagonal, its trace is simply  
| ​=​​.\sum_{i}\bigl(s_{i}^{3}-s_{i}\bigr)(s_{i}-1)=\sum_{i}s_{i}(s_{i}+1)(s_{i}-1)^{2}.  |  
| --- |  
Because each s_{i}\geq 7/8, one checks that  
| ≥=,s_{i}(s_{i}+1)\;\geq\;\frac{7}{8}\cdot\frac{15}{8}=\frac{105}{64}>\frac{3}{2},  |  
| --- |  
and hence  
| =​​≥==.\bigl\langle\nabla\varphi(X),\,X-\bar{X}\bigr\rangle=\sum_{i}s_{i}(s_{i}+1)(s_{i}-1)^{2}\;\geq\;\frac{3}{2}\sum_{i}(s_{i}-1)^{2}=\frac{3}{2}\|s-\mathbf{1}\|_{2}^{2}=\frac{3}{2}\|X-\bar{X}\|^{2}.  |  
| --- |  
This completes the proof. ∎
### 
###### Proof.
Let the singular-value decomposition of the penalized iterate X_{k}^{\rm pen} be =,X_{k}^{\rm pen}=U_{k}\,S_{k}\,V_{k}^{\top}, and write \overline{X_{k}^{\rm pen}}=U_{k}V_{k}^{\top} for its closest point on the Stiefel manifold. Since  
| =​(​−I)=​​,\nabla\varphi(X_{k}^{\rm pen})=X_{k}^{\rm pen}\bigl((X_{k}^{\rm pen})^{\top}X_{k}^{\rm pen}-I\bigr)=U_{k}\,(S_{k}^{3}-S_{k})\,V_{k}^{\top},  |  
| --- |  
its squared Frobenius norm is  
| ===​​.\|\nabla\varphi(X_{k}^{\rm pen})\|_{F}^{2}=\mathrm{tr}\bigl((S_{k}^{3}-S_{k})^{2}\bigr)=\sum_{i}\bigl(s_{i}^{3}-s_{i}\bigr)^{2}=\sum_{i}s_{i}^{2}\,(s_{i}+1)^{2}\,(s_{i}-1)^{2}.  |  
| --- |  
Under the same small-deviation assumption ‖‖≤\|X_{k}^{\rm pen}-\overline{X_{k}^{\rm pen}}\|\leq\frac{1}{8}, each singular value lies in [\tfrac{7}{8},\tfrac{9}{8}], so  
| ≤.s_{i}^{2}\,(s_{i}+1)^{2}\;\leq\;\Bigl(\tfrac{9}{8}\Bigr)^{2}\Bigl(\tfrac{9}{8}+1\Bigr)^{2}\;<\;6.  |  
| --- |  
Hence  
| ≤=​‖‖F2.\|\nabla\varphi(X_{k}^{\rm pen})\|_{F}^{2}\;\leq\;6\sum_{i}(s_{i}-1)^{2}\;=\;6\,\|X_{k}^{\rm pen}-\overline{X_{k}^{\rm pen}}\|_{F}^{2}.  |  
| --- |  
Next, we use the fact that projecting onto the Stiefel manifold cannot increase distance:  
| ‖−‖2≤‖−‖2.\|X_{k+1}^{\rm pen}-\overline{X_{k+1}^{\rm pen}}\|^{2}\;\leq\;\bigl\|X_{k+1}^{\rm pen}-\overline{X_{k}^{\rm pen}}\bigr\|^{2}.  |  
| --- |  
By the gradient-step update  
| =−,X_{k+1}^{\rm pen}=X_{k}^{\rm pen}-\tfrac{1}{3}\,\nabla\varphi(X_{k}^{\rm pen}),  |  
| --- |  
we expand  
| ‖−‖2\displaystyle\bigl\|X_{k+1}^{\rm pen}-\overline{X_{k}^{\rm pen}}\bigr\|^{2}  | =‖−−‖2\displaystyle=\Bigl\|X_{k}^{\rm pen}-\tfrac{1}{3}\nabla\varphi(X_{k}^{\rm pen})-\overline{X_{k}^{\rm pen}}\Bigr\|^{2}  |  
| --- | --- |  
| =‖‖2−​⟨,⟩+​.\displaystyle=\|X_{k}^{\rm pen}-\overline{X_{k}^{\rm pen}}\|^{2}-\tfrac{2}{3}\bigl\langle X_{k}^{\rm pen}-\overline{X_{k}^{\rm pen}},\,\nabla\varphi(X_{k}^{\rm pen})\bigr\rangle+\tfrac{1}{9}\|\nabla\varphi(X_{k}^{\rm pen})\|^{2}.  |  
Now apply the two key inequalities we proved earlier:
  1. 1.
From Lemma , ⟨,⟩≥​‖‖2\langle\nabla\varphi(X_{k}^{\rm pen}),\,X_{k}^{\rm pen}-\overline{X_{k}^{\rm pen}}\rangle\geq\tfrac{3}{2}\|X_{k}^{\rm pen}-\overline{X_{k}^{\rm pen}}\|^{2}.
  2. 2.
From the singular-value bound, ≤6​‖‖2\|\nabla\varphi(X_{k}^{\rm pen})\|^{2}\leq 6\|X_{k}^{\rm pen}-\overline{X_{k}^{\rm pen}}\|^{2}.


Substituting gives  
| ‖−‖2\displaystyle\|X_{k+1}^{\rm pen}-\overline{X_{k+1}^{\rm pen}}\|^{2}  | ≤‖‖2−​‖‖2+​‖‖2\displaystyle\leq\|X_{k}^{\rm pen}-\overline{X_{k}^{\rm pen}}\|^{2}-\tfrac{2}{3}\cdot\tfrac{3}{2}\,\|X_{k}^{\rm pen}-\overline{X_{k}^{\rm pen}}\|^{2}+\tfrac{1}{9}\cdot 6\,\|X_{k}^{\rm pen}-\overline{X_{k}^{\rm pen}}\|^{2}  |  
| --- | --- |  
| =​‖‖2=​‖‖2.\displaystyle=\bigl(1-1+\tfrac{6}{9}\bigr)\|X_{k}^{\rm pen}-\overline{X_{k}^{\rm pen}}\|^{2}=\tfrac{2}{3}\,\|X_{k}^{\rm pen}-\overline{X_{k}^{\rm pen}}\|^{2}.  |  
This completes the proof. ∎
### 
###### Proof.
First inequality (): Due to the Lipschitz continuity of and the compactness of {\rm St}(d,r), the inequality () directly follows from and , where L:=L_{f}+D_{f} with being the Lipschitz constant of \nabla f_{i}(X) over {\rm St}(d,r) and :=⁡max⁡D_{f}:=\max_{i}\max_{X\in{\rm St}(d,r)}\|\nabla f_{i}(X)\|.
Second inequality: We want to bound ‖−‖.\bigl\|{\rm grad}f(X)-\hat{\nabla}f(Y)\bigr\|. Recall that =​\hat{\nabla}f(X)=\mathcal{P}_{T_{X}\mathrm{St}(d,r)}\left(\nabla f(X)\right) and =\hat{\nabla}f(X)=\operatorname{grad}f(X) whenever X\in\operatorname{St}(d,r). We split the difference as  
| ‖−‖\displaystyle\bigl\|{\rm grad}f(X)-\hat{\nabla}f(Y)\bigr\|  | ≤‖​−​‖\displaystyle\leq\bigl\|\mathcal{P}_{T_{X}{\rm St}(d,r)}(\nabla f(X))-\mathcal{P}_{T_{X}{\rm St}(d,r)}(\nabla f(Y))\|  |  
| --- | --- |  
| +‖​−​‖.\displaystyle\quad+\bigl\|\mathcal{P}_{T_{X}{\rm St}(d,r)}(\nabla f(Y))-\mathcal{P}_{T_{Y}{\rm St}(d,r)}(\nabla f(Y))\bigr\|.  |  
  1. 1.
The first term is bounded by the contractive property of \mathcal{P}_{T_{X}{\rm St}(d,r)} and the Lipschitz continuity of :  
| ‖​−​‖≤‖‖≤.\bigl\|\mathcal{P}_{T_{X}{\rm St}(d,r)}(\nabla f(X))-\mathcal{P}_{T_{X}{\rm St}(d,r)}(\nabla f(Y))\bigr\|\;\leq\;\bigl\|\nabla f(X)-\nabla f(Y)\bigr\|\;\leq\;L_{f}\,\|X-Y\|.  |  
| --- |  
  2. 2.
Recall that ​:=g−=g−​X​\mathcal{P}_{T_{X}\operatorname{St}(d,r)}(g):=g-X\operatorname{sym}\left(X^{\top}g\right)=g-\tfrac{1}{2}X\left(X^{\top}g+g^{\top}X\right). Thus, a direct calculation of second term shows  
| ‖​−​‖\displaystyle\bigl\|\mathcal{P}_{T_{X}{\rm St}(d,r)}(\nabla f(Y))-\mathcal{P}_{T_{Y}{\rm St}(d,r)}(\nabla f(Y))\bigr\|  |  
| --- |  
| =\displaystyle=  | ​‖X​(+)−Y​(+)‖\displaystyle\frac{1}{2}\Bigl\|X\bigl(X^{\top}\nabla f(Y)+\nabla f(Y)^{\top}X\bigr)-Y\bigl(Y^{\top}\nabla f(Y)+\nabla f(Y)^{\top}Y\bigr)\Bigr\|  |  
| ≤\displaystyle\leq  | ​‖X​(+)‖+​‖​(+)‖\displaystyle\frac{1}{2}\|X((X-Y)^{\top}\nabla f(Y)+\nabla f(Y)^{\top}(X-Y))\|+\frac{1}{2}\|(X-Y)(Y^{\top}\nabla f(Y)+\nabla f(Y)^{\top}Y)\|  |  
| ≤\displaystyle\leq  | ​​\displaystyle\frac{1}{2}(2\hat{D}_{f}+3\hat{D}_{f})\|X-Y\|  |  
| =\displaystyle=  | \displaystyle\tfrac{5}{2}\,\hat{D}_{f}\,\|X-Y\|,  |  
where :=maxY∈​⁡\hat{D}_{f}:=\max_{Y\in\bar{U}_{{\rm St}(d,r)}(\frac{1}{8})}\|\nabla f(Y)\|, and the last inequality is from the fact that =≤\|Y\|_{2}=\sigma_{\max}(Y)\leq\frac{3}{2}. Putting both pieces together,  
| ‖−‖≤​.\|{\rm grad}f(X)-\hat{\nabla}f(Y)\|\;\leq\;\Bigl(L_{f}+\tfrac{5}{2}\,\hat{D}_{f}\Bigr)\,\|X-Y\|.  |  
| --- |  
Setting \hat{L}=L_{f}+\tfrac{5}{2}\,\hat{D}_{f} completes the proof.


### 
###### Proof.
It follows from =argmin⁡\bar{X}_{k+1}=\operatorname*{\text{argmin}}_{X\in{\rm St}(d,r)}\|X-X_{k+1}\|^{2} that  
| \displaystyle\|X_{k+1}-\bar{X}_{k+1}\|  | \displaystyle\leq\|X_{k+1}-\bar{X}_{k}\|  |  
| --- | --- |  
| ≤‖−​​−−‖ (by update (), )\displaystyle\leq\|X_{k}-\alpha_{k}\mathcal{P}_{T_{X_{k}}\operatorname{St}(d,r)}(g_{k})-\frac{1}{3}\nabla\varphi(X_{k})-\bar{X}_{k}\|\quad\text{ (by update \eqref{eq:grad-it}, $\mu=\frac{1}{3}$) }  |  
| ≤‖−−‖+,\displaystyle\leq\|X_{k}-\frac{1}{3}\nabla\varphi(X_{k})-\bar{X}_{k}\|+\frac{5}{2}\alpha_{k}\|g_{k}\|,  |  
where we used the fact  
| ‖​‖≤≤.\|\mathcal{P}_{T_{X_{k}}{\rm St}(d,r)}(g)\|\leq\|g\|+\|X\|_{2}^{2}\|g\|\leq\frac{5}{2}\|g\|.  |  
| --- |  
According to the proof of Lemma , we have  
| ‖−−‖2≤.\|X_{k}-\frac{1}{3}\nabla\varphi(X_{k})-\bar{X}_{k}\|^{2}\leq\frac{2}{3}\|X_{k}-\bar{X}_{k}\|^{2}.  |  
| --- |  
Hence, we conclude that ≤+.\|X_{k+1}-\bar{X}_{k+1}\|\leq\sqrt{\frac{2}{3}}\|X_{k}-\bar{X}_{k}\|+\frac{5}{2}\alpha_{k}\|g_{k}\|. We complete the proof. ∎
### 
###### Proof.
First, let us prove the following equality  
| ⟨,⟩=⟨,​⟩.\left\langle\hat{\nabla}f_{i}(X),\nabla\phi(X)\right\rangle=\left\langle\nabla f_{i}(X),\mathcal{P}_{T_{X}{\rm St}(d,r)}(\nabla\phi(X))\right\rangle.  |  
| --- |  
In fact, using the definition of =\left\langle A,B\right\rangle={\rm tr}(A^{\top}B), we have  
| ⟨,⟩=\displaystyle\left\langle\hat{\nabla}f_{i}(X),\nabla\phi(X)\right\rangle=  | ⟨−X​sym​,⟩\displaystyle\left\langle\nabla f_{i}(X)-X{\rm sym}(X^{\top}\nabla f_{i}(X)),\nabla\phi(X)\right\rangle  |  
| --- | --- |  
| =\displaystyle=  | ⟨,⟩−⟨X​sym​,⟩\displaystyle\left\langle\nabla f_{i}(X),\nabla\phi(X)\right\rangle-\left\langle X{\rm sym}(X^{\top}\nabla f_{i}(X)),\nabla\phi(X)\right\rangle  |  
| =\displaystyle=  | ⟨,⟩−⟨sym​,⟩\displaystyle\left\langle\nabla f_{i}(X),\nabla\phi(X)\right\rangle-\left\langle{\rm sym}(X^{\top}\nabla f_{i}(X)),X^{\top}\nabla\phi(X)\right\rangle  |  
| =\displaystyle=  | ⟨,⟩−⟨,sym​⟩\displaystyle\left\langle\nabla f_{i}(X),\nabla\phi(X)\right\rangle-\left\langle X^{\top}\nabla f_{i}(X),{\rm sym}(X^{\top}\nabla\phi(X))\right\rangle  |  
| =\displaystyle=  | ⟨,⟩−⟨,X​sym​⟩\displaystyle\left\langle\nabla f_{i}(X),\nabla\phi(X)\right\rangle-\left\langle\nabla f_{i}(X),X{\rm sym}(X^{\top}\nabla\phi(X))\right\rangle  |  
| =\displaystyle=  | ⟨,​⟩.\displaystyle\left\langle\nabla f_{i}(X),\mathcal{P}_{T_{X}{\rm St}(d,r)}(\nabla\phi(X))\right\rangle.  |  
Then, it follows from () that  
| −≤\displaystyle f(\bar{X}_{k+1})-f(\bar{X}_{k})\leq  | ⟨,⟩+\displaystyle\left\langle{\rm grad}f(\bar{X}_{k}),\bar{X}_{k+1}-\bar{X}_{k}\right\rangle+\frac{L}{2}\|\bar{X}_{k+1}-\bar{X}_{k}\|^{2}  |  
| --- | --- |  
| ≤\displaystyle\leq  | ⟨,−⟩+⟨,⟩+\displaystyle\left\langle{\rm grad}f(\bar{X}_{k}),\bar{X}_{k+1}-X_{k+1}+X_{k}-\bar{X}_{k}\right\rangle+\left\langle{\rm grad}f(\bar{X}_{k}),X_{k+1}-X_{k}\right\rangle+2L\|X_{k+1}-X_{k}\|^{2}  |  
| ≤\displaystyle\leq  | ⟨,⟩+⟨,⟩+4​L​(​‖​‖2+)\displaystyle\left\langle{\rm grad}f(\bar{X}_{k}),\bar{X}_{k+1}-X_{k+1}\right\rangle+\left\langle{\rm grad}f(\bar{X}_{k}),X_{k+1}-X_{k}\right\rangle+4L(\alpha_{k}^{2}\|\mathcal{P}_{T_{X_{k}}{\rm St}(d,r)}(g_{k})\|^{2}+\mu^{2}\|\nabla\varphi(X_{k})\|^{2})  |  
| =\displaystyle=  | ⟨−grad​f​,⟩+⟨,⟩\displaystyle\left\langle{\rm grad}f(\bar{X}_{k})-{\rm grad}f(\bar{X}_{k+1}),\bar{X}_{k+1}-X_{k+1}\right\rangle+\left\langle\hat{\nabla}f(X_{k}),X_{k+1}-X_{k}\right\rangle  |  
| +⟨−,⟩+4​L​(​‖​‖2+)\displaystyle+\left\langle{\rm grad}f(\bar{X}_{k})-\hat{\nabla}f(X_{k}),X_{k+1}-X_{k}\right\rangle+4L(\alpha_{k}^{2}\|\mathcal{P}_{T_{X_{k}}{\rm St}(d,r)}(g_{k})\|^{2}+\mu^{2}\|\nabla\varphi(X_{k})\|^{2})  |  
| ≤\displaystyle\leq  | 2​​+​−​⟨,​⟩\displaystyle 2\hat{L}^{2}\|X_{k+1}-X_{k}\|^{2}+\frac{1}{2}\|X_{k+1}-\bar{X}_{k+1}\|^{2}-\alpha_{k}\left\langle\hat{\nabla}f(X_{k}),\mathcal{P}_{T_{X_{k}}{\rm St}(d,r)}(g_{k})\right\rangle  |  
| −μ​⟨,⟩+​(+)\displaystyle-\mu\left\langle\hat{\nabla}f(X_{k}),\nabla\varphi(X_{k})\right\rangle+\frac{1}{2}(\hat{L}^{2}\|X_{k}-\bar{X}_{k}\|^{2}+\|X_{k+1}-X_{k}\|^{2})  |  
| +4​L​(​‖​‖2+),\displaystyle+4L(\alpha_{k}^{2}\|\mathcal{P}_{T_{X_{k}}{\rm St}(d,r)}(g_{k})\|^{2}+\mu^{2}\|\nabla\varphi(X_{k})\|^{2}),  |  
where the second inequality is from the 2-Lipschitz continuity of \mathcal{P}_{{\rm St}(d,r)} over ​\bar{U}_{{\rm St}(d,r)}(\frac{1}{8}), the last inequality is due to the facts that ∈X_{k}-\bar{X}_{k}\in N_{\bar{X}_{k}}{\rm St}(d,r) and ≤\left\langle A,B\right\rangle\leq\frac{1}{2}(\|A\|^{2}+\|B\|^{2}) for any A,B\in\mathbb{R}^{d\times r}. Noticing that  
| ‖​‖\displaystyle\|\mathcal{P}_{T_{X_{k}}{\rm St}(d,r)}(\nabla\varphi(X_{k}))\|  | =‖‖≤‖​​​​‖\displaystyle=\|X_{k}(X_{k}^{\top}X_{k}-I)^{2}\|\leq\|U_{k}S_{k}V_{k}^{\top}(V_{k}S_{k}^{2}V_{k}^{\top}-I)V_{k}\|  |  
| --- | --- |  
| ≤​≤,\displaystyle\leq\|S_{k}(S_{k}+I)^{2}\|\|X_{k}-\bar{X}_{k}\|^{2}\leq 6\|X_{k}-\bar{X}_{k}\|^{2},  |  
we have  
| −≤\displaystyle\mathbb{E}_{k}[f(\bar{X}_{k+1})]-f(\bar{X}_{k})\leq  | −​−μ​⟨,​⟩+​​\displaystyle-\alpha_{k}\|\hat{\nabla}f(X_{k})\|^{2}-\mu\left\langle\nabla f(X_{k}),\mathcal{P}_{T_{X_{k}}{\rm St}(d,r)}(\nabla\varphi(X_{k}))\right\rangle+\frac{1}{2}\mathbb{E}_{k}\|X_{k+1}-\bar{X}_{k+1}\|^{2}  |  
| --- | --- |  
| +​(​​[‖​‖2]+)\displaystyle+\frac{1}{2}\|X_{k}-\bar{X}_{k}\|^{2}+(4\hat{L}^{2}+4L+1)(\alpha_{k}^{2}\mathbb{E}_{k}[\|\mathcal{P}_{T_{X_{k}}{\rm St}(d,r)}(g_{k})\|^{2}]+\mu^{2}\|\nabla\varphi(X_{k})\|^{2})  |  
| ≤\displaystyle\leq  | −(−​)​+​​\displaystyle-(\alpha_{k}-(4\hat{L}^{2}+4L+1)\alpha_{k}^{2})\|\hat{\nabla}f(X_{k})\|^{2}+\frac{1}{2}\mathbb{E}_{k}\|X_{k+1}-\bar{X}_{k+1}\|^{2}  |  
| +(++6​​)​+7​​​,\displaystyle+(6\mu\hat{D}_{f}+\frac{1}{2}\hat{L}^{2}+6(4\hat{L}^{2}+4L+1)\mu^{2})\|X_{k}-\bar{X}_{k}\|^{2}+7(4\hat{L}^{2}+4L+1)\alpha_{k}^{2}\sigma^{2},  |  
where we use 𝔼​[​]=​=\mathbb{E}[\mathcal{P}_{T_{X_{k}}{\rm St}(d,r)}(g_{k})]=\mathcal{P}_{T_{X_{k}}{\rm St}(d,r)}(\mathbb{E}[g_{k}])=\hat{\nabla}f(X_{k}) and 𝔼​[‖​‖2]≤+7​(𝔼​[2])=+\mathbb{E}[\|\mathcal{P}_{T_{X_{k}}{\rm St}(d,r)}(g_{k})\|^{2}]\leq\|\hat{\nabla}f(X_{k})\|^{2}+7(\mathbb{E}[\|g_{k}-\hat{\nabla}f(X_{k})\|^{2}])=\|\hat{\nabla}f(X_{k})\|^{2}+7\sigma^{2} from Assumption and (). Plugging \mu=\frac{1}{3} into () gives (). ∎
### 
###### Proof.
First, we show ∈​X_{k}\in\bar{U}_{{\rm St}(n,d)}(\frac{1}{8}) for any if \alpha_{k}\leq\frac{1}{120\hat{D}_{f}}. In fact, by proof of induction, we have from () that  
| ≤+≤.\|X_{k+1}-\bar{X}_{k+1}\|\leq\sqrt{\frac{2}{3}}\|X_{k}-\bar{X}_{k}\|+\frac{5}{240\hat{D}_{f}}\|g_{k}\|\leq\frac{1}{8}.  |  
| --- |  
Moreover, applying the standard sequence bound to () yields  
| 𝔼​[]≤60​𝔼​[​‖​‖2]+4≤60​(​+)+4\displaystyle\mathbb{E}\left[\sum_{k=1}^{K}\|X_{k}-\bar{X}_{k}\|^{2}\right]\leq 0\mathbb{E}\left[\sum_{k=1}^{K}\alpha_{k}^{2}\|\mathcal{P}_{T_{X_{k}}{\rm St}(d,r)}(g_{k})\|^{2}\right]+4\leq 0\sum_{k=1}^{K}\left(\alpha_{k}^{2}\|\hat{\nabla}f(X_{k})\|^{2}+7\alpha_{k}^{2}\sigma^{2}\right)+4  |  
| --- |  
Then, summing () over k=1,\ldots,K gives  
| −\displaystyle\mathbb{E}[f(\bar{X}_{K+1})]-f(\bar{X}_{1})  |  
| --- |  
| ≤\displaystyle\leq  | −(−​)​𝔼​[]\displaystyle-(\alpha_{k}-(4\hat{L}^{2}+4L+1)\alpha_{k}^{2})\mathbb{E}\left[\sum_{k=1}^{K}\|\hat{\nabla}f(X_{k})\|^{2}\right]  |  
| +​(+++4)​𝔼​[]\displaystyle+\frac{1}{2}\left(4\hat{D}_{f}+9\hat{L}^{2}+8L+4\right)\mathbb{E}\left[\sum_{k=1}^{K+1}\|X_{k}-\bar{X}_{k}\|^{2}\right]  |  
| ≤\displaystyle\leq  | −(−​+30​(+++4)​)​𝔼​[]\displaystyle-(\alpha_{k}-(4\hat{L}^{2}+4L+1)\alpha_{k}^{2}+0(4\hat{D}_{f}+9\hat{L}^{2}+8L+4)\alpha_{k}^{2})\mathbb{E}\left[\sum_{k=1}^{K}\|\hat{\nabla}f(X_{k})\|^{2}\right]  |  
| +​(+++4)​(+420​​+4).\displaystyle+\frac{1}{2}\left(4\hat{D}_{f}+9\hat{L}^{2}+8L+4\right)(0\alpha_{K+1}^{2}\hat{D}_{f}^{2}+20\sigma^{2}\sum_{k=1}^{K+1}\alpha_{k}^{2}+4).  |  
Define =+++121c_{1}=275\hat{L}^{2}+244L+120\hat{D}_{f}+121 and =(+210​​+2)​(+++4)c_{2}(k)=(30\alpha_{K+1}^{2}\hat{D}_{f}^{2}+210\sigma^{2}\sum_{k=1}^{K+1}\alpha_{k}^{2}+2)(4\hat{D}_{f}+8\hat{L}^{2}+8L+4). Then, we have  
| 𝔼​[​​]≤+,\mathbb{E}\left[\sum_{k=1}^{K}\alpha_{k}(1-c_{1}\alpha_{k})\|\hat{\nabla}f(X_{k})\|^{2}\right]\leq f(\bar{X}_{1})-f_{\min}+c_{2}(k),  |  
| --- |  
where :=argmin⁡f​f_{\min}:=\operatorname*{\text{argmin}}_{X\in{\rm St}(d,r)}\;f(X). Therefore, we have the following iteration complexity results.
  * •
If taking constant step sizes \alpha_{k}\equiv\alpha\in(0,\frac{1}{2c_{1}}] (which also gives \alpha\leq\frac{1}{120\hat{D}_{f}}), then  
| ​𝔼​[]≤\displaystyle\frac{1}{K}\mathbb{E}\left[\sum_{k=1}^{K}\|\hat{\nabla}f(X_{k})\|^{2}\right]\leq  | 2​++,\displaystyle\frac{2(f(\bar{X}_{1})-f_{\min})+24c_{3}}{\alpha K}+20\alpha\sigma^{2},  |  
| --- | --- |  
| ​𝔼​[]≤\displaystyle\frac{1}{K}\mathbb{E}\left[\sum_{k=1}^{K}\|X_{k}-\bar{X}_{k}\|^{2}\right]\leq  | 120​​+++420​​​,\displaystyle\frac{120\alpha^{2}(f(\bar{X}_{1})-f_{\min})+1440\alpha^{2}c_{3}+4\alpha}{\alpha K}+20\alpha^{2}(0\alpha+1)\sigma^{2},  |  
where :=+++1c_{3}:=\hat{D}_{f}+2\hat{L}^{2}+2L+1. If in addition the full gradient is used, i.e., g_{k}=\nabla f(x_{k}), we have and  
| ​𝔼​[(+)]=.\frac{1}{K}\mathbb{E}\left[\sum_{k=1}^{K}\left(\|\hat{\nabla}f(X_{k})\|^{2}+\|X_{k}-\bar{X}_{k}\|^{2}\right)\right]=\mathcal{O}\left(\frac{1}{K}\right).  |  
| --- |  
  * •
If taking step sizes \alpha_{k}=\frac{\alpha_{0}}{\sqrt{k}} with \alpha_{0}\in(0,\frac{1}{2c_{1}}], it follows from () and ≤\|\hat{\nabla}f(X_{k})\|\leq\frac{5}{2}\hat{D}_{f} that  
| ⁡≤​𝔼​[]=.\min_{k\leq K}\|X_{k}-\bar{X}_{k}\|^{2}\leq\frac{1}{K}\mathbb{E}\left[\sum_{k=1}^{K}\|X_{k}-\bar{X}_{k}\|^{2}\right]=\mathcal{O}\left(\frac{\log K}{K}\right).  |  
| --- |  
Besides, summing () and () gives  
| 𝔼​[(​​+)]=.\mathbb{E}\left[\sum_{k=1}^{K}\left(\alpha_{k}(1-c_{1}\alpha_{k})\|\hat{\nabla}f(X_{k})\|^{2}+\|X_{k}-\bar{X}_{k}\|^{2}\right)\right]=\mathcal{O}\left(\log K\right).  |  
| --- |  
Furthermore, dividing both sides of () by \sum_{k=1}\alpha_{k} leads to  
| ​[+]=.\min_{k\leq K}\;\;\mathbb{E}\left[\|\hat{\nabla}f(X_{k})\|^{2}+\|X_{k}-\bar{X}_{k}\|^{2}\right]=\mathcal{O}\left(\frac{\log K}{\sqrt{K}}\right).  |  
| --- |  


We complete the proof.
### 
###### Proof.
To prove the corollary from Theorem , we concatenate into a single variable , and verify that all assumptions and conclusions used in Theorem remain valid. For simplicity, we assume B\in{\rm St}(d,r). The oblique manifold {\rm Ob}(d,r) can be seen as the product of Stiefel manifolds {\rm St}(d,1), and the results can be easily extended to the setting. The detailed verifications are as follows:
  * •
: Since the constraint set is now \mathbb{R}^{d\times r}\times\mathrm{St}(d,r), we define \bar{X}:=(A,\bar{B}) and let =\varphi(X)=\frac{1}{4}\|B^{\top}B-I\|^{2}. With this definition, the same conclusion as in the original lemma holds.
  * •
: This lemma follows naturally under the new definition of , as it depends only on the conclusion of Lemma .
  * •
: A similar Lipschitz-type result holds by leveraging the Lipschitz continuity of \nabla\mathcal{L} and the geometric properties of the Stiefel manifold associated with .
  * •
: This is a direct consequence of Lemma and does not require further modification.
  * •
: By using all the results above, we can obtain a descent inequality for −\mathcal{L}(\bar{X}_{k+1})-\mathcal{L}(\bar{X}_{k}) in terms of the gradient norm and feasibility errors. Specifically, the descent is characterized by  
| ,,,and,\|\hat{\nabla}\mathcal{L}(X_{k})\|,\quad\|B_{k}-\bar{B}_{k}\|^{2},\quad\|B_{k+1}-\bar{B}_{k+1}\|^{2},\quad\text{and}\quad\sigma^{2},  |  
| --- |  
where  
| =[,].\hat{\nabla}\mathcal{L}(X_{k})=\left[\nabla_{A}\mathcal{L}(B_{k}A_{k}),\,\hat{\nabla}_{B}\mathcal{L}(B_{k}A_{k})\right].  |  
| --- |  


Therefore, by the chosen step size \alpha_{k}, we can invoke Theorem and complete the proof of the corollary. ∎
##  Appendix B Convergence of an Adam-style retraction-free update
The convergence of Algorithm with the AdamW update is difficult to guarantee directly, as even the Euclidean Adam method may fail to converge without suitable modifications . Nevertheless, our retraction-free idea can be combined with Adam-type correction techniques from Euclidean optimization to obtain convergence guarantees. As an example, following the decorrelation idea in AdaShift , we use a delayed adaptive preconditioner: the preconditioner is fixed before the current stochastic gradient is drawn, which makes the alignment argument conditionally clean.
We first introduce the Adam-style retraction-free update. Given a stochastic Euclidean gradient , define its projected stochastic gradient by :=​\tilde{g}_{k}:=P_{T_{X_{k}}\mathrm{St}(d,r)}(g_{k}). Let , , and \bar{v}_{0}=0. For , define  
| \displaystyle m_{k}  | =+,\displaystyle=\beta_{1,k}m_{k-1}+(1-\beta_{1,k})\tilde{g}_{k},  |  
| --- | --- |  
| \displaystyle v_{k}  | =+​,=max⁡,\displaystyle=\beta_{2}v_{k-1}+(1-\beta_{2})(\tilde{g}_{k}\odot\tilde{g}_{k}),\qquad\bar{v}_{k}=\max\\{\bar{v}_{k-1},v_{k}\\},  |  
where the maximum is taken componentwise. We identify matrices with their vectorizations when applying diagonal preconditioners, and define  
| :=Diag⁡,.H_{k-1}:=\operatorname{Diag}\left(\frac{1}{\sqrt{\bar{v}_{k-1}}+\epsilon}\right),\qquad d_{k}:=H_{k-1}m_{k}.  |  
| --- |  
The Adam-style retraction-free update is  
| =−−.X_{k+1}=X_{k}-\alpha_{k}d_{k}-\frac{1}{3}\nabla\varphi(X_{k}).  |  
| --- |  
For the notation, let \mathcal{F}_{k} denote the filtration generated by the history before drawing the stochastic gradient at iteration , and let \mathbb{E}_{k}[\cdot]:=\mathbb{E}[\cdot\mid\mathcal{F}_{k}]. For a matrix in a neighborhood of \mathrm{St}(d,r), let denote its projection onto \mathrm{St}(d,r), and define \delta(X):=\|X-\bar{X}\|. For the iterate , we write \bar{X}_{k} for its polar projection and set \delta_{k}:=\|X_{k}-\bar{X}_{k}\|. We also use the notation in the main paper,  
| f:=∇f−Xsym.\hat{\nabla}f(X):=\nabla f(X)-X\operatorname{sym}(X^{\top}\nabla f(X)).  |  
| --- |  
######  (Stochasticity for the Adam-style update).
For every , conditioned on \mathcal{F}_{k},  
| =,​‖‖2≤.\mathbb{E}_{k}[\tilde{g}_{k}]=\hat{\nabla}f(X_{k}),\qquad\mathbb{E}_{k}\|\tilde{g}_{k}-\hat{\nabla}f(X_{k})\|^{2}\leq\sigma^{2}.  |  
| --- |  
Moreover, there exist constants and G_{\infty}>0 such that \|\tilde{g}_{k}\|\leq G and \|\tilde{g}_{k}\|_{\infty}\leq G_{\infty} almost surely. Finally, 0\leq\beta_{1,k}\leq\bar{\beta}<1 and 0\leq\beta_{2}<1.
Set  
| ,,.h_{+}:=\frac{1}{\epsilon},\qquad h_{-}:=\frac{1}{G_{\infty}+\epsilon},\qquad q:=\sqrt{\frac{2}{3}}.  |  
| --- |  
Under Condition , we have h_{-}I\preceq H_{k}\preceq h_{+}I for every .
######  (Boundedness of the Adam direction).
Under Condition , for every , \|m_{k}\|\leq G and \|d_{k}\|\leq h_{+}G. Moreover,  
| ≤2​​(+)+.\mathbb{E}_{k}\|d_{k}\|^{2}\leq 2h_{+}^{2}\bigl(\|\hat{\nabla}f(X_{k})\|^{2}+\sigma^{2}\bigr)+8h_{+}^{2}\beta_{1,k}^{2}G^{2}.  |  
| --- |  
###### Proof.
The bound \|m_{k}\|\leq G follows by induction from (), because is a convex combination of and \tilde{g}_{k}. Since H_{k-1}\preceq h_{+}I, we get ≤≤\|d_{k}\|\leq h_{+}\|m_{k}\|\leq h_{+}G.
For the second-moment bound, write =+​m_{k}=\tilde{g}_{k}+\beta_{1,k}(m_{k-1}-\tilde{g}_{k}). Then  
| ≤+2​​≤+.\|m_{k}\|^{2}\leq 2\|\tilde{g}_{k}\|^{2}+2\beta_{1,k}^{2}\|m_{k-1}-\tilde{g}_{k}\|^{2}\leq 2\|\tilde{g}_{k}\|^{2}+8\beta_{1,k}^{2}G^{2}.  |  
| --- |  
Since =\mathbb{E}_{k}[\tilde{g}_{k}]=\hat{\nabla}f(X_{k}),  
| =+​‖‖2≤+.\mathbb{E}_{k}\|\tilde{g}_{k}\|^{2}=\|\hat{\nabla}f(X_{k})\|^{2}+\mathbb{E}_{k}\|\tilde{g}_{k}-\hat{\nabla}f(X_{k})\|^{2}\leq\|\hat{\nabla}f(X_{k})\|^{2}+\sigma^{2}.  |  
| --- |  
Using \|d_{k}\|\leq h_{+}\|m_{k}\| proves (). ∎
######  (Feasibility recursion).
Suppose ∈​X_{k}\in\bar{U}_{\mathrm{St}(d,r)}(1/8). Then  
| ≤.\delta_{k+1}\leq q\delta_{k}+\alpha_{k}\|d_{k}\|.  |  
| --- |  
Consequently, if \delta_{1}\leq 1/8 and ≤\alpha_{k}\leq(1-q)/(8h_{+}G) for all , then ∈​X_{k}\in\bar{U}_{\mathrm{St}(d,r)}(1/8) for all . Moreover, for any ,  
| ≤+60​.\sum_{k=1}^{K}\mathbb{E}[\delta_{k}^{2}]\leq 6\delta_{1}^{2}+60\sum_{k=1}^{K-1}\alpha_{k}^{2}\mathbb{E}\|d_{k}\|^{2}.  |  
| --- |  
In particular,  
| \displaystyle\sum_{k=1}^{K}\mathbb{E}[\delta_{k}^{2}]  | ≤+120​​​𝔼​+120​​​\displaystyle\leq 6\delta_{1}^{2}+120h_{+}^{2}\sum_{k=1}^{K-1}\alpha_{k}^{2}\mathbb{E}\|\hat{\nabla}f(X_{k})\|^{2}+120h_{+}^{2}\sigma^{2}\sum_{k=1}^{K-1}\alpha_{k}^{2}  |  
| --- | --- |  
| +480​​​.\displaystyle\quad+480h_{+}^{2}G^{2}\sum_{k=1}^{K-1}\alpha_{k}^{2}\beta_{1,k}^{2}.  |  
###### Proof.
By the definition of \bar{X}_{k+1} and the update (),  
| \displaystyle\delta_{k+1}  | =≤\displaystyle=\|X_{k+1}-\bar{X}_{k+1}\|\leq\|X_{k+1}-\bar{X}_{k}\|  |  
| --- | --- |  
| ≤‖−−‖+≤,\displaystyle\leq\left\|X_{k}-\frac{1}{3}\nabla\varphi(X_{k})-\bar{X}_{k}\right\|+\alpha_{k}\|d_{k}\|\leq q\delta_{k}+\alpha_{k}\|d_{k}\|,  |  
where the last inequality follows from the proof of Lemma . Since \|d_{k}\|\leq h_{+}G, the step-size condition gives ≤=\delta_{k+1}\leq q/8+(1-q)/8=1/8 whenever \delta_{k}\leq 1/8. Thus the iterates remain in ​\bar{U}_{\mathrm{St}(d,r)}(1/8) by induction.
Iterating () yields  
| ≤+.\delta_{k}\leq q^{k-1}\delta_{1}+\sum_{j=1}^{k-1}q^{k-1-j}\alpha_{j}\|d_{j}\|.  |  
| --- |  
Young’s convolution inequality gives  
| ≤+​.\sum_{k=1}^{K}\delta_{k}^{2}\leq\frac{2}{1-q^{2}}\delta_{1}^{2}+\frac{2}{(1-q)^{2}}\sum_{k=1}^{K-1}\alpha_{k}^{2}\|d_{k}\|^{2}.  |  
| --- |  
Since 2/(1-q^{2})=6 and 2/(1-q)^{2}<60, taking expectation proves (). Combining this estimate with Lemma proves (). ∎
######  (Adam-style alignment).
Under Condition , for every ,  
| ​≥−,:=,.\mathbb{E}_{k}\left\langle\hat{\nabla}f(X_{k}),d_{k}\right\rangle\geq a\|\hat{\nabla}f(X_{k})\|^{2}-R_{k},\qquad R_{k}:=\frac{\beta_{1,k}^{2}h_{+}^{2}G^{2}}{2(1-\bar{\beta})h_{-}},\qquad a:=\frac{(1-\bar{\beta})h_{-}}{2}.  |  
| --- |  
###### Proof.
Since and are \mathcal{F}_{k}-measurable and =\mathbb{E}_{k}[\tilde{g}_{k}]=\hat{\nabla}f(X_{k}),  
| ​\displaystyle\mathbb{E}_{k}\left\langle\hat{\nabla}f(X_{k}),d_{k}\right\rangle  | =​⟨,⟩\displaystyle=\mathbb{E}_{k}\left\langle\hat{\nabla}f(X_{k}),H_{k-1}m_{k}\right\rangle  |  
| --- | --- |  
| =​⟨,⟩+​⟨,⟩.\displaystyle=(1-\beta_{1,k})\left\langle\hat{\nabla}f(X_{k}),H_{k-1}\hat{\nabla}f(X_{k})\right\rangle+\beta_{1,k}\left\langle\hat{\nabla}f(X_{k}),H_{k-1}m_{k-1}\right\rangle.  |  
Using H_{k-1}\succeq h_{-}I, H_{k-1}\preceq h_{+}I, and \|m_{k-1}\|\leq G, we obtain  
| ​≥​​−​​G​.\mathbb{E}_{k}\left\langle\hat{\nabla}f(X_{k}),d_{k}\right\rangle\geq(1-\beta_{1,k})h_{-}\|\hat{\nabla}f(X_{k})\|^{2}-\beta_{1,k}h_{+}G\|\hat{\nabla}f(X_{k})\|.  |  
| --- |  
By Young’s inequality,  
| ​​G​≤​+.\beta_{1,k}h_{+}G\|\hat{\nabla}f(X_{k})\|\leq\frac{(1-\beta_{1,k})h_{-}}{2}\|\hat{\nabla}f(X_{k})\|^{2}+\frac{\beta_{1,k}^{2}h_{+}^{2}G^{2}}{2(1-\beta_{1,k})h_{-}}.  |  
| --- |  
Since \beta_{1,k}\leq\bar{\beta}, the claim follows. ∎
######  (One-step descent).
Suppose ∈​X_{k},X_{k+1}\in\bar{U}_{\mathrm{St}(d,r)}(1/8). Then  
| −\displaystyle\mathbb{E}_{k}[f(\bar{X}_{k+1})]-f(\bar{X}_{k})  | ≤−a​​+++\displaystyle\leq-a\alpha_{k}\|\hat{\nabla}f(X_{k})\|^{2}+\alpha_{k}R_{k}+\frac{1}{2}\mathbb{E}_{k}[\delta_{k+1}^{2}]+C_{\varphi}\delta_{k}^{2}  |  
| --- | --- |  
| +​​​.\displaystyle\quad+C_{0}\alpha_{k}^{2}\mathbb{E}_{k}\|d_{k}\|^{2}.  |  
where  
| :=4​++1,:=++,C_{0}:=4\max\\{\hat{L}^{2},\hat{D}_{f}^{2}\\}+4L+1,\qquad C_{\varphi}:=2\hat{D}_{f}+\frac{1}{2}\hat{L}^{2}+\frac{2}{3}C_{0},  |  
| --- |  
and are the constants in Lemma , :=maxX∈​⁡\hat{D}_{f}:=\max_{X\in\bar{U}_{\mathrm{St}(d,r)}(1/8)}\|\nabla f(X)\|, and is defined in Lemma .
###### Proof.
Let \Delta_{k}:=X_{k+1}-X_{k}, r_{k}:=X_{k}-\bar{X}_{k}, :=r_{k+1}:=X_{k+1}-\bar{X}_{k+1}, and :=\eta_{k}:=\operatorname{grad}f(\bar{X}_{k}). Averaging the quadratic upper bound in Lemma over the component functions gives  
| −≤+.f(\bar{X}_{k+1})-f(\bar{X}_{k})\leq\left\langle\eta_{k},\bar{X}_{k+1}-\bar{X}_{k}\right\rangle+\frac{L}{2}\|\bar{X}_{k+1}-\bar{X}_{k}\|^{2}.  |  
| --- |  
Writing the compact singular value decomposition of as U_{k}S_{k}V_{k}^{\top}, the polar projection is \bar{X}_{k}=U_{k}V_{k}^{\top}, and r_{k}=\bar{X}_{k}N_{k} with the symmetric matrix :=N_{k}:=V_{k}(S_{k}-I)V_{k}^{\top}. Since ∈\eta_{k}\in T_{\bar{X}_{k}}\mathrm{St}(d,r), we have \left\langle\eta_{k},r_{k}\right\rangle=0. Moreover, =\bar{X}_{k+1}-\bar{X}_{k}=\Delta_{k}+r_{k}-r_{k+1}, and hence  
| =+⟨,⟩−.\left\langle\eta_{k},\bar{X}_{k+1}-\bar{X}_{k}\right\rangle=\left\langle\hat{\nabla}f(X_{k}),\Delta_{k}\right\rangle+\left\langle\eta_{k}-\hat{\nabla}f(X_{k}),\Delta_{k}\right\rangle-\left\langle\eta_{k},r_{k+1}\right\rangle.  |  
| --- |  
By Lemma , applied with X=\bar{X}_{k} and and then averaged over the component functions,  
| ⟨,⟩≤+.\left\langle\eta_{k}-\hat{\nabla}f(X_{k}),\Delta_{k}\right\rangle\leq\frac{1}{2}\hat{L}^{2}\delta_{k}^{2}+\frac{1}{2}\|\Delta_{k}\|^{2}.  |  
| --- |  
Similarly, =r_{k+1}=\bar{X}_{k+1}N_{k+1} for a symmetric matrix with \|N_{k+1}\|=\delta_{k+1}. Since =0\left\langle\eta_{k},\bar{X}_{k}N_{k+1}\right\rangle=0, we have  
| \displaystyle\left|\left\langle\eta_{k},r_{k+1}\right\rangle\right|  | =|⟨,​⟩|\displaystyle=\left|\left\langle\eta_{k},(\bar{X}_{k+1}-\bar{X}_{k})N_{k+1}\right\rangle\right|  |  
| --- | --- |  
| ≤​​.\displaystyle\leq\|\eta_{k}\|\,\|\bar{X}_{k+1}-\bar{X}_{k}\|\,\delta_{k+1}.  |  
Since ∈​X_{k},X_{k+1}\in\bar{U}_{\mathrm{St}(d,r)}(1/8), their smallest singular values are at least . The standard perturbation bound for the polar factor therefore gives ≤=\|\bar{X}_{k+1}-\bar{X}_{k}\|\leq 2\|X_{k+1}-X_{k}\|=2\|\Delta_{k}\|. Together with ≤≤\|\eta_{k}\|\leq\|\nabla f(\bar{X}_{k})\|\leq\hat{D}_{f}, this gives  
| ≤2​​​≤+.\left|\left\langle\eta_{k},r_{k+1}\right\rangle\right|\leq 2\hat{D}_{f}\|\Delta_{k}\|\delta_{k+1}\leq 2\hat{D}_{f}^{2}\|\Delta_{k}\|^{2}+\frac{1}{2}\delta_{k+1}^{2}.  |  
| --- |  
The same projection Lipschitz estimate gives  
| ≤.\frac{L}{2}\|\bar{X}_{k+1}-\bar{X}_{k}\|^{2}\leq 2L\|\Delta_{k}\|^{2}.  |  
| --- |  
Combining these bounds with () yields  
| −≤+++.f(\bar{X}_{k+1})-f(\bar{X}_{k})\leq\left\langle\hat{\nabla}f(X_{k}),\Delta_{k}\right\rangle+\frac{1}{2}\delta_{k+1}^{2}+\frac{1}{2}\hat{L}^{2}\delta_{k}^{2}+\frac{C_{0}}{2}\|\Delta_{k}\|^{2}.  |  
| --- |  
From (), =−\Delta_{k}=-\alpha_{k}d_{k}-(1/3)\nabla\varphi(X_{k}). Thus  
| =−​−​⟨,⟩.\left\langle\hat{\nabla}f(X_{k}),\Delta_{k}\right\rangle=-\alpha_{k}\left\langle\hat{\nabla}f(X_{k}),d_{k}\right\rangle-\frac{1}{3}\left\langle\hat{\nabla}f(X_{k}),\nabla\varphi(X_{k})\right\rangle.  |  
| --- |  
The linear map A↦​A\mapsto P_{T_{X_{k}}\mathrm{St}(d,r)}(A) is self-adjoint with respect to the Frobenius inner product. Therefore,  
| ⟨,⟩=⟨,​⟩.\left\langle\hat{\nabla}f(X_{k}),\nabla\varphi(X_{k})\right\rangle=\left\langle\nabla f(X_{k}),P_{T_{X_{k}}\mathrm{St}(d,r)}(\nabla\varphi(X_{k}))\right\rangle.  |  
| --- |  
Let A_{k}=X_{k}^{\top}X_{k}-I. Since is symmetric,  
| ​=−​sym⁡=.P_{T_{X_{k}}\mathrm{St}(d,r)}(\nabla\varphi(X_{k}))=X_{k}A_{k}-X_{k}\operatorname{sym}(X_{k}^{\top}X_{k}A_{k})=-X_{k}A_{k}^{2}.  |  
| --- |  
Writing X_{k}=U_{k}S_{k}V_{k}^{\top}, the proof of Lemma gives 7/8\leq s_{i}\leq 9/8 for all singular values. Hence  
| ‖​‖=‖​​​‖≤.\|P_{T_{X_{k}}\mathrm{St}(d,r)}(\nabla\varphi(X_{k}))\|=\|U_{k}S_{k}(S_{k}^{2}-I)^{2}V_{k}^{\top}\|\leq 6\delta_{k}^{2}.  |  
| --- |  
Together with the definition of \hat{D}_{f}, this gives  
| −​⟨,⟩≤​​‖​‖≤.-\frac{1}{3}\left\langle\hat{\nabla}f(X_{k}),\nabla\varphi(X_{k})\right\rangle\leq\frac{1}{3}\|\nabla f(X_{k})\|\|P_{T_{X_{k}}\mathrm{St}(d,r)}(\nabla\varphi(X_{k}))\|\leq 2\hat{D}_{f}\delta_{k}^{2}.  |  
| --- |  
Furthermore,  
| ≤+≤+.\|\Delta_{k}\|^{2}\leq 2\alpha_{k}^{2}\|d_{k}\|^{2}+\frac{2}{9}\|\nabla\varphi(X_{k})\|^{2}\leq 2\alpha_{k}^{2}\|d_{k}\|^{2}+\frac{4}{3}\delta_{k}^{2}.  |  
| --- |  
Here the last inequality uses the bound ≤\|\nabla\varphi(X_{k})\|^{2}\leq 6\delta_{k}^{2} from the proof of Lemma . Substituting these estimates into () gives  
| −≤−​+++.f(\bar{X}_{k+1})-f(\bar{X}_{k})\leq-\alpha_{k}\left\langle\hat{\nabla}f(X_{k}),d_{k}\right\rangle+\frac{1}{2}\delta_{k+1}^{2}+C_{\varphi}\delta_{k}^{2}+C_{0}\alpha_{k}^{2}\|d_{k}\|^{2}.  |  
| --- |  
Taking conditional expectation and applying Lemma proves (). ∎
######  (Convergence of the Adam-style retraction-free update).
Suppose that Assumption and Condition hold. Let \\{X_{k}\\} be generated by (). Let :=inff_{\min}:=\inf_{X\in\mathrm{St}(d,r)}f(X)>-\infty. Assume \delta_{1}\leq 1/8 and  
| ≤min⁡{,},:=​[+],\alpha_{k}\leq\min\left\\{\frac{1-q}{8h_{+}G},\frac{a}{2C_{\star}}\right\\},\qquad C_{\star}:=h_{+}^{2}\left[2C_{0}+120\left(C_{\varphi}+\frac{1}{2}\right)\right],  |  
| --- |  
where is defined in Lemma , and and C_{\varphi} are defined in Lemma . Then all iterates stay in ​\bar{U}_{\mathrm{St}(d,r)}(1/8), and for every ,  
| ​𝔼​\displaystyle\sum_{k=1}^{K}\alpha_{k}\mathbb{E}\|\hat{\nabla}f(X_{k})\|^{2}  | ≤[f−+6+\displaystyle\leq\frac{2}{a}\Bigg[f(\bar{X}_{1})-f_{\min}+6\left(C_{\varphi}+\frac{1}{2}\right)\delta_{1}^{2}+C_{\star}\sigma^{2}\sum_{k=1}^{K}\alpha_{k}^{2}  |  
| --- | --- |  
| ++],\displaystyle\qquad\qquad+C_{\beta}\sum_{k=1}^{K}\alpha_{k}^{2}\beta_{1,k}^{2}+C_{R}\sum_{k=1}^{K}\alpha_{k}\beta_{1,k}^{2}\Bigg],  |  
where  
| :=​​[+],:=.C_{\beta}:=h_{+}^{2}G^{2}\left[8C_{0}+480\left(C_{\varphi}+\frac{1}{2}\right)\right],\qquad C_{R}:=\frac{h_{+}^{2}G^{2}}{2(1-\bar{\beta})h_{-}}.  |  
| --- |  
Moreover,  
| \displaystyle\sum_{k=1}^{K}\mathbb{E}[\delta_{k}^{2}]  | ≤+120​​​𝔼​+120​​​\displaystyle\leq 6\delta_{1}^{2}+120h_{+}^{2}\sum_{k=1}^{K-1}\alpha_{k}^{2}\mathbb{E}\|\hat{\nabla}f(X_{k})\|^{2}+120h_{+}^{2}\sigma^{2}\sum_{k=1}^{K-1}\alpha_{k}^{2}  |  
| --- | --- |  
| +480​​​.\displaystyle\quad+480h_{+}^{2}G^{2}\sum_{k=1}^{K-1}\alpha_{k}^{2}\beta_{1,k}^{2}.  |  
###### Proof.
The step-size condition and Lemma imply that all iterates remain in ​\bar{U}_{\mathrm{St}(d,r)}(1/8). Hence Lemma applies for k=1,\ldots,K. Summing () gives  
| −\displaystyle\mathbb{E}[f(\bar{X}_{K+1})]-f(\bar{X}_{1})  | ≤−a​​𝔼​+\displaystyle\leq-a\sum_{k=1}^{K}\alpha_{k}\mathbb{E}\|\hat{\nabla}f(X_{k})\|^{2}+\sum_{k=1}^{K}\alpha_{k}R_{k}  |  
| --- | --- |  
| +​++​.\displaystyle\quad+\frac{1}{2}\sum_{k=1}^{K}\mathbb{E}[\delta_{k+1}^{2}]+C_{\varphi}\sum_{k=1}^{K}\mathbb{E}[\delta_{k}^{2}]+C_{0}\sum_{k=1}^{K}\alpha_{k}^{2}\mathbb{E}\|d_{k}\|^{2}.  |  
Since f(\bar{X}_{K+1})\geq f_{\min},  
| a​​𝔼​\displaystyle a\sum_{k=1}^{K}\alpha_{k}\mathbb{E}\|\hat{\nabla}f(X_{k})\|^{2}  | ≤++​\displaystyle\leq f(\bar{X}_{1})-f_{\min}+\sum_{k=1}^{K}\alpha_{k}R_{k}+\left(C_{\varphi}+\frac{1}{2}\right)\sum_{k=1}^{K+1}\mathbb{E}[\delta_{k}^{2}]  |  
| --- | --- |  
| +​.\displaystyle\quad+C_{0}\sum_{k=1}^{K}\alpha_{k}^{2}\mathbb{E}\|d_{k}\|^{2}.  |  
By Lemmas and ,  
| ≤2​​𝔼​++,\mathbb{E}\|d_{k}\|^{2}\leq 2h_{+}^{2}\mathbb{E}\|\hat{\nabla}f(X_{k})\|^{2}+2h_{+}^{2}\sigma^{2}+8h_{+}^{2}\beta_{1,k}^{2}G^{2},  |  
| --- |  
and  
| \displaystyle\sum_{k=1}^{K+1}\mathbb{E}[\delta_{k}^{2}]  | ≤+120​​​𝔼​+120​​​\displaystyle\leq 6\delta_{1}^{2}+120h_{+}^{2}\sum_{k=1}^{K}\alpha_{k}^{2}\mathbb{E}\|\hat{\nabla}f(X_{k})\|^{2}+120h_{+}^{2}\sigma^{2}\sum_{k=1}^{K}\alpha_{k}^{2}  |  
| --- | --- |  
| +480​​​.\displaystyle\quad+480h_{+}^{2}G^{2}\sum_{k=1}^{K}\alpha_{k}^{2}\beta_{1,k}^{2}.  |  
Substituting these two estimates into () yields  
| a​​𝔼​\displaystyle a\sum_{k=1}^{K}\alpha_{k}\mathbb{E}\|\hat{\nabla}f(X_{k})\|^{2}  | ≤++​​𝔼​\displaystyle\leq f(\bar{X}_{1})-f_{\min}+6\left(C_{\varphi}+\frac{1}{2}\right)\delta_{1}^{2}+C_{\star}\sum_{k=1}^{K}\alpha_{k}^{2}\mathbb{E}\|\hat{\nabla}f(X_{k})\|^{2}  |  
| --- | --- |  
| +​+.\displaystyle\quad+C_{\star}\sigma^{2}\sum_{k=1}^{K}\alpha_{k}^{2}+C_{\beta}\sum_{k=1}^{K}\alpha_{k}^{2}\beta_{1,k}^{2}+\sum_{k=1}^{K}\alpha_{k}R_{k}.  |  
Since \alpha_{k}\leq a/(2C_{\star}), we have ≤C_{\star}\alpha_{k}^{2}\leq(a/2)\alpha_{k}. Moving this term to the left-hand side gives  
| ​​𝔼​≤+++​+.\frac{a}{2}\sum_{k=1}^{K}\alpha_{k}\mathbb{E}\|\hat{\nabla}f(X_{k})\|^{2}\leq f(\bar{X}_{1})-f_{\min}+6\left(C_{\varphi}+\frac{1}{2}\right)\delta_{1}^{2}+C_{\star}\sigma^{2}\sum_{k=1}^{K}\alpha_{k}^{2}+C_{\beta}\sum_{k=1}^{K}\alpha_{k}^{2}\beta_{1,k}^{2}+\sum_{k=1}^{K}\alpha_{k}R_{k}.  |  
| --- |  
Finally, R_{k}=C_{R}\beta_{1,k}^{2}, which proves (). The feasibility estimate () is exactly (). ∎
######  (Iteration complexity).
Suppose the assumptions of Theorem hold.
_(i)_ If \alpha_{k}\equiv\alpha and  
| α≤min⁡{1,,},\alpha\leq\min\left\\{1,\frac{1-q}{8h_{+}G},\frac{a}{2C_{\star}}\right\\},  |  
| --- |  
| ​=O​(++).\frac{1}{K}\sum_{k=1}^{K}\mathbb{E}\|\hat{\nabla}f(X_{k})\|^{2}=O\left(\frac{1}{\alpha K}+\alpha\sigma^{2}+\frac{1}{K}\sum_{k=1}^{K}\beta_{1,k}^{2}\right).  |  
| --- |  
If, in addition, \beta_{1,k}=\beta_{1}\lambda^{k-1} with \lambda\in(0,1), then  
| ​=,\frac{1}{K}\sum_{k=1}^{K}\mathbb{E}\|\hat{\nabla}f(X_{k})\|^{2}=O\left(\frac{1}{\alpha K}+\alpha\sigma^{2}\right),  |  
| --- |  
| ​=.\frac{1}{K}\sum_{k=1}^{K}\mathbb{E}[\delta_{k}^{2}]=O\left(\frac{1}{K}+\alpha^{2}\sigma^{2}\right).  |  
| --- |  
In particular, in the full-gradient case and with geometrically decaying \beta_{1,k},  
| ​𝔼​[+]=.\frac{1}{K}\sum_{k=1}^{K}\mathbb{E}\bigl[\|\hat{\nabla}f(X_{k})\|^{2}+\delta_{k}^{2}\bigr]=O\left(\frac{1}{K}\right).  |  
| --- |  
_(ii)_ If \alpha_{k}=\alpha_{0}/\sqrt{k} with  
| ≤min⁡{,},\alpha_{0}\leq\min\left\\{\frac{1-q}{8h_{+}G},\frac{a}{2C_{\star}}\right\\},  |  
| --- |  
and if \beta_{1,k}=\beta_{1}\lambda^{k-1} with \lambda\in(0,1), then  
| ⁡=,​=.\min_{1\leq k\leq K}\mathbb{E}\|\hat{\nabla}f(X_{k})\|^{2}=O\left(\frac{\log K}{\sqrt{K}}\right),\qquad\frac{1}{K}\sum_{k=1}^{K}\mathbb{E}[\delta_{k}^{2}]=O\left(\frac{\log K}{K}\right).  |  
| --- |  
Consequently,  
| ​[+]=.\min_{1\leq k\leq K}\mathbb{E}\bigl[\|\hat{\nabla}f(X_{k})\|^{2}+\delta_{k}^{2}\bigr]=O\left(\frac{\log K}{\sqrt{K}}\right).  |  
| --- |  
###### Proof.
For constant \alpha_{k}\equiv\alpha, dividing () by gives  
| ​=O​(+++).\frac{1}{K}\sum_{k=1}^{K}\mathbb{E}\|\hat{\nabla}f(X_{k})\|^{2}=O\left(\frac{1}{\alpha K}+\alpha\sigma^{2}+\frac{\alpha}{K}\sum_{k=1}^{K}\beta_{1,k}^{2}+\frac{1}{K}\sum_{k=1}^{K}\beta_{1,k}^{2}\right).  |  
| --- |  
Since \alpha\leq 1, this implies the stated stationarity estimate. If \beta_{1,k}=\beta_{1}\lambda^{k-1}, then =\sum_{k=1}^{K}\beta_{1,k}^{2}=O(1), and hence the averaged stationarity estimate reduces to  
| ​=.\frac{1}{K}\sum_{k=1}^{K}\mathbb{E}\|\hat{\nabla}f(X_{k})\|^{2}=O\left(\frac{1}{\alpha K}+\alpha\sigma^{2}\right).  |  
| --- |  
Substituting this bound into () gives  
| ​=O​(+​++).\frac{1}{K}\sum_{k=1}^{K}\mathbb{E}[\delta_{k}^{2}]=O\left(\frac{1}{K}+\alpha^{2}\cdot\frac{1}{K}\sum_{k=1}^{K}\mathbb{E}\|\hat{\nabla}f(X_{k})\|^{2}+\alpha^{2}\sigma^{2}+\frac{\alpha^{2}}{K}\sum_{k=1}^{K}\beta_{1,k}^{2}\right).  |  
| --- |  
Using \alpha\leq 1 and the geometrically decaying momentum sequence, this yields  
| ​=.\frac{1}{K}\sum_{k=1}^{K}\mathbb{E}[\delta_{k}^{2}]=O\left(\frac{1}{K}+\alpha^{2}\sigma^{2}\right).  |  
| --- |  
The full-gradient statement follows by setting .
For \alpha_{k}=\alpha_{0}/\sqrt{k}, we have \sum_{k=1}^{K}\alpha_{k}=\Omega(\sqrt{K}) and =\sum_{k=1}^{K}\alpha_{k}^{2}=O(\log K). If \beta_{1,k} decays geometrically, then  
| =,=.\sum_{k=1}^{K}\alpha_{k}^{2}\beta_{1,k}^{2}=O(1),\qquad\sum_{k=1}^{K}\alpha_{k}\beta_{1,k}^{2}=O(1).  |  
| --- |  
Dividing () by \sum_{k=1}^{K}\alpha_{k} gives  
| ⁡≤​𝔼​=.\min_{1\leq k\leq K}\mathbb{E}\|\hat{\nabla}f(X_{k})\|^{2}\leq\frac{\sum_{k=1}^{K}\alpha_{k}\mathbb{E}\|\hat{\nabla}f(X_{k})\|^{2}}{\sum_{k=1}^{K}\alpha_{k}}=O\left(\frac{\log K}{\sqrt{K}}\right).  |  
| --- |  
Moreover, since \alpha_{k}^{2}\leq\alpha_{0}\alpha_{k}, substituting the same step sizes into () yields  
| =,\sum_{k=1}^{K}\mathbb{E}[\delta_{k}^{2}]=O(\log K),  |  
| --- |  
and hence  
| ​=.\frac{1}{K}\sum_{k=1}^{K}\mathbb{E}[\delta_{k}^{2}]=O\left(\frac{\log K}{K}\right).  |  
| --- |  
Finally, since \alpha_{k}\leq\alpha_{0},  
| ≤​=.\sum_{k=1}^{K}\alpha_{k}\mathbb{E}[\delta_{k}^{2}]\leq\alpha_{0}\sum_{k=1}^{K}\mathbb{E}[\delta_{k}^{2}]=O(\log K).  |  
| --- |  
Combining this estimate with the weighted stationarity estimate and dividing by \sum_{k=1}^{K}\alpha_{k}=\Omega(\sqrt{K}) gives  
| ​[+]=.\min_{1\leq k\leq K}\mathbb{E}\bigl[\|\hat{\nabla}f(X_{k})\|^{2}+\delta_{k}^{2}\bigr]=O\left(\frac{\log K}{\sqrt{K}}\right).  |  
| --- |  
∎
######  (Bias correction and standard Adam notation).
If bias-corrected moments are used while keeping the preconditioner delayed, for example by replacing and \bar{v}_{k-1} with their corresponding bias-corrected versions in the definition of , then the same proof applies after replacing , , and by enlarged constants. Thus bias correction affects only constants and not the convergence rates. The non-delayed version that uses the current adaptive preconditioner depending on \tilde{g}_{k} is not covered by the conditional-independence argument above.
######  (Constant first-momentum parameter).
If \beta_{1,k}\equiv\beta_{1} is kept constant, then the same proof yields, for constant step size,  
| ​=O​().\frac{1}{K}\sum_{k=1}^{K}\mathbb{E}\|\hat{\nabla}f(X_{k})\|^{2}=O\left(\frac{1}{\alpha K}+\alpha\sigma^{2}+\beta_{1}^{2}\right).  |  
| --- |  
Thus, under this proof strategy, a non-vanishing first-momentum parameter leads to convergence to a neighborhood. To obtain convergence to a stationary point, one may use a decaying momentum sequence, for example \beta_{1,k}=\beta_{1}\lambda^{k-1} with \lambda\in(0,1).
