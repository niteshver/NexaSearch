arXiv is now an independent nonprofit! Learn more ×
License: CC BY 4.0 
arXiv:2607.16891v2 [cs.LG] 28 Jul 2026
# Certified-Gap Dual-Price Policies for Real-Time Truckload Bid Acceptance with Relocating, Clock-Constrained Resources††thanks: Evaluation artifact: FreightBidBench (freightbidbench-v0.4-dev); .
Aswin Chandrasekaran Bubba AI aswin@bubba.ai
(July 2026)
###### Abstract
A truckload carrier must accept or reject each load tender within seconds. The decision depends on fleet state, hours-of-service (HOS) clocks, and appointment windows. We model this as a weakly coupled dynamic program in which the resources relocate and carry clocks: serving a request moves the truck to a new market and depletes its clocks, and whether a truck can serve a request depends on its state. Occupancy-based reusable-resource models do not cover this setting.
We build a real-time dual-price policy from the same Lagrangian relaxation that gives the problem’s upper bound. Policy and bound come from one object, so every run reports a _certified optimality gap_. We prove three things. First, the certificate is valid for any duals, any discretization, and any surrogate quality (Theorem 1). Second, the policy’s same-time spatial-gradient rule is exactly fluid complementary slackness, and the policy is asymptotically optimal in the subcritical fluid regime (Theorem 2); the fitted prices are also portable across sample paths, by linear-programming basis stability. Third, certificates have limits: per-resource Lagrangian slack can stay bounded away from zero at every fleet size. We exhibit a three-truck kernel with an exact rational certificate and a replication lemma (Theorem 3).
On a public closed-loop benchmark with thirty paired seeds, the policy — which needs no rollout labels, only one offline dual solve — beats a rollout-trained surrogate on two of three scenarios (tight: pp, 95% CI [+0.5,+3.6], Wilcoxon p=0.023; mild: pp, CI [+2.4,+4.5]) and ties the third. It decides in 0.04–0.09 ms, three orders of magnitude faster than the Monte Carlo rollout teacher. Its certificates are stable across ten bounded instances per scenario, at 57–64% of optimal, within 3–6 points of what the 1000\times-slower teacher certifies.
##  Introduction
Truckload carriers and brokers answer a stream of load tenders in real time. Each acceptance consumes a truck. The truck’s future usefulness depends on where the load delivers it and how much drive and duty time its driver has left. Each rejection preserves options whose value depends on demand that has not arrived yet. The operational state of the art for future-aware acceptance is finite-lookahead Monte Carlo rollout (Bertsekas et al., 1997; Goodson et al., 2017). Rollout is accurate, but it costs tens to hundreds of milliseconds per decision and scales poorly with fleet size. The benchmark release preceding this paper, FreightBidBench v0.3 (Chandrasekaran, 2026), made the problem reproducible: public calibration, explicit feasibility, versioned contracts, and hindsight upper bounds. Its best latency-aware method, a surrogate-to-rollout cascade, recovers about of rollout profit. But the cascade inherits rollout’s cost on escalated decisions, and it says nothing about distance from _optimal_.
This paper asks the harder question the bounds make precise: how close to optimal can a real-time policy be, and can that closeness be certified per instance? We use one object twice. The Lagrangian relaxation that dualizes the one-truck-per-load constraint gives two things: an upper bound on the optimum (the benchmark’s ceiling), and per-load dual prices with per-market value potentials. From the prices and potentials we build an acceptance policy that runs in microseconds. Policy and bound come from the same relaxation, so every run ships with an observable certificate: realized policy value against realized bound.
Pairing a Lagrangian policy with its bound is the standard playbook of weakly coupled dynamic programs. The contribution here is the model in which we make it work, the form of the policy, and the two-sided theory of the certificate:
  1. 1.
Resources whose _service capability is a controlled state variable_ : state-dependent feasibility (location and HOS clocks gate eligibility), controlled relocation (service moves the resource), autonomous renewal (rests restore clocks). None of the adjacent literatures — classical weakly coupled DPs, online reusable resources, dual mirror descent for online allocation — covers this combination (§).
  2. 2.
Policy and certificate (§–). A dual-price policy whose relocation term is a _same-time spatial gradient_ of an aggregated value surface, plus Theorem : the certificate is valid for any duals, any permissive discretization, and any surrogate quality. Lemma isolates the design rule — _duals price time, gradients price place_ — and we show the naive continuation rule collapses (14% of rollout) by double-charging the busy-time cost.
  3. 3.
Fluid consistency and portability (§). In the subcritical regime the gradient rule is exactly fluid complementary slackness (Lemma : idle potentials are flat), fitted prices converge to fluid rents (Lemma ), and the policy is asymptotically optimal (Theorem ). Basis stability further implies the fitted prices are _portable across days_ — a prediction the benchmark confirms — and predicts where portability fails.
  4. 4.
Limits of the relaxation (§). Certificates cannot promise everything: we exhibit a three-truck kernel with a certified duality gap (exact rational arithmetic; a half-integral chain packing beats the integer optimum) and a replication lemma, so per-resource Lagrangian slack can stay bounded away from zero at every fleet size (Theorem ). Random search shows such kernels are rare at small scale ({\sim}0.03\%), while the benchmark’s proportional-scaling experiments show per-resource bound slack _growing_ with contention density — behavior outside the conditions under which classical -gap results hold.
  5. 5.
Thirty paired seeds, paired-bootstrap confidence intervals: the training-free policy beats a rollout-trained linear surrogate on the tight ( pp, CI95 [+0.5,+3.6]) and mild ( pp) scenarios and ties it on scarce, at lower latency than the surrogate and {\sim}1000\times below rollout; certificates over ten independently bounded instances per scenario are stable (57–64% of optimal) and within 3–6 points of what rollout itself certifies — locating most of the certified gap in the relaxation, not the policy.


###  1.1 Related work
#### Weakly coupled DPs and bid prices.
Bid-price control goes back to Talluri and van Ryzin (1998), and fluid policies are asymptotically optimal in classical network revenue management (Gallego and van Ryzin, 1997). Dynamic bid prices from value approximations originate with Adelman (2007); Lagrangian decompositions of weakly coupled DPs with policy-plus-bound pairing are standard since Hawkins (2003), Adelman and Mersereau (2008), and Topaloglu (2009), who reports bounds and policy values on the same network revenue-management instances. Our Theorem inherits this mechanism; the extension is the resource model (state-dependent participation, controlled relocation) and the explicit certificate framing at sub-millisecond decision latency. Brown and Zhang (2023) bound the ALP–Lagrangian gap by constants under conditions on linking constraints that state-dependence violates; Theorem and the scaling experiments exhibit exactly the behavior their conditions exclude — in the natural freight scaling, constraints number and participation is state-dependent.
#### Online reusable resources.
Zhang and Cheung (2022) and the nonstationary extension Zhang and Cheung (2025) allocate units that occupy and _return unchanged_ ; feasibility is availability-only. A truck returns elsewhere with depleted clocks. This controlled-relocation distinction is not cosmetic: it is what makes the chain-packing LP non-integral (§) and what the same-time gradient prices (§).
#### Online allocation with dual updates.
Balseiro et al. (2020) attain optimal O(\sqrt{T}) regret with per-request dual mirror descent for consumable budgets, motivated by the same latency constraints. We deliberately make no regret-rate claim — rates are a settled literature — and instead fix prices offline (portable by Lemma ), leaving the online variant to future work. A related alternative is re-solving a deterministic LP at each decision epoch, which achieves excellent regret in network revenue management; we do not pursue it because per-decision LP solves conflict with the benchmark’s dependency-free runtime and with microsecond latency at growing instance sizes, but a re-solving baseline is a fair addition for future comparisons.
#### Fleet ADP.
Industrial-scale approximate dynamic programming for truckload fleets (Simão et al., 2009; Powell et al., 2012) produced marginal values of driver states as model outputs, aimed at calibration rather than optimality certification; no per-instance gap reporting appears in this line.
#### Compute-aware deferral.
Value-of-computation metareasoning for Monte Carlo search (Sezener and Dayan, 2020) has not been applied to operational dispatch; the benchmark’s cascade and this paper’s policy make the freight instantiation available, and we leave a learned deferral rule to future work.
##  Model
###  2.1 Resources, requests, and controlled relocation
Time is continuous on . A fleet of resources (trucks) has states =∈𝒳x_{k}=(m_{k},\tau_{k},h_{k},d_{k})\in\mathcal{X}: market in a finite set ℳ\mathcal{M}, next-available time , and remaining drive/duty clocks . Requests (load tenders) \ell_{1},\dots,\ell_{N} arrive at times t_{1}<\dots<t_{N}; request carries origin , destination \delta(\ell), and the price/cost/window attributes of the benchmark. Three primitives distinguish the model from occupancy-based reusable resources:
  * •
State-dependent feasibility. A deterministic map f(x,\ell)\in\\{0,1\\} gates service: only if the resource is in the origin market, can reach the pickup inside its appointment window, and admits an HOS-feasible schedule through delivery.
  * •
Controlled relocation. Serving executes x\leftarrow S(x,\ell): the market becomes \delta(\ell), the next-available time advances by the service duration including inserted rests, and the clocks are updated by the drive/duty consumed. The resource does not return to its prior pool state.
  * •
Autonomous renewal. Idle resources renew clocks by resting (11/14/10 in the benchmark), so service capability regenerates in a state-dependent way.


###  2.2 Joint control problem
At each arrival the controller observes (x_{1},\dots,x_{K},\ell_{t}) and chooses a_{t}\in\\{0,1,\dots,K\\} (reject, or assign to a feasible resource). Writing a^{(k)}_{t}=\mathbf{1}\\{a_{t}=k\\}, the joint constraint is \sum_{k}a^{(k)}_{t}\leq 1. Rewards follow the benchmark: realized profit r(x_{k},\ell_{t}) on feasible accepts, a service-failure penalty on infeasible attempts (excluded under -gated policies), and terminal fleet value Φ=\Phi=\omega\sum_{k}V(m_{k}(T)). V^{\star}(\xi) denotes the optimum on realized scenario .
###  2.3 Lagrangian per-resource decomposition
Dualizing the joint constraint with prices \lambda=(\lambda_{t})\geq 0,  
| =+,L(\lambda;\xi)\;=\;\sum_{t}\lambda_{t}\;+\;\sum_{k}V^{k}_{\lambda}(x_{k}(0);\xi),  |  
| --- |  
where V^{k}_{\lambda} is resource ’s optimal value in its own sub-MDP with per-service reward r(x,\ell_{t})-\lambda_{t}, retaining , , and renewal. Weak duality gives V^{\star}(\xi)\leq L(\lambda;\xi) pointwise for every \lambda\geq 0 (see also the information-relaxation view of Brown et al., 2010). The benchmark computes by bucketed forward enumeration with a permissive-corner guarantee, so reported bounds remain valid under discretization; per-resource solves parallelize (standard-library multiprocessing; bit-identical to serial).
##  The Dual-Price Policy
###  3.1 Price and value surrogates
Fix any duals \lambda\geq 0 (in practice subgradient iterates on one training scenario; policy quality saturates well before bound-grade convergence). Two aggregated objects are fitted offline:
  * •
A price surface: the mean dual of loads, fitted either by origin market and hour-of-day (:\hat{\lambda}:\mathcal{M}\times[0,24)\to\mathbb{R}_{\geq 0}, the deployed table — lower variance, uses shrinkage) or at lane–hour granularity (the variant analyzed in §; Lemma identifies its limit as the fluid rent \mathbb{E}[(r-\theta_{\ell h})_{+}], and Remark quantifies the gap between the two).
  * •
An aggregated value-to-go \widehat{W}:\mathcal{M}\times[0,T]\to\mathbb{R}: the backward recursion on the (market, hour) grid over the realized train stream with dual-netted rewards, =\widehat{W}(m,T)=\omega V(m) and  
| =max⁡(,max⁡[+​(,)]).\widehat{W}(m,t)=\max\Bigl(\widehat{W}(m,t{+}1),\;\max_{\ell:\,t_{\ell}=t,\,o(\ell)=m}\bigl[\bar{r}(\ell)-\lambda_{t_{\ell}}+\widehat{W}(\delta(\ell),t+tt(\ell))\bigr]\Bigr).  |  
| --- |  


###  3.2 Policy
The policy makes three moves at each arrival at time .
_Step 1: pick the truck._ Among feasible trucks, take the one with the best realized profit:  
| ∈arg⁡max⁡r​.k^{\star}\in\arg\max_{k:\,f(x_{k},\ell)=1}r(x_{k},\ell).  |  
| --- |  
_Step 2: price the decision._ Compute the relocation gain first. It is the value of standing at the destination instead of the origin, compared at the same time t+tt(\ell):  
| =​(,)−​(,).g(\ell,t)\;=\;\widehat{W}\bigl(\delta(\ell),t+tt(\ell)\bigr)\;-\;\widehat{W}\bigl(o(\ell),t+tt(\ell)\bigr).  |  
| --- |  
Then start from the profit, subtract the price of the slot, and add the relocation gain:  
| score=−+.\mathrm{score}\;=\;r(x_{k^{\star}},\ell)\;-\;\hat{\lambda}(o(\ell),t)\;+\;g(\ell,t).  |  
| --- |  
_Step 3: decide._ Accept with iff \mathrm{score}\geq 0.
We call the same-time spatial gradient. The busy-time opportunity cost is priced once, by \hat{\lambda}; prices only the change of place. Each decision costs a feasibility probe over origin-market resources plus two table lookups. Measured latency is 0.02–0.12 ms across fleet sizes 35–140, versus 32–159 ms for rollout. The advantage widens with fleet size.
##  Certified Gaps
######  (Certified optimality gap).
Let \lambda\geq 0 be arbitrary and any feasible policy. On every realized scenario , ≤≤V^{\pi}(\xi)\leq V^{\star}(\xi)\leq L(\lambda;\xi), hence the observable :=\mathrm{gap}(\xi):=L(\lambda;\xi)-V^{\pi}(\xi) upper-bounds the true suboptimality V^{\star}(\xi)-V^{\pi}(\xi). The certificate remains valid for any , any per-resource discretization with the permissive-corner property, and any quality of \hat{\lambda},\widehat{W} — surrogate error degrades policy value (widening the reported gap) but never invalidates the certificate.
###### Proof.
We argue in four short steps.
_Step 1._ V^{\pi}(\xi)\leq V^{\star}(\xi). The policy is feasible, and V^{\star}(\xi) is by definition the best value any feasible policy attains on .
_Step 2._ V^{\star}(\xi)\leq L(\lambda;\xi). This is weak duality for the dualized joint constraint (§), and it holds pointwise in for every \lambda\geq 0.
_Step 3._ Subtract V^{\pi}(\xi) from both sides of Step 2:  
| ≤=.V^{\star}(\xi)-V^{\pi}(\xi)\;\leq\;L(\lambda;\xi)-V^{\pi}(\xi)\;=\;\mathrm{gap}(\xi).  |  
| --- |  
_Step 4._ The invariances. The certificate uses two ingredients only: an upper bound computed from (\lambda,\xi), and the realized value of a feasible policy. Neither ingredient depends on how was chosen, on the discretization (which can only enlarge the bound, by the permissive-corner property), or on the quality of \hat{\lambda},\widehat{W} (which affect only which feasible policy we happen to run). ∎
######  (Exactness).
If \lambda^{\star} minimizes L(\cdot\,;\xi) and the per-resource optimizers jointly satisfy primal feasibility and complementary slackness, the assembled plan is optimal and the certificate is tight.
In practice a residual contested set (– of loads) keeps the certificate strictly positive.
######  (Busy-time double-counting).
In recursion (), −\widehat{W}(m,t)-\widehat{W}(m,t^{\prime}) equals the value of the best dual-netted service chain departing within [t,t^{\prime}); hence the naive continuation −\widehat{W}(\delta,t{+}tt)-\widehat{W}(o,t) subtracts the busy-time opportunity value a second time on top of \hat{\lambda}, while the same-time gradient prices relocation alone.
###### Proof.
Define two quantities at the decision (\ell,t) with t^{\prime}=t+tt(\ell):  
| =−,=−.D_{\mathrm{time}}\;=\;\widehat{W}(o,t)-\widehat{W}(o,t^{\prime}),\qquad D_{\mathrm{place}}\;=\;\widehat{W}(\delta,t^{\prime})-\widehat{W}(o,t^{\prime}).  |  
| --- |  
_Step 1._ D_{\mathrm{time}}\geq 0, and it equals the value of the best dual-netted chain departing within [t,t^{\prime}). The wait branch of recursion () makes \widehat{W}(m,\cdot) non-increasing; unrolling the max branches over the window expresses the drop as exactly the best chain the truck gives up by being busy.
_Step 2._ The naive continuation splits into the two parts:  
| −=−.\widehat{W}(\delta,t^{\prime})-\widehat{W}(o,t)\;=\;D_{\mathrm{place}}-D_{\mathrm{time}}.  |  
| --- |  
This is a one-line check: add and subtract \widehat{W}(o,t^{\prime}).
_Step 3._ The duals already price the busy time on the contested stream, so a rule that uses the naive continuation charges D_{\mathrm{time}} a second time on top of \hat{\lambda}. The same-time gradient g=D_{\mathrm{place}} charges the relocation only. ∎
Empirically the naive rule collapses to 14–17% of rollout profit; the gradient rule attains 90–95% (§). _Duals price time; gradients price place._ Lemma upgrades this from a property of our construction to fluid optimality.
##  Fluid Consistency in the Subcritical Regime
###  5.1 Fluid model and its LP
Scale fleet and demand together: resources, lane- arrivals a Poisson process of rate K\Lambda_{\ell}(t) with mean reward \bar{r}_{\ell} and occupation time \tau_{\ell} (travel plus any inserted rests, so a resource completing service is idle with renewed clocks), lane geometry fixed. Normalize per resource. The fluid control problem routes resource mass over the market–time network: dispatch rates ∈u_{\ell}(t)\in[0,\Lambda_{\ell}(t)] with u_{\ell}\equiv 0 on , idle mass z_{m}(t)\geq 0 with initial condition z_{m}(0)=z^{0}_{m}, \sum_{m}z^{0}_{m}=1 (the benchmark’s proportional placement), and flow balance  
| =−,max​+ω​,\dot{z}_{m}(t)=\sum_{\ell:\,\delta_{\ell}=m}u_{\ell}(t-\tau_{\ell})-\sum_{\ell:\,o_{\ell}=m}u_{\ell}(t),\qquad\max\int_{0}^{T}\sum_{\ell}\bar{r}_{\ell}u_{\ell}(t)\,dt+\omega\sum_{m}V(m)\,\tilde{z}_{m}(T),  |  
| --- |  
where =+\tilde{z}_{m}(T)=z_{m}(T)+\sum_{\ell:\,\delta_{\ell}=m}\int_{T-\tau_{\ell}}^{T}u_{\ell}(t)\,dt credits in-transit mass at its destination, matching the model’s terminal value Φ=\Phi=\omega\sum_{k}V(m_{k}(T)) (a served resource’s market is its destination). For dispatch arcs landing after the horizon, extend the potentials by :=w_{m}(s):=\omega V(m) for ; at the horizon the terminal dual condition is w_{m}(T)\geq\omega V(m), with equality where z_{m}(T)>0 — so under (A1) below, equality holds everywhere. This display is the continuous fluid limit, in the tradition of revenue management (Gallego and van Ryzin, 1997); all quantitative statements below are made on its _hourly_ finite LP (cells (\ell,h), the fitting granularity), whose optimal basis is well-defined, and _basis switches_ mean changes of that optimal basis across hour boundaries. Write for the value and (,)(\lambda_{\ell}(t),w_{m}(t)) for optimal duals on the arrival-capacity and flow-balance constraints. Dual feasibility on dispatch and waiting arcs reads  
| ≥+,with equality on used arcs,\displaystyle w_{o_{\ell}}(t)\geq\bar{r}_{\ell}-\lambda_{\ell}(t)+w_{\delta_{\ell}}(t+\tau_{\ell}),\quad\text{with equality on used arcs},  |  
| --- |  
| ,with equality where ​​0.\displaystyle w_{m}(t)\geq w_{m}(t^{+}),\quad\text{with equality where }z_{m}(t)>0.  |  
######  .
(A1) Subcriticality: the fluid optimum keeps z_{m}(t)\geq z_{\min}>0 for all . (A2) Clock and window slack, as a primitive condition on model data: occupation times \tau_{\ell} are rest-inclusive, so a resource completing service is idle with renewed clocks, and an idle in-market resource is service-feasible for any in-market arrival (appointment windows and pickup reach are non-binding for idle local resources). (A3) Nondegeneracy: the hourly fluid LP has a unique, nondegenerate optimal basis on each hour cell. (A4) Bounded rewards and occupation times; \Lambda_{\ell}(\cdot) and \bar{r}_{\ell}(\cdot) constant on hourly cells (the benchmark’s periodic hourly schedule), so all basis switches occur at hour boundaries and no cell straddles one; and rewards within a lane–hour cell are homogeneous at scale (r\equiv\bar{r}_{\ell}(h) up to dispersion). Remark discusses heterogeneous rewards.
######  (Idle potentials are pinned; the gradient rule is fluid CS).
(i) Under (A1), (D2) holds with equality for all , and combined with the terminal condition this pins the potentials globally: w_{m}(t)\equiv\omega V(m) on . Consequently the fluid-optimal acceptance rule — serve lane at iff +−≥0\bar{r}_{\ell}-\lambda_{\ell}(t)+w_{\delta_{\ell}}(t+\tau_{\ell})-w_{o_{\ell}}(t)\geq 0 — coincides with the same-time spatial-gradient rule  
| +[−]≥0.\bar{r}_{\ell}-\lambda_{\ell}(t)+\bigl[w_{\delta_{\ell}}(t+\tau_{\ell})-w_{o_{\ell}}(t+\tau_{\ell})\bigr]\geq 0.  |  
| --- |  
(ii) Locally, without (A1): if on [t,t+\tau] then =w_{o}(t)=w_{o}(t+\tau), and the naive-continuation penalty −w_{o}(t)-w_{o}(t+\tau) is exactly the binding-capacity correction: zero where idle mass persists, strictly positive where capacity binds.
###### Proof.
_Step 1._ Complementary slackness on a waiting arc says: if idle mass sits at , the arc is used, so its dual inequality (D2) is tight. Under (A1) idle mass is positive everywhere, so (D2) holds with equality everywhere: w_{m}(\cdot) cannot drop at any time, including hour boundaries, so it is constant on .
_Step 2._ Terminal complementary slackness: z_{m}(T)\geq z_{\min}>0 makes the terminal dual condition tight, w_{m}(T)=\omega V(m). With Step 1, w_{m}(t)\equiv\omega V(m).
_Step 3._ Take the fluid-optimal rule from (D1), +−≥0\bar{r}_{\ell}-\lambda_{\ell}(t)+w_{\delta_{\ell}}(t+\tau_{\ell})-w_{o_{\ell}}(t)\geq 0, and replace w_{o_{\ell}}(t) by w_{o_{\ell}}(t+\tau_{\ell}) using Step 1. This gives rule () exactly.
_Step 4._ For (ii): where capacity binds () Step 1 fails, (D2) can be strict, and the replaced term −w_{o}(t)-w_{o}(t+\tau) becomes a positive correction. That correction is what the naive rule charges even when capacity is slack. ∎
######  (What the subcritical limit does and does not certify).
Under (A1) the potentials are constants pinned to terminal values, so in the fluid _limit_ the naive continuation −w_{o}(t)-w_{o}(t+\tau) vanishes and the ε\varepsilon-margin naive rule satisfies the same guarantee as the gradient rule: Theorem certifies that the gradient rule is fluid-optimal, not that it dominates the naive rule in the limit. The gradient-versus-naive separation — the empirical collapse of Lemma — is a finite-, contested-regime phenomenon, where fitted chain rents make the time profile of \widehat{W} informative and the naive rule double-charges it. Likewise, in the limit \widehat{W} carries no information beyond the terminal values; its value at finite on the benchmark’s tight and scarce scenarios — which violate (A1) — is an empirical matter (§).
###  5.2 Price consistency and the theorem
######  (Dual stability).
Work with the hourly fluid LP (cells (\ell,h); the fitting granularity of \hat{\lambda} and \widehat{W}). Under (A1)–(A4), on a sample path at scale : (a) the realized arc-flow LP’s node potentials \hat{w}_{m}(h) equal the fluid potentials w_{m}(h) with probability , and its cell thresholds converge at O_{p}(K^{-1/2}); (b) the bucket-averaged load duals satisfy →:=\hat{\lambda}(\ell,h)\to\lambda_{\ell}(h):=\mathbb{E}[(r-\theta_{\ell h})_{+}] at O_{p}(K^{-1/2}), where =−\theta_{\ell h}=w_{o_{\ell}}(h)-w_{\delta_{\ell}}(h+\tau_{\ell}) is the fluid acceptance threshold; (c) the chain-packing duals of Lemma coincide with the arc-flow duals on an event of probability .
###### Proof.
_(Step 1: cell LP and basis stability.)_ Aggregate the realized instance into cells: N_{\ell h} loads arrive in cell (\ell,h) with i.i.d. rewards; the arc-flow relaxation over the market–hour network is a finite LP with data b_{K}=N/K (arrival masses) and (mean rewards). By Poisson concentration and the CLT over finitely many cells, =b_{K}=b+O_{p}(K^{-1/2}) and =c_{K}=c+O_{p}(K^{-1/2}). Let be the fluid LP’s optimal basis, unique and nondegenerate by (A3). Its optimality conditions — reduced-cost signs and basic-variable positivity (e.g., Bertsimas and Tsitsiklis, 1997) — hold with strict inequality, hence remain valid on an open neighborhood of . On the event ={​ in that neighborhood}E_{K}=\\{(b_{K},c_{K})\text{ in that neighborhood}\\} — probability — the realized LP has the same optimal basis. Dual solutions are functions of the basis and objective only, y=c_{B}(B^{\star})^{-1}. For the node potentials the equality is exact, not merely approximate: under (A1) the waiting arcs are basic everywhere, so each \hat{w}_{m}(h) telescopes along waiting arcs to the terminal coefficient \omega V(m) — a deterministic model constant that the random reward components of never touch. Hence ==\hat{w}_{m}(h)=w_{m}(h)=\omega V(m) exactly on , and the cell thresholds \hat{\theta}_{\ell h}, which inherit randomness only through the potentials, converge at the stated O_{p}(K^{-1/2}) rate from the objective perturbation of the dispatch arcs. This proves (a).
_(Step 2: disaggregation.)_ In the load-level LP each load in cell (\ell,h) has its own capacity constraint with dual \lambda_{i}. With the basis fixed, complementary slackness gives \lambda_{i}=(r_{i}-\hat{\theta}_{\ell h})_{+}: under (A1) idle capacity is ample within every cell, so the load-level optimum serves exactly the above-threshold loads — those earn their surplus as rent; loads below have slack capacity and zero dual. Averaging over the N_{\ell h}=\Theta(K) i.i.d. rewards, →\hat{\lambda}(\ell,h)\to\mathbb{E}[(r-\theta_{\ell h})_{+}] by the law of large numbers at rate O_{p}(K^{-1/2}), using \hat{\theta}\to\theta from (a). This proves (b).
_(Step 3: chains vs flows.)_ conv​⊆{unit flows from ​k​’s start}\mathrm{conv}(\text{chains}_{k})\subseteq\\{\text{unit flows from }k\text{'s start}\\}, with equality of optimal values whenever every unit flow decomposes into clock-feasible chains. Under (A2) this holds by construction: occupation times are rest-inclusive, so a resource finishing any dispatch is idle with renewed clocks and, being in the destination market, is feasible for any subsequent in-market dispatch. Every path in the market–time network is therefore itself a feasible chain, the two polyhedra have equal optimal values, and both LPs share optimal duals. This proves (c). ∎
######  (Heterogeneous rewards).
Under within-cell homogeneity, the mean rent equals the classical bid-price form: =\mathbb{E}[(r-\theta)_{+}]=(\bar{r}-\theta)_{+}, and subtracting it in rule () reproduces the fluid rule exactly. With genuinely heterogeneous within-cell rewards, the fluid optimum is a _threshold_ rule (accept iff r\geq\theta), while the mean rent is strictly positive on contested cells; a policy that subtracts the mean rent over-shades and accepts conservatively. The consistent extension is to fit quantile (marginal) prices rather than mean rents. We leave that refinement to future work and note two things: the certificate of Theorem is unaffected (it holds for any policy), and any bias the conservative shading induces at finite is visible in — and bounded by — the reported certified gaps; we do not attempt to decompose it here.
######  (Portability, explained).
Step 1 says the price surface is a _basis object_ : two independent sample paths that land in the stability neighborhood — probability — share the same optimal basis, hence identical potentials and thresholds within O_{p}(K^{-1/2}), hence identical policy decisions by the threshold separation of (A3′). The empirical portability finding (identical policy decisions under frozen vs fresh duals) is LP basis stability, not an accident of the benchmark; and portability should fail near fluid basis switches (arrival-regime changes), a testable prediction.
######  (Subcritical fluid consistency).
Assume (A1)–(A4), the threshold separation (A3′, Assumption ), and . Let the dual-price policy \pi_{\lambda} be built from \hat{\lambda},\widehat{W} fitted _at lane–hour granularity_ from an optimal dual solution of an independent sample path’s relaxation (Remark discusses the deployed market–hour table), and run with the soft acceptance margin of Appendix (accept iff \mathrm{score}\geq-\varepsilon_{K}, for any sequence \varepsilon_{K} between the fit-plus-dispersion error and the threshold-separation constant; the margin is an existence device, cf. the remark there). Writing V^{\pi_{\lambda}}(K) for the expected total value over the product of the independent training and evaluation paths at scale , and :=V^{\star}(K):=\mathbb{E}[V^{\star}(\xi)] over the evaluation path,  
| ≥≤,hence→1.V^{\pi_{\lambda}}(K)\geq V_{f}\cdot K-o(K)\quad\text{and}\quad V^{\star}(K)\leq V_{f}\cdot K+o(K),\qquad\text{hence}\quad\frac{V^{\pi_{\lambda}}(K)}{V^{\star}(K)}\to 1.  |  
| --- |  
###### Proof.
We prove the two inequalities separately. Steps 1–3 are proved here; Step 4, the fleet-coupling estimate, is proved in full in Appendix , where the soft margin is also defined.
_Step 1 (upper bound on )._ The realized instance is the fluid LP with perturbed arrival counts. Poisson concentration (Bernstein, applied per lane–hour cell and summed over the finitely many cells) bounds the perturbation’s effect on the LP value by on an event of probability 1-O(K^{-2}); off that event the LP value is at most +r_{\max}N+\omega V_{\max}K, whose expectation against the O(K^{-2}) tail is by Cauchy–Schwarz. Hence ≤V^{\star}(K)\leq V_{f}K+o(K).
_Step 2 (the fitted surfaces are nearly fluid)._ By Lemma and the \widehat{W}-consistency lemma of the appendix (Lemma ), the fitted tables are within \delta_{K}=o(1) of the fluid duals on every cell, on an event of probability ; under (A4) all basis switches sit on hour boundaries, so no cell mixes two dual regimes.
_Step 3 (the policy plays the fluid rule)._ By Lemma , the fluid-optimal rule is the same-time gradient rule. Combining with Step 2 and the soft margin, the policy’s decisions agree with the fluid-optimal decisions on every cell (Lemma ); the only exceptions are the -probability bad events.
_Step 4 (accepted arrivals find trucks: Appendix )._ Because the policy is state-blind, the dispatch process is a thinning of the arrival stream as long as idle pools are nonempty; a time-uniform concentration bound plus a first-violation argument shows idle mass stays above z_{\min}K/2 throughout with probability , so no accepted arrival is refused for lack of a resource, and realized service rates track fluid rates to O(\sqrt{K\log K}) (Lemmas –).
_Step 5 (sum up)._ The value ledger (Proposition ) totals the losses — counting fluctuations, within-cell dispersion, and the bad events — at , so ≥V^{\pi_{\lambda}}(K)\geq V_{f}K-o(K). Combine with Step 1 to get the ratio. ∎
(a) The critical regime z_{\min}=0 (tight/scarce economics) is genuinely open: (D2) equality fails, the gradient rule loses its CS status, and § shows per-resource duality slack can persist; we conjecture V^{\pi_{\lambda}}/V^{\star}\to 1-\varepsilon(\rho) with ε\varepsilon growing in contention. (b) Assumption (A2) is where HOS clocks live; relaxing it requires a regeneration argument at rest events and is this section’s remaining technical debt. (c) The theorem is stated for exactly optimal training duals. In practice the fitted duals are subgradient iterates; empirically the policy’s decisions are already invariant at that budget (§), consistent with the basis-stability mechanism of Lemma — the tables only need to land in the same basin, not converge in norm. Extending the theorem to value-suboptimal duals requires a sharpness modulus for the bucket-averaged functionals that is uniform in ; we do not pursue it here.
##  Limits of the Relaxation
######  (Per-resource slack bounded away from zero).
There exist instance families of the model with []/K≥γ0[\min_{\lambda}L(\lambda)-V^{\star}]/K\geq\gamma>0 for all fleet sizes .
######  (Dantzig–Wolfe equivalence).
For a realized instance, let \mathcal{C}_{k} denote resource ’s finitely many feasible service chains and \mathrm{val}(S) a chain’s total reward. Then \min_{\lambda\geq 0}L(\lambda) equals the chain-packing LP  
| max⁡{:,}.\max\Bigl\\{\textstyle\sum_{k}\sum_{S\in\mathcal{C}_{k}}\mathrm{val}(S)\,x_{k,S}\;:\;\sum_{k}\sum_{S\ni t}x_{k,S}\leq 1\;\forall t,\;\;\sum_{S}x_{k,S}\leq 1\;\forall k,\;\;x\geq 0\Bigr\\}.  |  
| --- |  
###### Proof.
=+⁡[]L(\lambda)=\sum_{t}\lambda_{t}+\sum_{k}\max_{S\in\mathcal{C}_{k}\cup\\{\emptyset\\}}[\mathrm{val}(S)-\lambda(S)] is the Lagrangian dual function of the integer chain-packing program; each inner maximum is over a finite set, so \min_{\lambda}L equals the LP relaxation over the convex hulls of the per-resource feasible sets (Dantzig–Wolfe; Geoffrion, 1974), which is the displayed column LP. ∎
######  (Replication).
Let have resources and duality gap =−0g_{0}=\min_{\lambda}L(\lambda;I_{0})-V^{\star}(I_{0})>0. Then disjoint copies satisfy =V^{\star}(I_{0}^{n})=nV^{\star}(I_{0}) and ​=n​​\min_{\lambda}L(\cdot\,;I_{0}^{n})=n\min_{\lambda}L(\cdot\,;I_{0}), hence per-resource slack g_{0}/k_{0}>0 for every K=nk_{0}.
###### Proof.
No chain, constraint, or transition couples distinct copies, so both the joint DP and the chain-packing LP separate and optima add; apply Lemma per copy. ∎
#### Kernel (exhibited; exact rational certificates).
Three resources with staggered availabilities and service budget 2 (a clock proxy), six loads in cyclic geography over three markets (scripts/find_gap_kernel.py; reports/gap_kernel_search.md). The certificate is checkable by hand in four steps.
_Check 1 (integer optimum)._ Exhaustive enumeration of the joint decision tree gives V^{\star}(K_{1})=41.
_Check 2 (a fractional packing)._ Put weight \tfrac{1}{2} on five chains:  
| :::::.T_{0}{:}\\{L_{4}\\},\quad T_{1}{:}\\{L_{2},L_{3}\\},\quad T_{1}{:}\\{L_{5}\\},\quad T_{2}{:}\\{L_{3},L_{5}\\},\quad T_{2}{:}\\{L_{1},L_{4}\\}.  |  
| --- |  
Feasibility: each shared load appears in exactly two chains (, , ), so its total weight is \tfrac{1}{2}+\tfrac{1}{2}=1; every other load appears once at weight \tfrac{1}{2}; trucks and each run two chains at total weight , and runs one at \tfrac{1}{2}.
_Check 3 (its value)._ The chain values are 7,20,14,28,207,20,14,28,20, so the packing is worth  
| ​(7+20+14+28+20)== 44.5.\tfrac{1}{2}\,(7+20+14+28+20)\;=\;\tfrac{89}{2}\;=\;44.5.  |  
| --- |  
_Check 4 (conclude)._ By Lemma , \min_{\lambda}L equals the chain-packing LP value, which is at least the value of any feasible packing. So  
| ≥ 41=,\min_{\lambda}L\;\geq\;\tfrac{89}{2}\;>\;41\;=\;V^{\star}(K_{1}),  |  
| --- |  
a certified duality gap of at least \tfrac{7}{2} ( of ). The binding structure — one resource holding two valuable chains it can execute only once, with odd-cyclic overlap through shared loads — is exactly the structure whose absence provably collapses the gap (identical loads, one-shot assignment, and exchangeable resources each yield zero gap). Service budgets are the special case of duty clocks whose renewal is slower than the residual horizon, so lies inside the model of §; and is not subcritical — its contested loads bind capacity — so there is no tension with Theorem . Theorem follows from with \gamma=7/6.
######  (Rarity and density).
Random micro-instances carry certified gaps in only {\sim}0.03\% of draws — at small scale the relaxation is almost always tight — while the benchmark’s proportional-scaling experiments show per-resource bound slack growing with density ( per resource from to , robust to two step-size schedules). The density-growth characterization is open. There is no contradiction with the total-gap results of Brown and Zhang (2023): those hold for a fixed number of state-independent linking constraints, whereas here constraints number with state-dependent participation — the natural scaling for online freight.
##  Experiments
All experiments use the public benchmark’s frozen scenario-v0.3.2 contract (Chandrasekaran, 2026), thirty held-out paired seeds (pairs 1–30; the dual tables are fitted on pair 0’s stream and never refitted), paired-bootstrap 95% confidence intervals (20,000 resamples), and two-sided sign tests. Policies: bid_price (future-value proxy), surrogate_linear (ridge regression on 200 rollout labels per pair, retrained per pair), dual_price (flat-price ablation), dual_price_vf (this paper), and rollout_teacher. Latencies in Table were measured single-threaded on a 4-core cloud VM; absolute values are Python reference latencies, and the cross-policy ratios are the meaningful quantity.
Table 1: Retention vs the rollout teacher over thirty held-out seed pairs. The last rows give the paired difference of the zero-label dual policy against the rollout-trained surrogate, and mean per-decision latency.  
 |  
|  |  
 |  
| surrogate (200 labels/pair)  |  
|  dual_price (flat ablation)  |  
| dual_price_vf (zero labels)  |  
|  paired vf  surrogate  |  [+0.5,+3.6]  |  [-3.2,+1.3]  |  [+2.4,+4.5]  |  
|  Wilcoxon signed-rank   |  
| latency vf / surrogate / rollout (ms)  | 0.059 / 0.081 / 74.5  | 0.040 / 0.075 / 48.0  | 0.035 / 0.106 / 111.7  |  
The training-free policy beats the rollout-trained surrogate on tight (bootstrap CI excludes zero; Wilcoxon p=0.023; the pure sign test is weaker at 19/30 wins, p=0.20 — the edge is in magnitudes) and on mild (all three tests agree; 26/30 wins), and ties on scarce. On mild the policy also exceeds the rollout teacher itself (101.8%): rollout is a finite-lookahead stochastic heuristic whose Monte Carlo noise mis-rejects at low contention, while the global price signal does not — the same mechanism as the scaling cell below. The flat ablation sits at bid-price level: the same-time gradient is the source of the edge, as Lemmas and predict.
Table 2: Per-instance certificates: realized policy value over the instance’s Lagrangian bound, across ten independently bound-solved instances per scenario (45-iteration solves).  
| dual_price_vf  | rollout_teacher  |  
| --- | --- |  
|  60.0% of optimal (56.9–63.6)  | 62.8% (60.5–66.8)  |  
|  57.5% (53.4–61.5)  | 63.7% (61.2–65.3)  |  
Certificates are narrow, stable distributions; the 1000\times-slower rollout teacher certifies only 2.8–6.2 pp higher, locating most of the certified gap in the relaxation — consistent with § and with the scaling diagnosis below. Figure shows the latency–quality frontier (left) and the scaling decomposition (right).
\approx\\!1000\timesmean decision latency (ms, log scale)% of rollout profit
certified % of boundfleet size (arrivals scaled with )
Figure 1: Left: the latency–quality frontier over thirty held-out seed pairs (solid marks: tight; open marks: scarce). The log axis shows what the tables cannot: dual_price_vf gives up 4.6–10 points of rollout profit to move three orders of magnitude left, and it sits above the rollout-trained surrogate at lower latency. Right: proportional fleet scaling. The policy tracks rollout (dashed, top) while the certified fraction of the bound falls at — the bound loosens with density (Theorem ); the policy does not degrade. Table 3: Proportional fleet scaling (tight economics; plateau-converged solves, trajectory-verified). Unlike Table , policy values here are in-sample: tables are fitted from each cell’s own solved stream, since certificates require the bound and the policy on the same instance.  
| policy vs rollout  | certified gap  |  
| --- | --- |  
 |  
 |  
 |  
The policy is scale-robust against rollout while per-resource bound slack inflates at density (Figure , right); the level is robust to a step-size schedule reset (13 aggressive iterations never improve on the fine-schedule bound). Rollout’s latency grows 32\to 159 ms over this range; the dual policy stays {\sim}0.1 ms.
#### Portability and solver budgets.
On the tested pairs, decisions are identical under frozen (pair-0) vs freshly solved (pair-1) duals on both stress scenarios — the finite-sample shadow of Lemma ’s basis stability. Policy quality saturates by subgradient iterations; bound-grade certificates require ; per-resource solves parallelize at 3.05\times on four cores.
##  Discussion
#### A certificate culture for freight decisioning.
Every policy run on the benchmark can now report “provably of optimal on this instance.” The certificates are conservative exactly where § says they must be. The decomposition into policy loss and relaxation slack tells methods researchers where improvement is possible (tighter bounds) and where it is not (the policy is already near rollout).
#### Relation to the benchmark’s cascade.
The v0.3 surrogate-to-rollout cascade (Chandrasekaran, 2026) attains about of rollout on these scenarios. But it needs 200 rollout labels per stream to train, it invokes rollout on escalated decisions (12–17 ms mean latency), and it carries no optimality statement. The dual policy trades 3–8 retention points for three orders of magnitude in latency, zero training data, and a certificate. The two are complementary. Replacing the cascade’s cheap stage with the dual policy is a natural follow-up, and we leave it to future work.
#### Prices are infrastructure.
Basis stability makes the fitted price surface a reusable asset. One offline solve prices weeks of operations. There is a testable warning condition — arrival-regime switches — for when to refit. Per-request-learning schemes do not offer this property.
#### Where the money is.
The remaining certified gap at scale is dominated by cross-resource coupling slack. Neither faster policies nor more solver iterations recover it. Tightening the bound itself — chain-aware duals, restricted exchange constraints — is the natural next methodological target, and Theorem ’s kernel families are the test cases.
##  Limitations
Certificates inherit the bound’s looseness. In the critical regime they understate policy quality (Table ’s rollout row), and Theorem does not apply there; the critical case is open. Assumption (A2) folds HOS-clock renewal into the regime definition, and relaxing it needs a regeneration argument. The surrogate baseline is the benchmark’s dependency-free linear model, not a tuned learned policy; rollout is the strong baseline. Benchmark caveats — public calibration, lane concentration — carry over from the v0.3 release (Chandrasekaran, 2026). We defer the online-updating variant and a learned deferral rule to future work, by scope discipline.
##  10 Conclusion
One Lagrangian object gives two things: a microsecond, training-free, portable acceptance policy, and a per-instance certificate of its optimality gap. The theory says when the certificate is tight (subcritical fluid consistency, Theorem ) and shows, with exact certificates, why it cannot always be (kernels with irreducible per-resource slack, Theorem ). On a public benchmark the policy beats a rollout-trained surrogate on two of three scenarios and ties the third, at three orders of magnitude lower latency than the teacher. We release the benchmark, the solver, the kernel search, and every table for independent verification.
## Declaration of Competing Interest
The author is employed by Bubba AI, which develops AI-based load-planning and carrier-operations products in the freight domain addressed by this paper. This study uses only public data and the open-source benchmark; no proprietary data or systems were used, and Bubba AI had no role in the study design, the analysis, or the decision to publish.
## Acknowledgements
The author used Anthropic’s Claude, a large language model, to assist with software implementation and experiment scaffolding, drafting and editing of the manuscript, and literature and formatting support. All model designs, proofs, experiments, results, and claims were verified and approved by the author, who takes full responsibility for the content.
##  Appendix A Completing the Proof of Theorem : the Fleet-Coupling Estimate
This appendix proves Step 4 of Theorem in full. The argument is simpler than the mean-field coupling one might expect, for one structural reason: _the dual-price policy is state-blind_. Its accept/reject decision depends only on the load’s features and the clock — never on the fleet state — except through the feasibility guard, which requires one idle resource at the origin. So, as long as no market’s idle pool is exhausted, the dispatch process is a deterministic thinning of the arrival stream, and there is no feedback loop between fleet state and decisions to control. The proof therefore has three parts: a uniform concentration bound on arrivals (Lemma ), an agreement lemma matching policy decisions to fluid decisions (Lemma ), and a first-violation argument showing the idle pools never empty (Lemma ). A value ledger (Proposition ) then completes Steps 4–5 of the theorem.
###  A.1 The soft acceptance margin
One honest repair to the policy is needed first. Under within-cell homogeneity (A4), a fluid-_served_ cell has fitted rent \hat{\lambda}\approx\bar{r}-\theta and gradient g\approx-\theta, so the score of a served load is  
| score=≈=.\mathrm{score}\;=\;r-\hat{\lambda}+g\;\approx\;\bar{r}-(\bar{r}-\theta)-\theta\;=\;0.  |  
| --- |  
Served cells sit _exactly at the acceptance boundary_ : the rent cancels the surplus. This knife edge is not an artifact of our policy; it is the classical degeneracy of bid-price controls at the fluid optimum (Talluri and van Ryzin, 1998), usually repaired by booking limits or perturbed prices. We repair it with a soft margin.
First extend the nondegeneracy assumption with an explicit threshold-separation constant.
######  (A3: threshold separation).
No cell sits at its threshold:  
| η:=min⁡{:cell ​​ fluid-rejected},\eta\;:=\;\min\bigl\\{\,\theta_{\ell h}-\bar{r}_{\ell}(h)\;:\;\text{cell }(\ell,h)\text{ fluid-rejected}\,\bigr\\}\;>\;0,  |  
| --- |  
and every fluid-served cell is served in full (u_{\ell}=\Lambda_{\ell} on the cell).
(A3) strengthens (A3) from dual uniqueness to primal nondegeneracy: a cell with \bar{r}=\theta on an open time set admits a continuum of alternative primal optima, which (A3) excludes. Note that under (A4) the dual data are constant on each hourly cell and all basis switches sit on hour boundaries, so () is a minimum over finitely many well-defined cells.
Two conventions used throughout the appendix. _Durations:_ \tau_{\ell} is the deterministic, rest-inclusive occupation time of §: a resource dispatched at is occupied on [t,t+\tau_{\ell}) and is idle in market \delta_{\ell} from t+\tau_{\ell} with renewed clocks, hence feasible for any subsequent in-market arrival by (A2). A resource still resting at is physically at its destination, so the model’s terminal value credits V(\delta_{\ell}) — consistent with the pipeline term in (). Bounded i.i.d. duration noise (yard delays) would shift completions by a bounded amount and add one further martingale term to Lemma without structural change. _Tables:_ \hat{\lambda} and \widehat{W} are hourly; an evaluation at an off-grid time t+\tau_{\ell} reads the value of the containing hour, as in the implementation.
Let \delta_{K} denote the within-cell reward dispersion of (A4) plus the table-fit error of Lemma , so \delta_{K}\to 0. Fix any sequence \varepsilon_{K} with  
| 3\,\delta_{K}\;\leq\;\varepsilon_{K}\;\leq\;\eta/2,  |  
| --- |  
which exists for large since \delta_{K}\to 0. The _ε \varepsilon-margin policy_ \pi_{\lambda}^{\varepsilon} accepts iff \mathrm{score}\geq-\varepsilon_{K} (and a feasible resource exists); Theorem is stated for \pi_{\lambda}^{\varepsilon}.
The benchmark policy uses \varepsilon=0. In practice rewards within a cell are genuinely heterogeneous, contested cells have interior thresholds, and the zero-margin rule is not degenerate (Remark ); the margin is a proof device for the homogenized limit, not a change to the deployed policy. Its width depends on the unobservable constants and \delta_{K} — it is an existence statement, not a tuning recipe.
######  (Table granularity).
The theorem analyzes tables fitted at _lane–hour_ granularity (origin, destination, hour), matching the cell structure of the fluid LP: the rent =+\lambda_{\ell}(h)=(\bar{r}_{\ell}(h)-\theta_{\ell h})_{+} depends on the destination through \theta_{\ell h}, so two lanes sharing an origin generically carry different rents. The deployed table of § pools by _market–hour_ (with shrinkage), i.e., it stores a demand-weighted mixture of lane rents. The two coincide exactly when rents are homogeneous within each origin–hour; otherwise the pooled table over-shades served lanes whose rent is below their origin’s average, by exactly the within-origin rent dispersion. The pooled variant trades this bias for lower estimation variance — the empirical choice of § — but the guarantee of Theorem is proved for the lane-granular variant.
###  A.2 Uniform concentration of arrivals
Work on the hourly cell grid (\ell,h); there are finitely many cells and lanes. Let N_{\ell}(t) count lane- arrivals in , a Poisson process with compensator =K\mu_{\ell}(t)=K\int_{0}^{t}\Lambda_{\ell}(s)\,ds; write :=​\Lambda_{\max}:=\max_{\ell}\sup_{t}\Lambda_{\ell}(t).
######  (Uniform arrival concentration).
Let be the event  
| |−|≤:=6​​T​K​.\sup_{\ell\in L}\;\sup_{t\leq T}\;\bigl|N_{\ell}(t)-K\mu_{\ell}(t)\bigr|\;\leq\;a_{K}\;:=\;\sqrt{6\,\Lambda_{\max}T\,K\log K}.  |  
| --- |  
Then ≥\Pr(E_{1})\geq 1-O(K^{-2}).
###### Proof.
_Step 1._ −N_{\ell}(t)-K\mu_{\ell}(t) is a martingale with jumps of size and predictable quadratic variation ≤K\mu_{\ell}(t)\leq K\Lambda_{\max}T.
_Step 2._ Freedman’s inequality (Freedman, 1975), in its continuous-time form for counting-process martingales (van de Geer, 1995), gives, for each lane, Pr⁡(|−|≥a)≤2​exp⁡(−/(+))\Pr\bigl(\sup_{t\leq T}|N_{\ell}(t)-K\mu_{\ell}(t)|\geq a\bigr)\leq 2\exp\bigl(-a^{2}/(2K\Lambda_{\max}T+2a/3)\bigr).
_Step 3._ Take : the exponent is at most -2\log K for large. A union bound over the lanes gives ≤=\Pr(E_{1}^{c})\leq 2|L|K^{-2}=O(K^{-2}). ∎
###  A.3 The value-to-go table is consistent
######  (\widehat{W}-consistency).
Under (A1)–(A4), on the basis-stability event of Lemma (training path, optimal duals), the backward recursion () satisfies, for a constant depending only on ,  
| ⁡|−|≤≤​C​​for every cell.\max_{m,t}\;\bigl|\widehat{W}(m,t)-\omega V(m)\bigr|\;\leq\;C\,\delta_{K},\qquad\text{hence}\qquad\bigl|g(\ell,h)+\theta_{\ell h}\bigr|\;\leq\;2C\,\delta_{K}\;\;\text{for every cell.}  |  
| --- |  
###### Proof.
Let =⁡|−|\epsilon(t)=\max_{m}|\widehat{W}(m,t)-\omega V(m)| on the hourly grid.
_Step 1 (base)._ =\widehat{W}(m,T)=\omega V(m) by definition, so \epsilon(T)=0.
_Step 2 (wait branch)._ The wait branch of () contributes ≥−\widehat{W}(m,t{+}1)\geq\omega V(m)-\epsilon(t{+}1), so ≥−\widehat{W}(m,t)\geq\omega V(m)-\epsilon(t{+}1): the recursion never falls more than the next step’s error below the pin.
_Step 3 (dispatch branches)._ A dispatch branch contributes +r_{i}-\lambda_{i}+\widehat{W}(\delta,t+\tau_{\ell}). On the stability event the training dual of load is \lambda_{i}=(r_{i}-\hat{\theta}_{\ell h})_{+} (Lemma , Step 2), so =min⁡≤r_{i}-\lambda_{i}=\min(r_{i},\hat{\theta}_{\ell h})\leq\theta_{\ell h}+\delta_{K}. Since =ω​[]\theta_{\ell h}=\omega[V(o_{\ell})-V(\delta_{\ell})] under the pinning of Lemma , the branch is at most ++\omega V(o_{\ell})+\delta_{K}+\epsilon(t+\tau_{\ell}).
_Step 4 (recursion)._ Combining, ≤+\epsilon(t)\leq\max_{s>t}\epsilon(s)+2\delta_{K}, and backward induction over the at most grid steps gives \epsilon(t)\leq 2T\delta_{K}. The gradient statement follows from =−g(\ell,h)=\widehat{W}(\delta,\cdot)-\widehat{W}(o,\cdot) and the triangle inequality. ∎
###  A.4 Decisions agree with the fluid
Let be the basis-stability event of Lemma for the (independent) training path: on the frozen tables satisfy |−|≤|\hat{\lambda}(\ell,h)-\lambda_{\ell}(h)|\leq\delta_{K} (Lemma (b)) and ≤|g(\ell,h)+\theta_{\ell h}|\leq\delta_{K} (Lemma , absorbing its constant into \delta_{K}) for every cell, and =\Pr(E_{2})=1-o(1). Note is a single event about the frozen tables — it is not re-drawn per decision. By the hour-grid clause of (A4), every cell lies strictly inside one dual regime: there are no cells straddling a basis switch.
######  (Decision agreement).
On , for every arrival whose origin market has an idle feasible resource: \pi_{\lambda}^{\varepsilon} accepts iff the cell is fluid-served.
###### Proof.
_Step 1 (served cells)._ Let the cell be fluid-served, so \bar{r}\geq\theta and \lambda_{\ell}(h)=\bar{r}-\theta. The load’s reward is r=\bar{r}\pm\delta_{K} by (A4). Then  
| score=≥−−=≥,\mathrm{score}=r-\hat{\lambda}+g\;\geq\;(\bar{r}-\delta_{K})-(\bar{r}-\theta+\delta_{K})-(\theta+\delta_{K})\;=\;-3\,\delta_{K}\;\geq\;-\varepsilon_{K},  |  
| --- |  
using (). The policy accepts.
_Step 2 (rejected cells)._ Let the cell be fluid-rejected, so \bar{r}\leq\theta-\eta by () and \lambda_{\ell}(h)=0, hence \hat{\lambda}\geq 0 always. Then  
| score≤−0−≤,\mathrm{score}\;\leq\;(\bar{r}+\delta_{K})-0-(\theta-\delta_{K})\;\leq\;-\eta+2\,\delta_{K}\;<\;-\varepsilon_{K},  |  
| --- |  
since \varepsilon_{K}\leq\eta/2 and 2\delta_{K}<\eta/2 for large. The policy rejects. ∎
###  A.5 Occupancy tracks the fluid; no forced rejections
Let X_{m}(t) be the number of idle resources in market at time , with deterministic proportional initial placement, so =X_{m}(0)=Kz^{0}_{m}+O(1) (rounding only). Let S_{\ell}(t) count dispatches on lane by time . Define the deviation  
| =|−|,D(t)\;=\;\sum_{m}\bigl|X_{m}(t)-Kz_{m}(t)\bigr|,  |  
| --- |  
and the first-violation time =inf{t:}\tau^{\star}=\inf\\{t:\min_{m}X_{m}(t)<z_{\min}K/2\\}.
######  (Occupancy tracking).
On E_{1}\cap E_{2}, for all large: ≤\sup_{t\leq T}D(t)\leq C\sqrt{K\log K} and \tau^{\star}>T. In particular no accepted arrival is ever rejected for lack of an idle resource, and under (A2) every accepted arrival is served.
###### Proof.
_Step 1 (flow identity)._ Idle counts change only by dispatches out and service completions in:  
| =+−.X_{m}(t)=X_{m}(0)+\sum_{\ell:\,\delta_{\ell}=m}S_{\ell}\bigl((t-\tau_{\ell})_{+}\bigr)-\sum_{\ell:\,o_{\ell}=m}S_{\ell}(t).  |  
| --- |  
The fluid mass z_{m}(t) satisfies the same identity with K\int u_{\ell} in place of (equation ()).
_Step 2 (dispatches are thinned arrivals before \tau^{\star})._ For t<\tau^{\star} every origin market has an idle resource (z_{\min}K/2\geq 1), so by Lemma the dispatch process on lane is exactly the arrival process restricted to fluid-served cells. Given the frozen tables, the accept set is a deterministic union of cells — acceptance is all-or-nothing per cell under (A4) homogeneity — so this thinning is adapted: no decision peeks at the future. Since the fluid serves the full arrival mass of served cells (Assumption ),  
| ||≤,\sup_{s\leq t}\,\bigl|S_{\ell}(s)-K\textstyle\int_{0}^{s}u_{\ell}\bigr|\;\leq\;C_{1}\,a_{K},  |  
| --- |  
by Lemma , where the constant absorbs the conversion from initial-segment deviations to the finitely many served-cell intervals.
_Step 3 (deviation bound)._ Plug Step 2 into Step 1: each −X_{m}(t)-Kz_{m}(t) is a signed sum of at most dispatch deviations plus the initial rounding error (deterministic proportional placement), so for t<\tau^{\star},  
| ≤​​​​+=.D(t)\;\leq\;2|M|\,|L|\,C_{1}\,a_{K}+O(1)\;=\;O\bigl(\sqrt{K\log K}\bigr).  |  
| --- |  
_Step 4 (no violation)._ Suppose \tau^{\star}\leq T. Just before \tau^{\star}, Step 3 and (A1) give  
| ≥≥−\min_{m}X_{m}(\tau^{\star-})\;\geq\;z_{\min}K-D(\tau^{\star-})\;\geq\;z_{\min}K-O(\sqrt{K\log K})\;>\;\tfrac{3}{4}z_{\min}K  |  
| --- |  
for large. A single arrival changes any by one, so cannot jump from above \tfrac{3}{4}z_{\min}K to below \tfrac{1}{2}z_{\min}K: contradiction. Hence \tau^{\star}>T, and Steps 2–3 hold on all of . ∎
###  A.6 The value ledger
######  (Steps 4–5 of Theorem ).
Under (A1)–(A4), (A3′), and (), ≥V^{\pi_{\lambda}^{\varepsilon}}(K)\geq V_{f}K-o(K).
###### Proof.
_Step 1 (restrict to the good event)._ Rewards may be negative, so we truncate rather than discard. Pathwise, ≤+|\text{value}|\leq r_{\max}N+\omega V_{\max}K with the total arrival count. Against E_{1}^{c}: Cauchy–Schwarz with =\mathbb{E}[\text{value}^{2}]=O(K^{2}) (Poisson second moment) and =\Pr(E_{1}^{c})=O(K^{-2}) (Lemma ) gives 𝔼≤Pr=O⋅O=o\mathbb{E}[|\text{value}|\,\mathbf{1}_{E_{1}^{c}}]\leq(\mathbb{E}\,\text{value}^{2})^{1/2}\Pr(E_{1}^{c})^{1/2}=O(K)\cdot O(K^{-1})=o(K). Against E_{2}^{c}: concerns only the training path, independent of the evaluation path, so 𝔼​=⋅==\mathbb{E}[|\text{value}|\,\mathbf{1}_{E_{2}^{c}}]=\mathbb{E}[|\text{value}|]\cdot\Pr(E_{2}^{c})=O(K)\cdot o(1)=o(K). Hence ≥𝔼​[]−V^{\pi_{\lambda}^{\varepsilon}}(K)\geq\mathbb{E}\bigl[\text{value}\cdot\mathbf{1}_{E_{1}\cap E_{2}}\bigr]-o(K).
_Step 2 (dispatch reward)._ On the good event, by Lemmas – the served set matches the fluid-served set up to counting fluctuations of O(\sqrt{K\log K}) per lane. With rewards bounded by r_{\max} and within-cell dispersion \delta_{K},  
| ≥K​−​O​−.\sum_{\text{served}}r\;\geq\;K\int_{0}^{T}\sum_{\ell}\bar{r}_{\ell}u_{\ell}\,dt\;-\;r_{\max}\,O(\sqrt{K\log K})\;-\;\delta_{K}\,O(K).  |  
| --- |  
_Step 3 (terminal value)._ The realized terminal value credits idle resources at their market and in-transit resources at their destination, matching in (). The idle part deviates from ω​V​​K​​\omega\sum_{m}V(m)Kz_{m}(T) by at most \omega V_{\max}D(T); the in-transit part per lane is −S_{\ell}(T)-S_{\ell}(T-\tau_{\ell}), which tracks the fluid pipeline mass K\int_{T-\tau_{\ell}}^{T}u_{\ell} within twice the dispatch deviation of Lemma , Step 2. Both errors are O(\sqrt{K\log K}).
_Step 4 (sum the ledger)._ Every loss line is : fluctuations O(\sqrt{K\log K}); dispersion \delta_{K}K with \delta_{K}\to 0; the bad events, by Step 1. Hence ≥V^{\pi_{\lambda}^{\varepsilon}}(K)\geq V_{f}K-o(K). ∎
######  (Why no mean-field analysis is needed).
The proof never couples interacting particles: state-blindness of the dual-price policy removes the feedback from fleet state to decisions, so the only interaction channel is the feasibility guard, which subcriticality (A1) keeps inactive with high probability. This is a structural advantage of price-based policies over state-feedback policies (e.g., rollout), for which the corresponding limit theorem does require a propagation-of-chaos argument. It also localizes exactly what breaks in the critical regime: \tau^{\star}\leq T becomes likely, forced rejections re-introduce state feedback, and the gradient rule loses its complementary-slackness status (§).
##  Appendix B Reproducibility
Python 3.10 or newer, standard library only. Headline commands: the 30-seed program (scripts/run_30seed_program.sh: Phase A policy comparisons via scripts/run_dual_price_experiment.py, Phase B certificates via scripts/run_lagrangian_bound.py with --workers 4; the corrected mild rows with fitted tables are under benchmark_runs/v04_dev/seed30_mild_fitted/), price/value fitting (scripts/fit_dual_prices.py, scripts/fit_value_togo.py), scaling cells (configs/freightbidbench_v04_dev_scaling.json), kernel search with exact certificates (scripts/find_gap_kernel.py). The public v0.4 contract (configs/freightbidbench_v04_scenarios.json, policy-set-v0.4.0) includes both dual policies in the default policy set; artifacts under benchmark_runs/v04_dev/; manifests and per-iteration dual checkpoints throughout.
## References
  * Adelman [2007] Daniel Adelman.  Dynamic bid prices in revenue management.  _Operations Research_ , 55(4):647–661, 2007.  doi: 10.1287/opre.1060.0368. 
  * Adelman and Mersereau [2008] Daniel Adelman and Adam J. Mersereau.  Relaxations of weakly coupled stochastic dynamic programs.  _Operations Research_ , 56(3):712–727, 2008.  doi: 10.1287/opre.1070.0445. 
  * Balseiro et al. [2020] Santiago R. Balseiro, Haihao Lu, and Vahab Mirrokni.  Dual mirror descent for online allocation problems.  In _Proceedings of the 37th International Conference on Machine Learning (ICML), PMLR 119_ , 2020. 
  * Bertsekas et al. [1997] Dimitri P. Bertsekas, John N. Tsitsiklis, and Cynara Wu.  Rollout algorithms for combinatorial optimization.  _Journal of Heuristics_ , 3(3):245–262, 1997.  doi: 10.1023/A:1009635226865. 
  * Bertsimas and Tsitsiklis [1997] Dimitris Bertsimas and John N. Tsitsiklis.  _Introduction to Linear Optimization_.  Athena Scientific, 1997. 
  * Brown and Zhang [2023] David B. Brown and Jingwei Zhang.  On the strength of relaxations of weakly coupled stochastic dynamic programs.  _Operations Research_ , 2023.  doi: 10.1287/opre.2022.2287. 
  * Brown et al. [2010] David B. Brown, James E. Smith, and Peng Sun.  Information relaxations and duality in stochastic dynamic programs.  _Operations Research_ , 58(4):785–801, 2010.  doi: 10.1287/opre.1090.0796. 
  * Chandrasekaran [2026] Aswin Chandrasekaran.  Latency-aware bid acceptance under operational feasibility: A public benchmark with hindsight ceilings.  arXiv preprint arXiv:2607.07343, 2026. 
  * Freedman [1975] David A. Freedman.  On tail probabilities for martingales.  _The Annals of Probability_ , 3(1):100–118, 1975. 
  * Gallego and van Ryzin [1997] Guillermo Gallego and Garrett van Ryzin.  A multiproduct dynamic pricing problem and its applications to network yield management.  _Operations Research_ , 45(1):24–41, 1997. 
  * Geoffrion [1974] Arthur M. Geoffrion.  Lagrangean relaxation for integer programming.  _Mathematical Programming Study_ , 2:82–114, 1974. 
  * Goodson et al. [2017] Justin C. Goodson, Barrett W. Thomas, and Jeffrey W. Ohlmann.  A rollout algorithm framework for heuristic solutions to finite-horizon stochastic dynamic programs.  _European Journal of Operational Research_ , 258(1):216–229, 2017.  doi: 10.1016/j.ejor.2016.09.040. 
  * Hawkins [2003] Jeffrey Thomas Hawkins.  _A Lagrangian Decomposition Approach to Weakly Coupled Dynamic Optimization Problems and its Applications_.  PhD thesis, Massachusetts Institute of Technology, 2003. 
  * Powell et al. [2012] Warren B. Powell, Hugo P. Simão, and Belgacem Bouzaiene-Ayari.  Approximate dynamic programming in transportation and logistics: A unified framework.  _EURO Journal on Transportation and Logistics_ , 1(3):237–284, 2012.  doi: 10.1007/s13676-012-0015-8. 
  * Sezener and Dayan [2020] Eren Sezener and Peter Dayan.  Static and dynamic values of computation in mcts.  In _Proceedings of the 36th Conference on Uncertainty in Artificial Intelligence (UAI), PMLR 124_ , 2020. 
  * Simão et al. [2009] Hugo P. Simão, Joseph Day, Abraham P. George, Ted Gifford, John Nienow, and Warren B. Powell.  An approximate dynamic programming algorithm for large-scale fleet management: A case application.  _Transportation Science_ , 43(2):178–197, 2009.  doi: 10.1287/trsc.1080.0238. 
  * Talluri and van Ryzin [1998] Kalyan T. Talluri and Garrett van Ryzin.  An analysis of bid-price controls for network revenue management.  _Management Science_ , 44(11):1577–1593, 1998. 
  * Topaloglu [2009] Huseyin Topaloglu.  Using lagrangian relaxation to compute capacity-dependent bid prices in network revenue management.  _Operations Research_ , 57(3):637–649, 2009.  doi: 10.1287/opre.1080.0641. 
  * van de Geer [1995] Sara A. van de Geer.  Exponential inequalities for martingales, with application to maximum likelihood estimation for counting processes.  _The Annals of Statistics_ , 23(5):1779–1801, 1995. 
  * Zhang and Cheung [2022] Xilin Zhang and Wang Chi Cheung.  Online resource allocation for reusable resources.  arXiv:2212.02855, 2022. 
  * Zhang and Cheung [2025] Xilin Zhang and Wang Chi Cheung.  Online allocation of reusable resources in nonstationary environments.  _Mathematics of Operations Research_ , 2025.  doi: 10.1287/moor.2023.0250. 


