pandas.Index.inferred_type# Index.inferred_type[source]# Return a string of the type inferred from the values. Examples >>> idx = pd.Index([1, 2, 3]) >>> idx Index([1, 2, 3], dtype='int64') >>> idx.inferred_type 'integer'