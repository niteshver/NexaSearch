pandas.Index.identical# final Index.identical(other)[source]# Similar to equals, but checks that object attributes and types are also equal. Returns boolIf two Index objects have equal elements and same type True, otherwise False.