pandas.DatetimeIndex.to_perioddelta# DatetimeIndex.to_perioddelta(freq)[source]# Calculate deltas between self values and self converted to Periods at a freq. Used for vectorized offsets. Parameters freqPeriod frequency Returns TimedeltaArray/Index