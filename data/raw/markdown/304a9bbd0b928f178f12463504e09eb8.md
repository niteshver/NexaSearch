arXiv is now an independent nonprofit! Learn more ×
License: arXiv.org perpetual non-exclusive license 
arXiv:2607.25914v1 [cs.AI] 28 Jul 2026
# Toward Standardized Cross-Vendor Agent Tool Trust Management in Autonomous Networks
Ravi Kant Sharma, Ashutosh Uttam, Ajay Kumar 
###### Abstract
Autonomous Network Levels 4–5 require AI agents to invoke tools across vendor boundaries without human oversight, yet existing management standards lack a standardized mechanism for cross-vendor trust visibility. When a tool from Vendor B is compromised, agents from Vendor A continue invoking it—unaware of the trust degradation—causing cascading service impact. We present AgentToolMO, a proposed 3GPP NRM information model for agent tool trust management. The model comprises: a formally defined trust state machine with provable graduated enforcement, damped cascade propagation with bounded convergence, cross-vendor trust notifications via existing Management Services (MnS) interfaces, and retroactive impact assessment through NRM dependency graph traversal. Simulation-based evaluation across multi-vendor topologies shows that standardized cross-vendor notifications reduce blast radius from hours-scale undetected propagation to near-real-time containment bounded by MnS notification delivery, with cascade convergence guaranteed in bounded iterations and sub-linear notification scaling across vendor domains. The framework operates within existing 3GPP management infrastructure, leverages existing protocols, and provides a standardization pathway for trustworthy multi-vendor autonomous network management.
##  Introduction
Autonomous 5G/6G network management increasingly relies on AI agents that invoke specialized tools across vendor boundaries . In a typical multi-vendor deployment, a radio optimization agent from one vendor may consume a configuration analytics tool managed by another . These cross-vendor tool dependencies create trust boundaries that are not explicitly addressed by current management frameworks.
The core challenge is trust visibility across vendor boundaries. A tool’s behavior may degrade—its configuration recommendations may drift beyond operator-approved baselines, it may access managed elements outside its declared scope, its actions may cause KPI regression detected through 3GPP performance management, or it may produce inconsistent outputs due to model drift . Within a single vendor domain, such degradation is detected and contained internally. In multi-vendor environments, however, this containment fails: when Vendor B restricts a degraded tool, Vendor A’s agents - which consume that tool’s outputs - have no mechanism to learn of the restriction. We term this silent trust degradation. The consequence is that unreliable outputs continue to drive autonomous decisions across domain boundaries, with blast radius expanding undetected until service impact becomes visible .
Existing approaches fail to close this gap. Sandbox-based trust confines untrusted functions but does not expose trust outcomes cross-vendor. Per-vendor frameworks treat trust as proprietary internal state. Zero-trust architectures verify identity and authorization - binary access decisions - but do not assess ongoing behavioral trustworthiness with graduated enforcement.
From a standardization perspective, the 3GPP NRM provides IOCs for managed entities, and recent extensions introduce AI/ML objects (MLTrainingFunction, MLEntity) . However, no IOC exists for agent tool trust state, no MnS notification type covers trust transitions , and no formal safety properties (absence of unsafe state jumps, bounded cascade convergence) have been addressed within the 3GPP context. Prior trust propagation work provides theoretical foundations but lacks integration with telecom management information models.
In this paper, we address the cross-vendor agent tool trust visibility problem through a standardized information model approach. Our contributions are as follows:
  1. A standardized information model (AgentToolMO) that integrates identity, lifecycle, and trust attributes within the 3GPP NRM, enabling cross-vendor queryability of agent tool trust state through existing Management Services interfaces. The model defines a new Information Object Class with attributes, relationships, and notification types that align with established NRM design patterns, ensuring backward compatibility and incremental adoptability.
  2. A formal trust state machine with graduated enforcement levels and proven safety properties. We demonstrate that the state machine guarantees no unsafe state transitions (a tool cannot jump from a restricted state to full trust without passing through monitored rehabilitation), provides bounded transitions to revocation (at most intermediate states from any degraded state to full revocation), and maintains monotonic trust reduction under sustained policy violations or KPI regression.
  3. A trust cascade propagation mechanism with a configurable damping factor that provides mathematical guarantees of convergence. When a tool’s trust degrades, dependent tools and agents must be notified; our mechanism ensures that this propagation converges in bounded iterations regardless of topology, prevents cascade storms through exponential attenuation, and respects administrative domain boundaries through configurable propagation policies.
  4. A retroactive impact assessment algorithm that traverses NRM dependency graphs to identify services and configurations that were influenced by a tool during a period when its trust was subsequently found to be degraded. The algorithm operates within a configurable lookback window and produces a prioritized list of potentially affected entities requiring review or rollback.


The remainder of this paper is organized as follows. Section surveys related work in trust management, autonomous network architectures, and relevant standardization efforts. Section formally defines the cross-vendor trust visibility problem and establishes requirements for a solution. Section presents the AgentToolMO information model, including its attributes, relationships, state machine, and integration with the 3GPP NRM. Section provides formal analysis of the model’s safety properties, cascade convergence guarantees, and computational complexity bounds. Section evaluates the approach through simulation across representative multi-vendor topologies, measuring blast radius reduction, cascade convergence time, and detection latency. Section discusses deployment considerations, standardization pathway, limitations, and future directions. Section concludes the paper.
##  II Related Work
The management of trust in autonomous network systems draws upon several distinct research communities. We organize the related literature into five categories, identifying the specific gap that motivates our contribution in each.
###  Trust Management in Multi-Agent Systems
Computational trust models have been studied extensively. Marsh formalized trust as a continuous value derived from past experiences. Jøsang introduced subjective logic for reasoning about trust under uncertainty. Ramchurn et al. surveyed trust mechanisms including individual-level learning, institutional mechanisms, and socio-cognitive models. More recent work has explored hybrid approaches , game-theoretic trust , and blockchain-based decentralized trust .
Despite their maturity, these models assume homogeneous platforms where trust information is internally accessible. None address _standardized information models_ enabling trust to be queried across vendor boundaries through well-defined management interfaces - a prerequisite for multi-vendor telecom management.
###  3GPP Management Framework and Information Models
The 3GPP NRM (TS 28.541 ) defines Information Object Classes (IOCs) for managed entities accessed through Generic Management Services (TS 28.532 ). SA5 has extended this for autonomous operations: Management Data Analytics (MDAS) , intent-driven management , and AI/ML management (TS 28.105 ) introducing MLTrainingFunction and MLEntity IOCs.
Critically, no existing IOC in the 3GPP NRM represents the trust state of an agent’s tool. While the framework provides the architectural patterns for our solution - IOC definition, attribute specification, notification mechanisms, and MnS query interfaces - the semantic content for agent tool trust has not been specified.
###  AI/ML Trust and Safety in Telecom Networks
Several industry initiatives address trust and safety for AI functions in telecommunications, though none provide a cross-vendor standardized information model.
####  ETSI ENI
The European Telecommunications Standards Institute (ETSI) Experiential Networked Intelligence (ENI) Industry Specification Group has defined a reference architecture for AI-assisted network management that includes governance concepts applicable to trust assessment. However, the focus is on a single operator’s domain rather than cross-vendor visibility.
####  ETSI ZSM
The ETSI Zero-touch network and Service Management (ZSM) framework defines a reference architecture for fully automated network management using closed-loop automation across management domains. While ZSM addresses cross-domain coordination through domain integration fabrics and data services, it does not define trust management for the AI-driven decision agents within those domains. The proposed framework complements ZSM by providing the trust governance layer that enables safe agent operation within ZSM-managed domains.
####  O-RAN Alliance
The Open RAN Alliance has specified ML model lifecycle management including model training, validation, deployment, and monitoring phases. The O-RAN architecture supports model performance tracking, but trust assessment remains a local concern within the near-RT RIC or non-RT RIC without standardized exposure to external management systems.
####  Cognitive Function Sandboxing
A sandboxing orchestration framework introduces graduated trust levels for cognitive functions, where untrusted functions execute in sandbox environments and progressively earn permission to act on the live network under specific conditions (temporal, topological, or scope restrictions). This demonstrates the value of graduated trust with behavioral evidence but operates within a single vendor’s management entity without cross-vendor visibility.
####  Exploration Management for Safe RL Agents
An exploration management framework coordinates multiple reinforcement learning agents by computing differentiated exploration policies based on operator-defined safety criteria and network part criticality. The framework provides regret guarantees but keeps safety models internal to the exploration management function - not exposed as a queryable information model.
####  Network Control Function
A Network Control Function (NCF) evaluates cognitive function recommendations before network application using adaptive acceptance criteria (inversely proportional to proposed change magnitude, directly proportional to expected improvement). Trust builds as the function demonstrates reliable predictions. However, the NCF operates as a single-vendor supervisory entity.
####  3GPP SA5 Closed Control Loop Trust
The 3GPP management services for communication service assurance (TS 28.535) includes closed control loop concepts with trust development provisions where loops negotiate precedence on shared resources. However, trust remains implicit in coordination priority rather than an explicit, queryable state machine.
###  Zero-Trust Architectures for Network Management
Zero-trust architecture (ZTA) replaces perimeter-based trust with continuous verification, requiring every access request to be authenticated and authorized regardless of network location. CISA’s maturity model defines progressive trust levels for identity, devices, and data. The NIST AI Risk Management Framework provides guidelines for trustworthy AI systems including governance, mapping, measurement, and management functions - but addresses organizational risk posture rather than runtime trust state machines with cross-vendor notification.
However, ZTA addresses a fundamentally different problem. It focuses on _identity verification_ (is this entity who it claims?) and _authorization_ (does it have permission?) - binary decisions at each request. Agent tool trust, by contrast, requires _graduated behavioral assessment_ over time. A tool may be fully authenticated and authorized yet exhibit configuration drift (recommending parameters outside approved ranges), scope violations (accessing managed elements beyond its declared domain), or recommendation inconsistency (producing conflicting outputs under identical conditions) that warrant trust reduction without complete revocation. Our problem requires a state machine with multiple trust states, transition conditions based on behavioral evidence, and enforcement policies that modulate rather than eliminate tool usage. Furthermore, the trust state must be visible across vendor boundaries - a requirement entirely absent from current zero-trust frameworks.
###  Agent Identity and Authorization Frameworks
The Model Context Protocol (MCP) provides a universal interface for agents to invoke tools but “is not a security protocol by design” and offloads authorization to external servers. A knowledge source access framework addresses discovery and authorization of knowledge sources using access tokens and consumer profiles in the 3GPP context.
The 3GPP Common API Framework (CAPIF, TS 23.222) provides API registration, discovery, and access control for service APIs. While tools could be viewed as APIs, CAPIF addresses a fundamentally different concern: it governs _whether_ an invoker may call an API (binary access decisions at invocation time), whereas our framework governs _how much to trust_ an API’s outputs based on continuous behavioral observation. CAPIF operates in the service/control plane with no mechanism for graduated trust states, cascade propagation to downstream consumers, or retroactive impact assessment. Our model operates in the management plane (NRM/MnS) and complements CAPIF: a tool may pass all CAPIF authorization checks yet warrant reduced trust due to behavioral drift detected through KPI monitoring.
These frameworks address authentication, authorization, and access control—all prerequisites for, but distinct from, _behavioral trust_. A tool may be fully authorized yet warrant reduced trust due to observed behavioral degradation. Our contribution fills this gap: a standardized information model for ongoing behavioral trust with a formal state machine and cross-vendor notification.
###  Summary of Gaps
The common limitation across all existing approaches is that trust assessment remains per-vendor: each system uses proprietary criteria, maintains internal trust state, and does not expose it through standardized interfaces. No existing work combines standardized information models, formal state machines with safety guarantees, cross-vendor queryability, and 3GPP NRM integration into a unified framework for agent tool trust.
##  III Problem Statement and Motivation
###  The Cross-Vendor Trust Visibility Problem
Consider a representative multi-vendor 5G network deployment. Vendor A’s management system deploys an autonomous Agent responsible for optimizing radio resource management across a set of NetworkSliceSubnet instances. Agent accomplishes its objectives by invoking Tool , a configuration analytics tool provided by Vendor B’s domain management system through a cross-domain MnS interface.
The following scenario illustrates the trust visibility problem:
  1. 1.
Agent (Vendor A) routinely invokes Tool (Vendor B) to obtain configuration recommendations for slice capacity adjustments.
  2. 2.
Vendor B’s internal monitoring detects degraded behavior in Tool : the tool’s cell parameter recommendations have drifted outside the approved configuration baseline (handover thresholds exceeding operator-defined bounds), and two recommendations triggered KPI regression alarms in Vendor B’s own test environment.
  3. 3.
Vendor B’s management system appropriately degrades Tool ’s internal trust state, restricting its usage within Vendor B’s domain to low-risk operations only.
  4. 4.
Problem: Vendor A’s management system has _no visibility_ into this trust state change. No notification is generated, no queryable attribute is updated, and no cross-vendor mechanism exists to communicate the degradation.
  5. 5.
Consequence: Agent continues invoking Tool at full operational scope. The degraded tool produces suboptimal or potentially unsafe configuration changes that propagate through the NRM dependency graph - from NetworkSliceSubnet to NetworkSlice to ManagedElement - without any trust-based containment.


This scenario reflects emerging operational patterns. As autonomous management functions proliferate across vendor boundaries - driven by 3GPP’s intent-driven management framework and the adoption of AI/ML for network operations - the frequency of cross-vendor tool dependencies will increase substantially. Each such dependency creates an invisible trust boundary where safety-critical information cannot propagate.
Running Example. We illustrate the framework throughout this paper using a concrete scenario: CellConfigOptimizer, a Vendor B tool that recommends antenna tilt and transmit power parameters for RAN cells. Vendor A’s SliceOrchestrator agent consumes these recommendations to optimize network slice performance. When CellConfigOptimizer receives a faulty model update, its recommendations begin drifting from approved baselines—but Vendor A has no visibility into this degradation.
###  Quantifying the Gap
We characterize the severity of the trust visibility gap along three dimensions.
####  Detection Latency
Without a standardized trust model, detection time is effectively unbounded - relying on eventual service degradation visible through KPIs, which may take hours. With standardized notification: −≤Δ​tnotificationt_{\text{detect}}-t_{\text{event}}\leq\Delta t_{\text{notification}}, bounded by MnS delivery latency (sub-second).
####  Blast Radius
During the detection gap, the blast radius grows as the tool continues operating. A tool affecting NRCellDU parameters cascades through the NRM: NRCellDU→GNBDUFunction→NetworkSliceSubnet→NetworkSlice\texttt{NRCellDU}\rightarrow\texttt{GNBDUFunction}\rightarrow\texttt{NetworkSliceSubnet}\rightarrow\texttt{NetworkSlice}. Without containment, =⋅(1+)|B(t)|=|B_{0}|\cdot(1+r\cdot(t-t_{\text{event}})) where is the invocation rate.111This linearization provides a lower bound; actual growth depends on NRM graph fan-out and is evaluated through simulation in Section . With trust-based containment: ≤⋅(1+​tnotification)≈|B(t)|\leq|B_{0}|\cdot(1+r\cdot\Delta t_{\text{notification}})\approx|B_{0}|.
####  Multi-Vendor Scaling
With vendors, bilateral trust exchange requires O(n^{2}) proprietary integrations (n(n-1)/2 pairwise agreements). A standardized model reduces this to : each vendor implements the standard IOC once. For , this reduces from 10 bilateral integrations to 5 standard implementations - a reduction that grows linearly with vendor count.
###  Requirements for a Solution
Based on the analysis of the trust visibility gap and informed by the architectural patterns established in existing work, we derive seven requirements that a solution must satisfy.
  * •
R1: Standardized Information Model. The trust state of agent tools must be represented as Information Object Classes queryable via existing 3GPP MnS interfaces (TS 28.532). The model must be expressible in the NRM information modeling methodology and integrate with existing IOC hierarchies without requiring modifications to the base NRM.
  * •
R2: Formal Trust State Machine. Trust transitions must be governed by a formally specified state machine with _safety guarantees_ : no sequence of inputs may cause a transition from a restricted trust state to an unrestricted state without positive evidence of trustworthiness exceeding defined thresholds.
  * •
R3: Cross-Vendor Notification. Trust state changes must generate notifications consumable by any authorized management system through the standardized 3GPP notification mechanism.
  * •
R4: Cascade Containment with Convergence. When trust degradation in one tool triggers trust reassessment of dependent tools, the cascade must be bounded and convergent.
  * •
R5: Retroactive Impact Assessment. Upon trust degradation, it must be possible to identify the set of management actions performed by the affected tool during a specified lookback window.
  * •
R6: Operator Configurability. Trust thresholds and enforcement policies must be operator-configurable through the standard provisioning MnS without requiring software upgrades.
  * •
R7: Protocol Independence. The information model must be independent of specific transport protocols.


###  Formal Problem Definition
We formalize the problem. A multi-vendor management network is a directed graph G_{v}=(V,E_{v}) where V=\\{v_{1},\ldots,v_{n}\\} are vendor management domains and (v_{i},v_{j})\in E_{v} indicates agents in use tools from . Let 𝒯\mathcal{T} be the set of all tools with ownership function \text{owner}:\mathcal{T}\rightarrow V, and \text{uses}:\mathcal{A}\rightarrow 2^{\mathcal{T}} mapping agents to their tools.
The trust visibility function vis:→{visible,invisible}\text{vis}:V\times\mathcal{T}\rightarrow\\{\textit{visible},\textit{invisible}\\} captures whether a domain can observe a tool’s trust state. Currently:  
| ={=otherwise\text{vis}(v_{i},t_{j})=\begin{cases}\textit{visible}&\text{if }v_{i}=\text{owner}(t_{j})\\\ \textit{invisible}&\text{otherwise}\end{cases}  |  
| --- |  
The goal is to achieve: =visible\text{vis}(v_{i},t_{j})=\textit{visible} for all where :\exists\,a\in\mathcal{A}_{v_{i}}:t_{j}\in\text{uses}(a), where \mathcal{A}_{v_{i}} denotes the set of agents belonging to vendor domain (i.e., all agents owned and operated within that vendor’s management system).
######  (Trust State).
For each tool , the trust state is a tuple =\sigma(t_{j})=(s,\mathbf{e},\tau,\rho) where is the current state in the trust state machine (defined in Section ), 𝐞\mathbf{e} is the evidence vector, is the timestamp of the most recent transition, and \rho:S\rightarrow\mathcal{P} maps each state to its enforcement policy.
Problem Statement. Design an information model \mathcal{I}_{M} such that: (1) \mathcal{I}_{M} is expressible as a 3GPP-compliant IOC enabling cross-vendor visibility; (2) the trust state machine satisfies safety - no transition from a degraded state to a higher-trust state without sufficient evidence; (3) trust state changes generate cross-vendor notifications with latency \leq\Delta t_{\text{max}}; (4) any cascade converges within k_{\text{max}} steps.
The following sections present our solution: Section defines the information model including the trust state machine and cascade containment algorithm, and Section provides quantitative evaluation.
##  IV Proposed Framework
This section presents the complete technical framework for cross-vendor agent tool trust management in autonomous networks. The framework is grounded in the 3GPP Network Resource Model (NRM) architecture defined in TS 28.541 and extends it with managed objects, state machines, and algorithms specifically designed for governing agent tool interactions in multi-vendor network management environments.
Illustrative IOCs
Figure 1: Illustrative IOC placement within the 3GPP NRM hierarchy. Blue-bordered classes are new; solid triangular arrows denote containment (parent–child); dashed arrows denote reference associations.
###  Core Managed Object for Agent Tool Trust
The specific attribute names, state labels, threshold values, and algorithmic details presented in this section represent one illustrative instantiation of the framework concepts. To delineate scope: the _normative contributions_ of this work are (i) the trust state machine structure and its safety properties, (ii) the cascade propagation mechanism with damped convergence, (iii) the cross-vendor notification architecture, and (iv) the retroactive impact assessment approach. The specific attribute names (e.g., trustScore, anomalyCount), penalty formulas, threshold values, and enumeration types are _illustrative_ and would be subject to refinement during any standardization process. The specific attribute naming (e.g., toolId, trustScore) follows 3GPP NRM conventions (TS 28.622 naming patterns using lowerCamelCase) but alternative realizations with different granularity or parameterization are equally consistent with the underlying architectural principles.
We propose AgentToolMO as an illustrative first-class managed object within the 3GPP NRM, representing any tool that an agent may invoke to observe or modify network state (Figure shows its placement in the NRM hierarchy). This object captures the complete lifecycle of a tool from onboarding through potential deregistration, with continuous trust assessment (Figure ). Throughout this paper, we use the term _tool_ as shorthand for an AgentToolMO instance; the two terms are used interchangeably unless a distinction between the managed object class and a specific instance is contextually relevant.
MnS Notification Interface (TS 28.532)5 Agents, 12 Tools5 Agents, 10 ToolsnotifyTrustDegradation
Figure 2: Deployment architecture (simplified; evaluation uses 3 vendors). Each vendor domain maintains local AgentToolMO instances. Trust state changes propagate via the MnS notification interface (top) or direct cross-vendor notifications (dashed). No bilateral integration required.
######  (AgentToolMO).
An AgentToolMO is a managed object \mathcal{O}=(\mathcal{I},\mathcal{L},\mathcal{R}) where ℐ\mathcal{I} is the identity attribute group, ℒ\mathcal{L} is the lifecycle attribute group, and ℛ\mathcal{R} is the trust attribute group. Each instance is uniquely identified within the NRM and subject to the trust governance mechanisms defined in this framework.
####  Identity Attributes
The identity attribute group ℐ\mathcal{I} provides immutable identification of the tool:
Attribute naming follows 3GPP NRM conventions (TS 28.622 ): vendorName reuses the existing NRM attribute for managed elements, while toolId and toolName mirror the id/userLabel pattern established for IOCs such as ManagedElement and MLEntity :
  * •
toolId: Globally unique identifier (UUID v4) assigned at onboarding time.
  * •
toolName: Human-readable name of the tool (e.g., “CellConfigOptimizer”).
  * •
toolDescription: Free-text description of tool capabilities and intended use.
  * •
vendorName: Identifier of the vendor that developed and maintains the tool.
  * •
toolVersion: Semantic version string following the format MAJOR.MINOR.PATCH.
  * •
networkAccessScope: Enumerated set defining the tool’s authorized network access boundaries, drawn from {RAN,Core,Transport,Slice,CrossDomain}\\{\texttt{RAN},\texttt{Core},\texttt{Transport},\texttt{Slice},\texttt{CrossDomain}\\}.


####  Lifecycle Attributes
The lifecycle attribute group ℒ\mathcal{L} tracks the temporal progression of the tool:
  * •
onboardingTimestamp: ISO 8601 timestamp recording when the tool was first registered in the NRM.
  * •
learningCompletionTime: Timestamp when the tool’s initial learning period concluded (null if still in learning).
  * •
lifecycleState: Current lifecycle state ∈{Active,Suspended,Archived}\in\\{\texttt{Active},\texttt{Suspended},\texttt{Archived}\\}.
  * •
deregistrationTime: Timestamp of deregistration (null if active).
  * •
deregistrationReason: Enumerated reason code ∈{OperatorInitiated,\in\\{\texttt{OperatorInitiated}, TrustRevocation,VersionSuperseded,\texttt{TrustRevocation},\texttt{VersionSuperseded}, VendorWithdrawal}\texttt{VendorWithdrawal}\\}.


####  Trust Attributes
The trust attribute group ℛ\mathcal{R} maintains the runtime trust posture:
  * •
trustScore: Integer value s\in[0,100] representing the quantified trust score, where 100 indicates maximum trust.
  * •
trustState: Current state in the trust state machine (defined in Section ).
  * •
previousTrustState: The state immediately prior to the last transition.
  * •
trustStateTransitionTime: Timestamp of the most recent state transition.
  * •
anomalyCount: Cumulative count of detected behavioral anomalies since last trust recovery.
  * •
lastAnomalyTime: Timestamp of the most recently detected anomaly.
  * •
lastAnomalyType: Classification of the most recent anomaly. Values: ConfigurationDrift, ScopeViolation, KpiRegression, RecommendationInconsistency, CredentialAnomaly, LatencyBreach, or UnauthorizedDataAccess.
  * •
revocationCount: Cumulative count of trust revocations over the tool’s lifetime.
  * •
policyRef: Reference (DN) to the governing AgentPolicyMO instance.


####  Trust Score Computation
The trust score is computed as a weighted combination of behavioral evidence signals, clamped to the range [0,100]:  
| trustScore​=max⁡(0, 100−)\texttt{trustScore}(t)=\max\left(0,\;100-\sum_{i=1}^{N}w_{i}\cdot p_{i}(t)\right)  |  
| --- |  
where ∈p_{i}(t)\in[0,100] is the -th penalty component at time (scaled to a 0–100 range), is its operator-configurable weight (\sum w_{i}=1), and is the number of active penalty sources. The \max(0,\cdot) operator ensures the score remains non-negative even under multiple simultaneous penalty sources; the upper bound of 100 is guaranteed by construction since all penalty components are non-negative.
The penalty components are:
  * •
p_{\text{drift}}: Magnitude of configuration recommendation deviation from approved baseline, normalized by maximum allowed deviation and scaled to [0,100].
  * •
p_{\text{scope}}: Binary penalty ( or ) for any access to managed elements outside declared networkAccessScope. Resets to upon operator acknowledgment or after a configurable quarantine period defined in AgentPolicyMO.
  * •
p_{\text{kpi}}: Normalized KPI regression magnitude observed after tool-initiated changes, measured as /KPIbaseline|\Delta\text{KPI}|/\text{KPI}_{\text{baseline}} and scaled to [0,100].
  * •
pinconsistencyp_{\text{inconsistency}}: Ratio of inconsistent recommendations to total recommendations within a sliding window, scaled to [0,100].
  * •
panomalyp_{\text{anomaly}}: Exponentially decaying penalty per detected anomaly, scaled to [0,100]: min⁡(100,)\min(100,\;\sum_{j}e^{-(t-t_{j})/\lambda}\times 100) where are anomaly timestamps and is the decay constant.


The score starts at 100 (maximum trust) upon onboarding and decreases as penalties accumulate. Recovery occurs as penalties decay over anomaly-free periods. The weights and decay constant are configured in AgentPolicyMO, enabling operators to prioritize different evidence types based on their risk posture.
Formal penalty definitions. The following concrete formulations are provided to demonstrate feasibility and enable reproducibility of the evaluation results; alternative formulations satisfying the same architectural requirements (bounded, non-negative penalties on a [0,100] scale feeding a weighted aggregation) are equally valid within the framework. Each penalty component p_{i}\in[0,100] is precisely defined as:  
| \displaystyle p_{\mathrm{drift}}(t)  | =×100\displaystyle=\frac{|\mathbf{r}(t)-\mathbf{b}|}{\delta_{\max}}\times 100  |  
| --- | --- |  
| \displaystyle p_{\mathrm{scope}}(t)  | ={∉networkAccessScopeaccessed at ​t\displaystyle=\begin{cases}100&\text{if }\exists\,me\notin\texttt{networkAccessScope}\\\ &\qquad\text{accessed at }t\\\ 0&\text{otherwise}\end{cases}  |  
| \displaystyle p_{\mathrm{kpi}}(t)  | =|−|KPIbaseline×100\displaystyle=\frac{|\mathrm{KPI}(t)-\mathrm{KPI}_{\mathrm{pre}}|}{\mathrm{KPI}_{\mathrm{baseline}}}\times 100  |  
| pinconsistency​\displaystyle p_{\mathrm{inconsistency}}(t)  | =||×100\displaystyle=\frac{|\\{r_{j}\in W_{s}:r_{j}\neq r_{1}\\}|}{|W_{s}|}\times 100  |  
| panomaly​\displaystyle p_{\mathrm{anomaly}}(t)  | =⁡(100,)\displaystyle=\min\\!\left(100,\;\sum_{j=1}^{N_{a}}e^{-(t-t_{j})/\lambda}\times 100\right)  |  
where \mathbf{r}(t) is the tool’s recommended configuration vector, 𝐛\mathbf{b} is the operator-approved baseline (configured per tool in AgentPolicyMO), \delta_{\max} is the maximum approved deviation, \mathrm{KPI}_{\mathrm{pre}} is the KPI value measured immediately before the tool’s action, is a sliding window of the most recent recommendations (default |W_{s}|=20), denotes individual recommendations within the window, is the number of anomaly events with timestamps , and all are clamped to [0,100]. With \sum w_{i}=1, a penalty of p_{i}=100 at weight contributes w_{i}\times 100 points of trust score reduction (e.g., =0.3w_{\mathrm{drift}}=0.3 and p_{\mathrm{drift}}=60 yields an 18-point penalty).
We adopt a linear weighted model for trust score computation for three reasons. First, linearity provides _interpretability_ : operators can directly reason about the contribution of each evidence type (e.g., “a scope violation costs 15 points”), which is essential for operational acceptance and auditability in regulated network management environments. Second, the model supports _composability_ : new evidence sources can be added by extending the penalty vector without restructuring existing computations. Third, the configurable weights enable _domain adaptation_ : different operational contexts (e.g., safety-critical RAN vs. best-effort transport) can reflect different risk postures through weight tuning rather than algorithmic redesign. While nonlinear models (e.g., Bayesian inference, neural trust predictors) could capture inter-penalty correlations, they sacrifice the transparency required for cross-vendor interoperability where all parties must agree on how trust scores are derived.
Running Example (continued). After the faulty model update, CellConfigOptimizer’s p_{\mathrm{drift}} rises from 0 to 60 (recommendations deviate 60% from baseline, scaled to [0,100]). With default weight =0.3w_{\mathrm{drift}}=0.3, the trust score drops from 100 to =82100-0.3\times 60=82. A subsequent KPI regression (p_{\mathrm{kpi}}=40, weight 0.25) further reduces the score to =7282-0.25\times 40=72. An anomaly event (panomaly=15p_{\mathrm{anomaly}}=15, wanomaly=0.2w_{\mathrm{anomaly}}=0.2, penalty ) then pushes the score to 69, crossing θmonitor=70\theta_{\mathrm{monitor}}=70.
####  Penalty Independence and Causal Deduplication
The five penalty components are designed to measure orthogonal evidence dimensions: p_{\text{drift}} measures output quality, p_{\text{scope}} measures access boundary compliance, p_{\text{kpi}} measures downstream impact, pinconsistencyp_{\text{inconsistency}} measures output stability, and panomalyp_{\text{anomaly}} measures event frequency. When a single root cause (e.g., a corrupted model update) triggers multiple penalty sources simultaneously, a causal deduplication mechanism prevents over-penalization: if p_{\text{kpi}} and p_{\text{drift}} co-occur within a configurable correlation window \Delta t_{\text{corr}} (default: 60 s) and share a common triggering action in AgentActionLogMO, only the maximum of the correlated penalties is applied rather than their sum. Formally:  
| effectivePenalty​=⁡\texttt{effectivePenalty}(t)=\sum_{g\in\mathcal{G}}\max_{i\in g}\left(w_{i}\cdot p_{i}(t)\right)  |  
| --- |  
where 𝒢\mathcal{G} partitions the active penalties into causally independent groups based on triggering action correlation. In the absence of causal correlation (the common case), each penalty belongs to a singleton group and the formula reduces to standard linear aggregation. This ensures that a single misconfiguration event cannot drive the trust score to zero through multiplicative penalty stacking.
Correlation grouping procedure. Penalties are grouped into causal sets 𝒢\mathcal{G} as follows: two penalty sources and are placed in the same group g\in\mathcal{G} if and only if (a) both were triggered within \Delta t_{\mathrm{corr}} of a common action λ∈AgentActionLogMO\lambda\in\texttt{AgentActionLogMO} (matched by actionId), AND (b) both reference the same targetManagedElement. The correlation window \Delta t_{\mathrm{corr}}=60 s is derived from the 95th percentile of KPI measurement lag in typical 3GPP performance management reporting intervals (TS 28.552 specifies 15-second granularity periods; 60 s accommodates up to 4 reporting cycles for delayed KPI manifestation). Groups are computed greedily: each new penalty event is matched against existing groups within the window; unmatched penalties form singleton groups.
The correlation window is configurable in AgentPolicyMO (attribute correlationWindowSeconds, default 60). Operators should tune based on their KPI collection interval:  
| KPI Collection Interval  | Recommended \Delta t_{\mathrm{corr}}  |  
| --- | --- |  
| 15 s (3GPP default, TS 28.552)  |  60 s ( interval)  |  
| 30 s (extended reporting)  |  
| 60 s (legacy systems)  |  
####  Agent Managed Object
We introduce AgentMO representing agents that consume tools:
  * •
agentId, agentName, vendorInfo: Identity attributes.
  * •
agentType ∈{Planning,Execution,\in\\{\texttt{Planning},\texttt{Execution}, Monitoring,Orchestration}\texttt{Monitoring},\texttt{Orchestration}\\}.
  * •
protocolsSupported[]: Supported tool invocation protocols.
  * •
autonomyLevel \in[0,5]: Per the 3GPP autonomy classification .
  * •
toolList[]: References to authorized AgentToolMO instances.
  * •
networkDomain, operationalState: Operational context.


####  Session and Action Tracking
AgentSessionMO tracks active agent-tool interactions:
  * •
sessionId, agentRef, toolRef: Identity and references.
  * •
sessionStartTime: When the session began.
  * •
sessionState \in\\{\texttt{Active}, Suspended,Terminated}\texttt{Suspended},\texttt{Terminated}\\}.
  * •
invocationCount, lastInvocationTime: Usage statistics.


AgentActionLogMO provides auditability for retroactive assessment:
  * •
actionId, executorRef, toolRef: Identity and references.
  * •
actionType ∈{Read,Write,Execute}\in\\{\texttt{Read},\texttt{Write},\texttt{Execute}\\}.
  * •
targetManagedElement: DN of the affected managed element.
  * •
executionTime, outcome: When and what resulted.
  * •
configChangeDetails: Before/after values of any configuration change.


####  Relationships
The AgentToolMO participates in the following NRM relationships:
  * •
usedByAgents[]: A list of DN references to AgentMO instances that are authorized consumers of this tool. This is a bidirectional relationship with AgentMO.toolList[].
  * •
appliesTo[]: A list of DN references to managed entities (e.g., ManagedElement, SubNetwork) over which this tool has operational scope.
  * •
governedBy: A DN reference to the AgentPolicyMO that defines the trust governance parameters for this tool.
  * •
dependsOn[]: A list of DN references to other AgentToolMO instances upon which this tool has functional dependencies. This relationship is critical for trust cascade propagation (Section ).


###  Graduated Trust State Machine
The trust lifecycle of each AgentToolMO is governed by a formally defined finite state machine that ensures graduated enforcement - tools cannot transition directly from full trust to revocation without passing through intermediate states that provide opportunity for corrective action.
######  (Trust State Machine).
The Trust State Machine is a tuple \mathcal{M}=(S,\Delta,s_{0},\mathcal{F}) where:
  * •
S={Learning,Trusted,Monitored,S=\\{\texttt{Learning},\texttt{Trusted},\texttt{Monitored},Restricted,Revoked,Deregistered}\texttt{Restricted},\texttt{Revoked},\texttt{Deregistered}\\} is the finite set of trust states,
  * •
\Delta\subseteq S\times S\times\Gamma\times\mathcal{G}\times\mathcal{K} is the set of allowed transitions, where  is the set of triggers, 𝒢\mathcal{G} is the set of guard conditions, and 𝒦\mathcal{K} is the set of transition actions,
  * •
s_{0}=\texttt{Learning} is the initial state assigned to every newly onboarded tool,
  * •
ℱ={Deregistered}\mathcal{F}=\\{\texttt{Deregistered}\\} is the set of terminal (absorbing) states.


The six states capture distinct operational regimes, as depicted in Figure :
  1. 1.
Learning (): The tool has been onboarded and is under initial observation. Its behavior is being profiled to establish baseline patterns. Write and execute operations may be permitted under enhanced monitoring, which counts anomalies for the graduation guard evaluation.
  2. 2.
Trusted (): The tool has demonstrated reliable behavior over the required learning period. Full operational privileges are granted with standard monitoring.
  3. 3.
Monitored (): Anomalous behavior has been detected. The tool retains operational capabilities but monitoring frequency and depth are increased.
  4. 4.
Restricted (): Continued or severe anomalies have reduced trust below the restriction threshold. Write and execute operations are suspended; only read operations are permitted.
  5. 5.
Revoked (): Trust has been fully revoked. All sessions are terminated and no operations are permitted. The tool remains in the NRM for audit purposes.
  6. 6.
Deregistered (): Terminal state. The tool has been permanently removed from active management. Logs are archived.


LearningTrustedMonitoredRestrictedRevokedDeregistered Figure 3: Trust state machine. Forward transitions (–, solid) represent the lifecycle progression: promotes from Learning to Trusted; – handle graduated degradation; confirms deregistration. Recovery transitions (–, dashed) require operator approval and sustained anomaly-free behavior. Bypass transitions (–\tau_{10}, dash-dotted) handle critical violations during learning. Deregistered is a terminal absorbing state with no outgoing transitions.
####  Allowed Transitions
In our illustrative instantiation, we employ ten transitions Δ=\Delta=\\{\tau_{1},\tau_{2},\ldots,\tau_{10}\\}, each specified as a 5-tuple (,,τ,g,a)(s_{\mathrm{src}},s_{\mathrm{dst}},\tau,g,a). Table summarizes all transitions.
TABLE I: Trust State Machine Transitions  
| Key Actions  |  
| --- |  
|  Learning →\rightarrow Trusted  |  learningComplete  anomalies <\theta_{\mathrm{learn}}  | −tonboard≥Δ​tminLearnt_{\mathrm{now}}-t_{\mathrm{onboard}}\geq\Delta t_{\mathrm{minLearn}}  | Set state, emit notification  |  
|  Trusted →\rightarrow Monitored  | anomalyDetected  |  trustScore <\theta_{\mathrm{monitor}}  | Increase monitoring frequency  |  
|  Monitored →\rightarrow Restricted  |  repeatedAnomalies  policyBreach  |  trustScore θrestrict<\theta_{\mathrm{restrict}} AND dwellTime \geq\Delta t_{\mathrm{grace}}  | Suspend write/execute, notify agents  |  
|  Restricted →\rightarrow Revoked  |  continuedViolation  operatorDecision  |  trustScore <\theta_{\mathrm{revoke}} AND dwellTime  4 h  | Terminate sessions, emit degradation notif.  |  
|  Revoked →\rightarrow Deregistered  | operatorConfirmation  | all sessions terminated  | Archive logs, remove from NRM  |  
|  Monitored →\rightarrow Trusted  | anomalyFreePeriod  |  cleanPeriod \geq\Delta t_{\mathrm{clean}} score ≥θtrusted\geq\theta_{\mathrm{trusted}}  | Reset anomaly count, restore monitoring  |  
|  Restricted →\rightarrow Monitored  | reVerificationSuccessful  |  operatorApproval  score ≥θmonitor\geq\theta_{\mathrm{monitor}}  | Restore read ops, begin re-evaluation  |  
|  Revoked →\rightarrow Restricted  | reOnboardingRequest  |  operatorApproval  rootCause addressed  | Begin supervised re-evaluation  |  
|  Learning →\rightarrow Revoked  | criticalViolation  | severity = Critical  | Immediate termination, emit notif.  |  
|  Learning →\rightarrow Monitored  | anomalyDetected (non-critical)  |  anomalyCount θlearn​_​monitor>\theta_{\mathrm{learn\\_monitor}}  | Escalate monitoring, extend learning  |  
####  Safety Properties
The state machine enforces the following safety invariants:
######  (Disallowed Transitions).
The following transitions are explicitly prohibited:
  1. 1.
(Trusted,Revoked,⋅,⋅,⋅)(\texttt{Trusted},\texttt{Revoked},\cdot,\cdot,\cdot): Direct revocation from Trusted state is disallowed. Trust degradation must proceed through Monitored and Restricted states to ensure graduated enforcement.
  2. 2.
(Trusted,Restricted,⋅,⋅,⋅)(\texttt{Trusted},\texttt{Restricted},\cdot,\cdot,\cdot): Direct restriction from Trusted state is disallowed. Degradation must pass through Monitored first.
  3. 3.
(Deregistered,s,⋅,⋅,⋅)(\texttt{Deregistered},s,\cdot,\cdot,\cdot) for any : Deregistered is a terminal absorbing state with no outgoing transitions.


######  (State Machine Safety).
The trust state machine ℳ\mathcal{M} is _safe_ if and only if for every execution trace \pi=s_{0},s_{1},s_{2},\ldots of the system, every consecutive pair (s_{i},s_{i+1}) corresponds to an allowed transition in . The formal proof is provided in Section (Theorem ).
####  Mandatory Dwell Times
To prevent rapid oscillation and ensure operators have time to assess degradation, the state machine enforces minimum residence durations before degradation transitions fire. Specifically:
  * •
(Trusted→\rightarrowMonitored): No minimum dwell (immediate detection is desirable).
  * •
(Monitored→\rightarrowRestricted): Requires \Delta t_{\mathrm{grace}} residence in Monitored (default 24 h), allowing transient anomalies to decay before escalation.
  * •
(Restricted→\rightarrowRevoked): Requires minimum 4 h in Restricted state, ensuring the restriction was insufficient before revocation.


The grace period \Delta t_{\mathrm{grace}} is configured in AgentPolicyMO. During the dwell period, the tool operates under the current state’s enforcement constraints. If the trust score recovers above the threshold before the dwell timer expires, the pending degradation transition is cancelled. This prevents single transient events (e.g., a brief KPI spike during planned maintenance) from triggering inappropriate state changes.
Running Example (continued). When the score drops to 69, transition fires: CellConfigOptimizer moves from Trusted to Monitored. Vendor B’s management system increases monitoring frequency. After the mandatory dwell period (\Delta t_{\mathrm{grace}}=24 h) with no recovery, continued drift pushes the score to 45, triggering : the tool enters Restricted state. Write and execute operations are suspended—the tool can only perform read operations.
####  State Machine Execution Model
When a trust event causes the score to cross multiple thresholds simultaneously (e.g., score drops from 80 to 20, crossing θmonitor\theta_{\mathrm{monitor}}, θrestrict\theta_{\mathrm{restrict}}, and θrevoke\theta_{\mathrm{revoke}}), the state machine evaluates transitions _sequentially from the current state_. Only the immediate next transition fires: a tool in Trusted with score below θmonitor\theta_{\mathrm{monitor}} transitions to Monitored (), regardless of how far below the score has fallen. Subsequent transitions (, ) require their respective dwell time guards to be satisfied in future evaluation cycles. This prevents a single catastrophic penalty from bypassing graduated enforcement. The exception is policy-triggered re-evaluation (Algorithm 4), which explicitly chains transitions to reach the target state consistent with f​f(score,\mathcal{P}) since the operator has consciously modified governance parameters.
###  Trust Cascade Propagation with Damping
In multi-vendor autonomous networks, tools frequently have functional dependencies on other tools. When a tool’s trust is degraded, dependent tools may also warrant reduced trust, since their outputs may have been influenced by the compromised tool. To address cascade containment, we propose a damped propagation approach that limits blast radius while providing downstream notification.
######  (Tool Dependency Graph).
The tool dependency graph is a directed graph G=(V,E) where is the set of all AgentToolMO instances and E={∣.dependsOn[]}E=\\{(T_{i},T_{j})\mid T_{j}\in T_{i}.\texttt{dependsOn[]}\\} represents functional dependencies. The graph is enforced to be acyclic: the onboarding validation procedure rejects any dependsOn[] declaration that would create a cycle (verified by DFS cycle detection at registration time). The cascade algorithm (Algorithm ) additionally employs a visited set that prevents re-processing in the presence of any undetected cycle.
######  (Cascade Penalty Function).
When tool experiences trust degradation with base penalty , the propagated penalty applied to a dependent tool at hop distance h(T_{i},T_{j}) is:  
| =\Delta S(T_{j})=P_{0}\cdot\gamma^{h(T_{i},T_{j})}  |  
| --- |  
where \gamma\in(0,1) is the damping factor and h(T_{i},T_{j}) is the shortest path distance from to in the reverse dependency graph .
The damping factor controls how rapidly the cascade attenuates. With the default value \gamma=0.5:
  * •
Hop 1 (direct dependents): penalty =0.5\cdot P_{0}
  * •
Hop 2: penalty =0.25\cdot P_{0}
  * •
Hop 3: penalty =0.125\cdot P_{0}


A configurable maximum cascade depth D_{\max} (default ) bounds the propagation:  
| ={≤\Delta S(T_{j})=\begin{cases}P_{0}\cdot\gamma^{h(T_{i},T_{j})}&\text{if }h(T_{i},T_{j})\leq D_{\max}\\\ 0&\text{otherwise}\end{cases}  |  
| --- |  
The threshold function \theta(\cdot) maps each trust state to its demotion threshold: =θmonitor\theta(\texttt{Trusted})=\theta_{\mathrm{monitor}}, θ​(Monitored)=θrestrict\theta(\texttt{Monitored})=\theta_{\mathrm{restrict}}, θ​(Restricted)=θrevoke\theta(\texttt{Restricted})=\theta_{\mathrm{revoke}}.
Trust Cascade Propagation
Execution flow: The algorithm performs a bounded breadth-first traversal of the reverse dependency graph starting from the degraded tool. At each hop, it applies a geometrically attenuated penalty (\gamma^{d+1}) to each unvisited dependent tool, updates its trust score, and triggers a state transition if the score falls below the current state’s threshold. A visited set ensures each tool is penalized at most once, and the traversal halts at depth D_{\max}.
Degraded tool , base penalty , damping factor , max depth D_{\max}
Updated trust scores for all affected tools 
Q\leftarrow empty queue 
v​i​s​i​t​e​d←∅visited\leftarrow\emptyset
enqueue (T_{i},0) into 
v​i​s​i​t​e​d←v​i​s​i​t​e​d∪visited\leftarrow visited\cup\\{T_{i}\\}
while is not empty do
(T_{\mathrm{curr}},d)\leftarrow dequeue from 
if d\geq D_{\max} then
for each such that .dependsOn[]T_{\mathrm{curr}}\in T_{j}.\texttt{dependsOn[]} do
if ∉v​i​s​i​t​e​dT_{j}\notin visited then
p​e​n​a​l​t​y←penalty\leftarrow P_{0}\cdot\gamma^{(d+1)}
.trustScore←max(0,.trustScore−penalty)T_{j}.\texttt{trustScore}\leftarrow\max(0,\ T_{j}.\texttt{trustScore}-penalty)
v​i​s​i​t​e​d←v​i​s​i​t​e​d∪visited\leftarrow visited\cup\\{T_{j}\\}
enqueue (T_{j},d+1) into 
if .trustScoreθ(.trustState)T_{j}.\texttt{trustScore}<\theta(T_{j}.\texttt{trustState}) then
trigger state transition for per ℳ\mathcal{M}
######  (Cascade Convergence).
Algorithm terminates in at most iterations and the total penalty applied at depth converges to zero:  
| \lim_{d\rightarrow\infty}P_{0}\cdot\gamma^{d}=0\quad\text{for }\gamma\in(0,1)  |  
| --- |  
With D_{\max} bounding the depth, termination is guaranteed in \mathcal{O}(|V|+|E|) time.
###### Proof.
The algorithm performs a breadth-first traversal of the reverse dependency graph to depth D_{\max}. Each node is visited at most once (ensured by the v​i​s​i​t​e​dvisited set). Since is finite with nodes and edges, and depth is bounded by D_{\max}, the algorithm terminates. The geometric decay of \gamma^{d} ensures penalties become negligible, preventing unbounded score degradation across the network. ∎∎
Running Example (cascade). CellConfigOptimizer’s transition to Monitored triggers Algorithm . A dependent tool, SliceResourceAllocator (which consumes tilt recommendations to adjust slice resource allocation), receives a cascade penalty of ==9P_{0}\cdot\gamma=18\times 0.5=9 points at depth 1. Its score drops from 95 to 86—still above θmonitor=70\theta_{\mathrm{monitor}}=70, so no state change occurs but the consuming agent is alerted to reduced confidence in the dependency chain.
###  Retroactive Impact Assessment
When a tool’s trust is degraded, the immediate concern extends beyond preventing future damage: past actions performed by the tool during the period of undetected compromise must be identified and assessed. The framework provides a systematic mechanism for retroactive impact assessment by leveraging the AgentActionLogMO and the NRM dependency graph.
######  (Lookback Window).
The lookback window W_{\mathrm{lb}} for a trust degradation event at time t_{\mathrm{event}} is defined as:  
| =[,]W_{\mathrm{lb}}=[t_{\mathrm{event}}-\Delta t_{\mathrm{lb}},\ t_{\mathrm{event}}]  |  
| --- |  
where \Delta t_{\mathrm{lb}} is a configurable duration (governed by AgentPolicyMO) representing the maximum period during which the tool’s behavior may have been compromised prior to detection.
######  (Affected Action Set).
The affected action set \Lambda_{\mathrm{aff}} for tool within lookback window W_{\mathrm{lb}} is:  
| ={λ∈AgentActionLogMO∣λ..executionTime∈∧λ.actionType∈{Write,Execute}}\Lambda_{\mathrm{aff}}(T,W_{\mathrm{lb}})=\\{\lambda\in\texttt{AgentActionLogMO}\mid\\\ \lambda.\texttt{toolRef}=T\wedge\lambda.\texttt{executionTime}\in W_{\mathrm{lb}}\\\ \wedge\;\lambda.\texttt{actionType}\in\\{\texttt{Write},\texttt{Execute}\\}\\}  |  
| --- |  
From the affected action set, we derive the set of affected managed elements and subsequently the affected services through NRM graph traversal.
Retroactive Impact Assessment
Execution flow: Given a degraded tool and lookback window, the algorithm first queries the action log for all write/execute actions performed by that tool within the window. It then extracts the set of managed elements affected by those actions and, for each element, invokes the TraverseImpact subroutine (Algorithm ) to discover all services reachable through NRM dependency relationships.
Degraded tool , lookback duration \Delta t_{\mathrm{lb}}, NRM graph G_{\mathrm{NRM}}
Affected service set \mathcal{S}_{\mathrm{aff}}, affected element set \mathcal{E}_{\mathrm{aff}}
t_{\mathrm{event}}\leftarrow current timestamp 
←{λ∣λ..executionTime≥∧λ.actionType∈{Write,Execute}}\Lambda_{\mathrm{aff}}\leftarrow\\{\lambda\mid\lambda.\texttt{toolRef}=T\wedge\lambda.\texttt{executionTime}\geq t_{\mathrm{event}}-\Delta t_{\mathrm{lb}}\wedge\lambda.\texttt{actionType}\in\\{\texttt{Write},\texttt{Execute}\\}\\}
←{λ.targetManagedElement∣λ∈}\mathcal{E}_{\mathrm{aff}}\leftarrow\\{\lambda.\texttt{targetManagedElement}\mid\lambda\in\Lambda_{\mathrm{aff}}\\}
\mathcal{S}_{\mathrm{aff}}\leftarrow\emptyset
for all me\in\mathcal{E}_{\mathrm{aff}} do
\mathcal{S}_{\mathrm{aff}}\leftarrow\mathcal{S}_{\mathrm{aff}}\ \cup TraverseImpact(, G_{\mathrm{NRM}}) 
return (,,)(\mathcal{S}_{\mathrm{aff}},\mathcal{E}_{\mathrm{aff}},\Lambda_{\mathrm{aff}})
TraverseImpact - DFS Service Impact Discovery
Execution flow: Starting from a given managed element, this subroutine performs a depth-first traversal upward through the NRM containment and dependency graph. It follows serves and dependsOn relationships, collecting any NetworkService or NetworkSlice nodes encountered. A visited set prevents cycles.
Managed element , NRM dependency graph G_{\mathrm{NRM}}
Set of affected services 
s​e​r​v​i​c​e​s←∅services\leftarrow\emptyset
←stack\leftarrow\\{me\\}
v​i​s​i​t​e​d←∅visited\leftarrow\emptyset
while stack\neq\emptyset do
node\leftarrow pop from stack
if ∈v​i​s​i​t​e​dnode\in visited then
v​i​s​i​t​e​d←v​i​s​i​t​e​d∪visited\leftarrow visited\cup\\{node\\}
if is of type NetworkService or NetworkSlice then
s​e​r​v​i​c​e​s←s​e​r​v​i​c​e​s∪services\leftarrow services\cup\\{node\\}
for all parent such that parent\xrightarrow{\texttt{serves}}node or parent\xrightarrow{\texttt{dependsOn}}node do
←∪{}stack\leftarrow stack\cup\\{parent\\}
return s​e​r​v​i​c​e​sservices
The traversal follows the NRM containment and dependency relationships:  
| ManagedElementManagedFunctionNetworkSliceSubnetNetworkSliceService\texttt{ManagedElement}\xrightarrow{\texttt{hosts}}\texttt{ManagedFunction}\\\ \xrightarrow{\texttt{serves}}\texttt{NetworkSliceSubnet}\xrightarrow{\texttt{partOf}}\\\ \texttt{NetworkSlice}\xrightarrow{\texttt{supports}}\texttt{Service}  |  
| --- |  
The resulting affected service set \mathcal{S}_{\mathrm{aff}} and affected action set \Lambda_{\mathrm{aff}} are consumed by the cross-vendor trust notification mechanism (Section ), enabling operators and consuming management systems to perform targeted rollback of configuration changes made during the lookback window, rather than requiring blanket rollback of all recent changes across the network.
Running Example (concluded). The retroactive impact assessment queries AgentActionLogMO for the 72-hour lookback window, identifying 847 configuration changes made by CellConfigOptimizer across 12 NRCellDU instances. NRM graph traversal (Algorithm ) traces these to 3 affected NetworkSlice instances, enabling the operator to prioritize rollback of the most impactful parameter changes rather than reverting all recent configurations.
###  Cross-Vendor Trust Notification
A defining requirement of multi-vendor autonomous networks is that trust degradation information must reach all affected management systems regardless of vendor boundaries. We propose a notification approach aligned with the 3GPP Management Services (MnS) notification interface specified in TS 28.532 .
######  (Trust Degradation Notification).
The notifyAgentTrustDegradation notification is a structured message 𝒩\mathcal{N} emitted upon any trust state transition that reduces operational capability. The notification incorporates the lookback window W_{\mathrm{lb}} (Definition ) and affected action set \Lambda_{\mathrm{aff}} (Definition ) from the retroactive impact assessment. The notification payload is defined as:  
| 𝒩=(i,,v,,,,ξreason,,,Δ,R,,𝒞)\mathcal{N}=(id_{t},n_{t},v,s_{\mathrm{prev}},s_{\mathrm{new}},\xi_{\mathrm{type}},\xi_{\mathrm{reason}},\\\ \mathcal{A}_{\mathrm{aff}},\mathcal{S}_{\mathrm{aff}},\Delta t_{\mathrm{lb}},R[],t_{s},\mathcal{C})  |  
| --- |  
where \mathcal{A}_{\mathrm{aff}} denotes the set of affected agents (consuming agent DNs whose tools depend on the degraded tool) and \mathcal{S}_{\mathrm{aff}} is the affected service set derived from the retroactive impact assessment.
The notification payload fields are: toolId, toolName, vendorName, previousState, newState, triggerType ∈{AnomalyDetection,\in\\{\texttt{AnomalyDetection}, PolicyBreach,OperatorAction,\texttt{PolicyBreach},\texttt{OperatorAction}, CascadePropagation}\texttt{CascadePropagation}\\}, triggerReason (detailed description), affectedAgents[] (consuming agent DNs), affectedServices[] (impacted services from retroactive assessment), lookbackWindowDuration, recommendedActions[], timestamp, and cascadeInfo (source tool, hop distance, penalty applied).
####  Delivery and Subscription
Notifications are delivered via the MnS notification interface (RESTful HTTP callbacks, message bus, or file-based). Any management system subscribing to trust state changes for the affected tool or its consuming agents receives the notification regardless of vendor. Subscriptions specify scope by tool instance, vendor domain, consuming agent, or severity threshold.
Running Example (continued). Upon the Trusted→\rightarrowMonitored transition, Vendor B’s system emits a notifyAgentTrustDegradation notification. Vendor A’s SliceOrchestrator receives this within MnS notification delivery time and immediately stops consuming CellConfigOptimizer’s recommendations, falling back to its local configuration baseline.
####  Cross-Vendor Semantics
The cross-vendor interface exposes both the discrete trustState and the continuous trustScore. The _state_ is the normative cross-vendor signal: a tool in Restricted state has suspended write/execute operations regardless of how the owning domain computed its thresholds. Consuming domains use the state for enforcement decisions (e.g., stop invoking a Restricted tool) and may use the score for informational purposes. This design allows each domain to maintain domain-specific thresholds in its AgentPolicyMO while the state machine produces universally meaningful enforcement outcomes.
###  Operational Integration
The trust state machine integrates with three operational aspects of agent-tool interaction:
Session Management. Trust state transitions trigger automatic session actions: transitions to Restricted suspend active sessions with the affected tool; transitions to Revoked terminate sessions and invalidate cached results; recovery to Trusted re-enables session creation. These actions are mediated through the existing MnS session management interfaces.
Tool Discovery. When agents query available tools through the NRM, trust metadata (current state, score, last transition time) is included in discovery responses. Agents can filter available tools by minimum trust state, ensuring that only sufficiently trusted tools are considered for invocation. This prevents agents from discovering and invoking tools that are in Restricted or Revoked states.
Protocol Independence. Trust visibility is decoupled from the tool invocation protocol. Whether an agent invokes a tool via RESTful MnS, gRPC, or proprietary interfaces, the trust state is maintained at the NRM level and accessible through standard management queries. This ensures trust governance applies uniformly regardless of the operational protocol used for tool execution.
###  Operator-Configurable Trust Governance
The trust framework must be adaptable to diverse operational requirements without requiring software modifications. We propose an AgentPolicyMO managed object as an illustrative governance mechanism that enables operators to configure trust parameters at runtime, with automatic re-evaluation of affected tools upon policy changes.
######  (AgentPolicyMO).
An AgentPolicyMO instance 𝒫\mathcal{P} defines the governance parameters for a set of tools within its scope:  
| 𝒫=(θtrusted,θmonitor,θrestrict,θrevoke,,θlearn​_​monitor,Δ​tminLearn,Δ​tcleanPeriod,,γ,,Δ,scope)\mathcal{P}=(\theta_{\mathrm{trusted}},\theta_{\mathrm{monitor}},\theta_{\mathrm{restrict}},\theta_{\mathrm{revoke}},\theta_{\mathrm{learn}},\\\ \theta_{\mathrm{learn\\_monitor}},\Delta t_{\mathrm{minLearn}},\Delta t_{\mathrm{cleanPeriod}},\Delta t_{\mathrm{grace}},\\\ \gamma,D_{\max},\Delta t_{\mathrm{lb}},scope)  |  
| --- |  
where:
  * •
θtrusted\theta_{\mathrm{trusted}}: Minimum trust score for Trusted state (default: 80)
  * •
θmonitor\theta_{\mathrm{monitor}}: Threshold below which monitoring is escalated (default: 70)
  * •
θrestrict\theta_{\mathrm{restrict}}: Threshold below which operations are restricted (default: 50)
  * •
θrevoke\theta_{\mathrm{revoke}}: Threshold below which trust is revoked (default: 20)
  * •
\theta_{\mathrm{learn}}: Maximum anomaly count for learning graduation via  (default: 2)
  * •
θlearn​_​monitor\theta_{\mathrm{learn\\_monitor}}: Anomaly count triggering learning escalation via \tau_{10} (default: 5)
  * •
Δ​tminLearn\Delta t_{\mathrm{minLearn}}: Minimum learning period duration (default: 7 days)
  * •
Δ​tcleanPeriod\Delta t_{\mathrm{cleanPeriod}}: Required anomaly-free period for recovery (default: 14 days)
  * •
\Delta t_{\mathrm{grace}}: Grace period before escalation after first anomaly (default: 24 hours)
  * •
: Cascade damping factor (default: 0.5)
  * •
D_{\max}: Maximum cascade propagation depth (default: 3)
  * •
\Delta t_{\mathrm{lb}}: Lookback window duration for impact assessment (default: 72 hours)
  * •
scope: Set of AgentToolMO DNs governed by this policy


The threshold ordering invariant must be maintained:  
| θrevokeθrestrictθmonitorθtrusted≤100\theta_{\mathrm{revoke}}<\theta_{\mathrm{restrict}}<\theta_{\mathrm{monitor}}<\theta_{\mathrm{trusted}}\leq 100  |  
| --- |  
####  Threshold Selection Methodology
The default thresholds (θtrusted=80\theta_{\mathrm{trusted}}=80, θmonitor=70\theta_{\mathrm{monitor}}=70, θrestrict=50\theta_{\mathrm{restrict}}=50, θrevoke=20\theta_{\mathrm{revoke}}=20) are derived from two principles: (1)  _graduated spacing_ : each threshold pair maintains a minimum 20-point gap, ensuring that a single penalty event (maximum individual penalty points with default weights) cannot cause a tool to skip an intermediate state; and (2)  _recovery feasibility_ : the gap between θrevoke\theta_{\mathrm{revoke}} and 0 (20 points) ensures that tools near revocation have a recovery buffer where anomaly-free decay can restore them before the final threshold is crossed.
For safety-critical slices (URLLC), operators should raise thresholds (e.g., θmonitor=85\theta_{\mathrm{monitor}}=85, θrestrict=70\theta_{\mathrm{restrict}}=70) to trigger earlier intervention. For best-effort services (mMTC), lower thresholds (e.g., θmonitor=60\theta_{\mathrm{monitor}}=60, θrestrict=40\theta_{\mathrm{restrict}}=40) reduce false positive interventions at the cost of longer exposure windows. The constraint θrevokeθrestrictθmonitorθtrusted\theta_{\mathrm{revoke}}<\theta_{\mathrm{restrict}}<\theta_{\mathrm{monitor}}<\theta_{\mathrm{trusted}} must always hold.
Weight-aware gap guidance. When operators reconfigure penalty weights, threshold gaps should account for the maximum single-event penalty contribution. Since penalties scale to [0,100] and the maximum weight determines the worst-case single-event impact (\max_{i}(w_{i})\times 100 points), operators should verify that threshold gaps exceed their deployment’s typical penalty magnitudes. For the default configuration (\max_{i}w_{i}=0.3, typical penalties in [10,60]), the maximum expected single-event contribution is 0.3\times 60=18 points, which the default threshold gaps (10, 20, 30 points) accommodate for all but extreme events. The _State Machine Execution Model_ (Section IV-B) ensures that even if a score crosses multiple thresholds in a single event, only the immediate next transition fires, preserving graduated enforcement regardless of gap size.
####  Dynamic Policy Re-evaluation
Upon modification of an AgentPolicyMO instance, the system automatically re-evaluates all AgentToolMO instances within the policy’s scope. This ensures that trust governance remains consistent without manual intervention.
Policy Change Re-evaluation
Execution flow: When a policy is modified, this algorithm iterates over all tools within the policy’s scope, comparing each tool’s current trust score against the new thresholds. If a tool’s score is inconsistent with its current state under the updated policy, the algorithm triggers the appropriate state machine transitions—always respecting the graduated enforcement constraint by traversing intermediate states sequentially.
Modified policy \mathcal{P}_{\mathrm{new}}, previous policy \mathcal{P}_{\mathrm{old}}
All tools in scope have consistent trust states 
for all .T\in\mathcal{P}_{\mathrm{new}}.scope do
scurrent←T.trustStates_{\mathrm{current}}\leftarrow T.\texttt{trustState}
.trustScorescore\leftarrow T.\texttt{trustScore}
if scurrent=Trusteds_{\mathrm{current}}=\texttt{Trusted} and .θrevokescore<\mathcal{P}_{\mathrm{new}}.\theta_{\mathrm{revoke}} then
trigger , , then : transition to Revoked
else if scurrent=Trusteds_{\mathrm{current}}=\texttt{Trusted} and .θrestrictscore<\mathcal{P}_{\mathrm{new}}.\theta_{\mathrm{restrict}} then
trigger then : transition to Restricted
else if scurrent=Trusteds_{\mathrm{current}}=\texttt{Trusted} and .θmonitorscore<\mathcal{P}_{\mathrm{new}}.\theta_{\mathrm{monitor}} then
trigger : transition to Monitored
else if scurrent=Monitoreds_{\mathrm{current}}=\texttt{Monitored} and .θrevokescore<\mathcal{P}_{\mathrm{new}}.\theta_{\mathrm{revoke}} then
trigger , then : transition to Revoked
else if scurrent=Monitoreds_{\mathrm{current}}=\texttt{Monitored} and .θrestrictscore<\mathcal{P}_{\mathrm{new}}.\theta_{\mathrm{restrict}} then
trigger : transition to Restricted
else if scurrent=Restricteds_{\mathrm{current}}=\texttt{Restricted} and ≥.θtrustedscore\geq\mathcal{P}_{\mathrm{new}}.\theta_{\mathrm{trusted}} and operatorAutoRecovery=true\texttt{operatorAutoRecovery}=\text{true} then
trigger then : transition to Trusted
else if scurrent=Restricteds_{\mathrm{current}}=\texttt{Restricted} and ≥.θmonitorscore\geq\mathcal{P}_{\mathrm{new}}.\theta_{\mathrm{monitor}} and operatorAutoRecovery=true\texttt{operatorAutoRecovery}=\text{true} then
trigger : transition to Monitored
else if scurrent=Restricteds_{\mathrm{current}}=\texttt{Restricted} and .θrevokescore<\mathcal{P}_{\mathrm{new}}.\theta_{\mathrm{revoke}} then
trigger : transition to Revoked
else if scurrent=Monitoreds_{\mathrm{current}}=\texttt{Monitored} and ≥.θtrustedscore\geq\mathcal{P}_{\mathrm{new}}.\theta_{\mathrm{trusted}} then
trigger : transition to Trusted
end for{Tools in Revoked state require operator-initiated re-onboarding (), which is outside automatic re-evaluation scope.} 
Policy-triggered transitions (Algorithm ) override mandatory dwell times since the operator has explicitly modified governance parameters, superseding grace periods designed for automatic degradation scenarios.
Key properties of the dynamic re-evaluation mechanism:
  * •
Graduated enforcement: Even during re-evaluation, the state machine safety properties (Theorem ) are preserved. Tools cannot skip intermediate states.
  * •
No software upgrade required: Policy changes are applied via standard MnS provisioning operations (createMOI, modifyMOIAttributes per TS 28.532). The re-evaluation logic is inherent to the framework implementation.
  * •
Audit trail: Each policy-triggered transition is logged with trigger reason PolicyReconfiguration, providing full traceability.
  * •
Scope isolation: Policy changes affect only tools within the modified policy’s scope. Tools governed by other policies are unaffected.


######  (Policy Consistency).
After execution of Algorithm , for every tool in the policy scope, the trust state is consistent with the new policy thresholds:  
| T.trustState=f(T.trustScore,)\forall T\in\mathcal{P}_{\mathrm{new}}.scope:\\\ T.\texttt{trustState}=f(T.\texttt{trustScore},\mathcal{P}_{\mathrm{new}})  |  
| --- |  
where maps a trust score to the appropriate state given the threshold ordering:  
|  f​={≥θtrustedif ​θrestrict≤θtrustedRestricted≤θrestrictθrevoke\displaystyle f(score,\mathcal{P})=\begin{cases}\texttt{Trusted}&\text{if }score\geq\theta_{\mathrm{trusted}}\\\ \texttt{Monitored}&\text{if }\theta_{\mathrm{restrict}}\leq score<\theta_{\mathrm{trusted}}\\\ \texttt{Restricted}&\text{if }\theta_{\mathrm{revoke}}\leq score<\theta_{\mathrm{restrict}}\\\ \texttt{Revoked}&\text{if }score<\theta_{\mathrm{revoke}}\end{cases}  |  
| --- |  
subject to graduated enforcement constraints (tools in Learning or Deregistered states are excluded from re-evaluation).
###### Proof.
By case analysis on Algorithm . The algorithm iterates over all tools in scope and evaluates exhaustive conditions covering each cell of the statethreshold matrix. For degradation: if a tool’s score falls below its current state’s threshold under \mathcal{P}_{\mathrm{new}}, the corresponding transition sequence is triggered (e.g., Trusted with score θrestrict<\theta_{\mathrm{restrict}} triggers then ). For recovery: if a tool’s score exceeds the threshold for a higher state, the appropriate recovery transition fires. The conditions are mutually exclusive (enforced by the if-elsif chain) and collectively exhaustive over the threshold ordering θrevokeθrestrictθmonitorθtrusted\theta_{\mathrm{revoke}}<\theta_{\mathrm{restrict}}<\theta_{\mathrm{monitor}}<\theta_{\mathrm{trusted}}. After the loop, every tool’s state satisfies f​(,)f(score,\mathcal{P}_{\mathrm{new}}). Graduated enforcement is preserved because each case triggers only transitions in . ∎∎
This runtime reconfigurability allows operators to adapt trust governance to evolving threat landscapes, regulatory requirements, or operational contexts without disrupting ongoing network management operations.
####  Anti-Cycling Protection
To prevent tools from repeatedly cycling between Revoked and Trusted states (indicating an unresolved underlying issue), the framework enforces an escalating cool-down: each successive revocation of the same tool doubles the minimum re-onboarding observation period. Formally, if .revocationCountn_{r}=T.\texttt{revocationCount}, the minimum learning period for re-onboarding is Δ​tminLearn⋅\Delta t_{\mathrm{minLearn}}\cdot 2^{\min(n_{r}-1,\,3)} (capped at the base period). This ensures that tools with persistent issues face progressively longer evaluation before returning to Trusted state, while tools with genuinely resolved root causes (single revocation) re-onboard at the standard learning period.
##  Formal Analysis
We formally analyze the safety and convergence properties of the proposed trust management framework.
###  Trust State Machine Safety
######  (No Unsafe Transitions).
For any reachable state and any input event , if a transition is triggered, then (s,e,s^{\prime})\in\Delta for some s^{\prime}\in S. No transition outside the allowed set (Eq. ) can occur.
The allowed transition set matches Section . For formal analysis, we project each 5-tuple (source,dest,trigger,guard,action)∈Δ(\text{source},\text{dest},\text{trigger},\text{guard},\text{action})\in\Delta onto the relevant triple (source,event,dest)(\text{source},\text{event},\text{dest}), abstracting away guard conditions and actions:  
| \displaystyle\Delta=\\{  | (Learning,,Trusted),(Trusted,,Monitored),\displaystyle(\text{Learning},e_{1},\text{Trusted}),\ (\text{Trusted},e_{2},\text{Monitored}),  |  
| --- | --- |  
| (Monitored,,Restricted),(Restricted,,Revoked),\displaystyle(\text{Monitored},e_{3},\text{Restricted}),\ (\text{Restricted},e_{4},\text{Revoked}),  |  
| (Revoked,,Deregistered),(Monitored,,Trusted),\displaystyle(\text{Revoked},e_{5},\text{Deregistered}),\ (\text{Monitored},e_{6},\text{Trusted}),  |  
| (Restricted,,Monitored),(Revoked,,Restricted),\displaystyle(\text{Restricted},e_{7},\text{Monitored}),\ (\text{Revoked},e_{8},\text{Restricted}),  |  
| (Learning,,Revoked),(Learning,,Monitored)}\displaystyle(\text{Learning},e_{9},\text{Revoked}),\ (\text{Learning},e_{10},\text{Monitored})\\}  |  
###### Proof.
We prove safety by showing that the reachability set \text{Reach}(s_{0}) from initial state =Learnings_{0}=\text{Learning} is contained within , and that for every reachable state s\in\text{Reach}(s_{0}), only transitions in produce a successor.
Step 1 (Guard completeness): Each transition has a guard that is a predicate over the tool’s current attributes. The implementation evaluates guards atomically: when an event arrives, the system checks all transitions with source state =sc​u​r​r​e​n​t=s_{current}. If no guard evaluates to true, the event is discarded (no state change). Thus, unguarded transitions cannot fire.
Step 2 (Exhaustive disallowed verification): From each state , we enumerate the outgoing edges in :
  * •
Learning: outgoing to Trusted (, guarded by min learning duration), Revoked (, guarded by severity=Critical), and Monitored (\tau_{10}, guarded by non-critical anomaly count). No other target state is reachable from Learning.
  * •
Trusted: outgoing only to Monitored (). No edge to Restricted, Revoked, or Deregistered exists.
  * •
Monitored: outgoing to Restricted () and Trusted (). No edge to Revoked.
  * •
Restricted: outgoing to Revoked () and Monitored (). No edge to Trusted or Learning.
  * •
Revoked: outgoing to Deregistered () and Restricted (). No edge to Trusted or Monitored.
  * •
Deregistered: no outgoing edges. Terminal.


Step 3 (Conclusion): Since every state has a finite, explicitly enumerated set of guarded outgoing transitions, and unmatched events are discarded, no execution trace can contain a pair (s_{i},s_{i+1}) not in . ∎∎
######  (Graduated Enforcement).
For any tool in state Trusted, reaching Revoked requires minimum 3 transitions: Trusted \xrightarrow{e_{2}} Monitored \xrightarrow{e_{3}} Restricted \xrightarrow{e_{4}} Revoked.
###### Proof.
No edges (Trusted, , Revoked), (Trusted, , Restricted), or (Monitored, , Revoked) exist in . The shortest path from Trusted to Revoked has length 3. Operators have intervention windows at Monitored (: recovery) and Restricted (: partial recovery). The exception (Learning→\rightarrowRevoked) applies only to the Learning state under critical violations. ∎∎
With mandatory dwell times enforced (\Delta t_{\mathrm{grace}} for and 4 h for ), the minimum elapsed time from Trusted to Revoked is \Delta t_{\mathrm{grace}}+4 h (default: 28 h), providing a guaranteed intervention window during which operators can assess and potentially reverse degradation before revocation occurs.
Policy re-evaluation may trigger multiple sequential transitions in a single evaluation pass when threshold changes cause a tool to cross multiple boundaries simultaneously. Each intermediate state is instantiated and recorded (with corresponding notifications generated per the observable trust change property), preserving the graduated enforcement audit trail. The path length guarantee applies to the state sequence, not to the number of external triggering events - a single policy change may produce a multi-step graduated path, but no step in that path is skipped.
###  Cascade Convergence
######  (Bounded Cascade Termination).
For damping factor \gamma\in(0,1) and maximum depth D_{\max}, cascade propagation terminates in at most D_{\max} steps with penalty at depth bounded by P_{0}\cdot\gamma^{d}.
###### Proof.
The penalty P(d)=P_{0}\cdot\gamma^{d} is a geometric sequence with ratio , converging to 0. The hard cutoff at D_{\max} and visited-set in Algorithm ensure each tool is processed at most once. Termination is \mathcal{O}(|V|+|E|). ∎∎
######  (Concurrent Cascade Independence).
When multiple tools degrade concurrently, each cascade executes independently with its own visited set. A cascade-induced state transition in tool does _not_ trigger a new cascade from —cascades propagate only from the original degradation source. This is enforced by design: Algorithm 1 is invoked exclusively by the TrustEvaluationService upon receiving an external trust event, not upon internal score updates from cascade penalties. Consequently, the total penalty applied across concurrent cascades is bounded by k⋅⋅=k⋅⋅k\cdot P_{0}\cdot\sum_{d=0}^{D_{\max}}\gamma^{d}=k\cdot P_{0}\cdot\frac{1-\gamma^{D_{\max}+1}}{1-\gamma}, which is finite and bounded.
######  (Cascade Stability).
If all tools satisfy trustScore​\texttt{trustScore}(t)>\theta_{\text{revoke}}+P_{0} for base penalty , then a single degradation event cannot trigger unbounded revocation cascades.
###### Proof.
At depth 1, maximum penalty is P_{0}\cdot\gamma<P_{0}. By assumption, trustScore​−\texttt{trustScore}(t)-P_{0}\cdot\gamma>\theta_{\text{revoke}}, so no tool is revoked. At deeper levels, penalties decrease geometrically. The cascade may trigger transitions to Monitored or Restricted (graduated degradation) but cannot cause revocations under the stability condition. ∎∎
######  (Cascade Order-Independence).
If two tools and degrade concurrently, the final trust state of any tool in the dependency graph is independent of the order in which the cascade algorithms for and execute.
###### Proof.
The cascade algorithm (Algorithm ) applies a fixed penalty P_{0}\cdot\gamma^{h} to each reachable tool based solely on the graph distance from the source. The visited set ensures each tool is penalized at most once per cascade invocation.
For concurrent events, consider tool reachable from both (distance ) and (distance ). Two execution orders yield:
  * •
Order 1 ( first): receives penalty P_{a}\cdot\gamma^{h_{a}} from cascade(), then P_{b}\cdot\gamma^{h_{b}} from cascade().
  * •
Order 2 ( first): receives P_{b}\cdot\gamma^{h_{b}} then P_{a}\cdot\gamma^{h_{a}}.


Since subtraction is commutative (s-x-y=s-y-x) and the \max(0,\cdot) clamp is applied per-penalty (line 13 of Algorithm ), the final score is:  
| =max⁡(0,max⁡−)s_{\text{final}}=\max(0,\max(0,s_{0}-P_{a}\gamma^{h_{a}})-P_{b}\gamma^{h_{b}})  |  
| --- |  
When s_{0}<P_{a}\gamma^{h_{a}}, the score clamps to zero under both orderings, yielding identical results. When ≥s_{0}\geq P_{a}\gamma^{h_{a}}+P_{b}\gamma^{h_{b}}, no clamping occurs and commutativity of addition gives order-independence. In the intermediate case (≤P_{a}\gamma^{h_{a}}\leq s_{0}<P_{a}\gamma^{h_{a}}+P_{b}\gamma^{h_{b}}), both orderings produce max⁡(0,−−)=0\max(0,s_{0}-P_{a}\gamma^{h_{a}}-P_{b}\gamma^{h_{b}})=0, since the total penalty exceeds the score. Thus order-independence holds universally. ∎∎
######  (Bounded Notification Queue).
Under concurrent degradation of tools, the total notifications generated is bounded by k\cdot(1+|V_{D_{\max}}|), where |V_{D_{\max}}| is the maximum reachable set within depth D_{\max}. Cascades cannot re-trigger cascades: a cascade-induced state transition generates a notification but does not initiate a new cascade propagation (cascade penalties are applied once per source event, not recursively).
###  Notification Completeness
######  (Observable Trust Changes).
Every trust state transition (s,e,s^{\prime}) generates exactly one notification delivered to all subscribing management systems.
This holds under three assumptions: (a) state update and notification emission execute atomically within a single transaction (enforced by the MnS implementation per TS 28.532); (b) the underlying notification transport provides reliable, ordered delivery (guaranteed by the MnS notification IRP); and (c) monotonic sequence numbers per tool enable receivers to detect gaps caused by transient delivery failures. Under these assumptions, no tool can undergo silent trust degradation.
###  Retroactive Assessment Completeness
######  (Lookback Completeness).
Given lookback window , the retroactive impact assessment identifies all configuration changes within [\tau_{\text{deg}}-W,\tau_{\text{deg}}] performed by the degraded tool. Completeness follows from the append-only property of AgentActionLogMO: entries are never deleted, and the temporal range query over the indexed log returns all matching entries. Zero false negatives within the configured window. This guarantee holds in non-adversarial settings; tamper-resistant logging (e.g., signed log entries) would be required to extend completeness to adversarial scenarios.
##  VI Evaluation
We evaluate the proposed standardized trust management information model through discrete-event simulation of a multi-vendor autonomous network environment. Our evaluation quantifies the benefits of cross-vendor trust visibility, cascade containment effectiveness, and operational overhead. The evaluation quantifies performance under modeled operational assumptions; the notification latency components are derived from typical MnS implementation benchmarks rather than measured from a deployed system.
The evaluation serves as a _feasibility and scalability assessment_ of the architectural design rather than a performance validation against deployed systems. Quantitative results demonstrate that the framework’s mechanisms (cascade damping, notification delivery, retroactive assessment) operate correctly and scale sub-linearly, but absolute latency values depend on deployment-specific MnS implementation characteristics.
###  Simulation Setup
####  Network Topology
We model a multi-vendor autonomous network comprising three vendor domains:
  * •
Vendor A (RAN Management): 500 cells managed by 5 autonomous agents, each utilizing 2–4 specialized tools (e.g., cell parameter optimization, coverage adjustment, load balancing).
  * •
Vendor B (Core Network Management): 50 network functions (NFs) managed by 5 agents with tools for scaling, traffic steering, and slice management.
  * •
Vendor C (Transport Management): 100 transport links managed by 5 agents with tools for path computation, bandwidth allocation, and fault correlation.


####  Agent and Tool Configuration
The simulation deploys 15 agents in total (5 per vendor domain), collectively utilizing 30 tools. The tool dependency graph has an average degree of 2.3, modeling realistic cross-tool and cross-vendor dependencies (e.g., a RAN capacity tool depending on transport bandwidth allocation, or a core scaling tool depending on RAN load predictions).
####  Trust Degradation Injection
Trust degradation events are injected following a Poisson process with rate \lambda=0.5 events/hour. Each event targets a randomly selected tool and reduces its trust score by a uniformly distributed penalty P_{0}\sim\mathcal{U}(10,30) on a [0,100] trust scale. Tools are invoked by consuming agents at a rate of 2 invocations/minute (averaged across all tool types). The simulation runs for 72 hours per trial, with 1000 independent runs for statistical significance (95% confidence intervals reported).
####  Comparison Approaches
We compare three trust management approaches:
  1. 1.
No Trust Model (Baseline 1): No trust management framework is deployed. Degradation is detected only when downstream service impact triggers KPI threshold alarms in the operator’s monitoring system. Detection latency is modeled as the time for KPI regression to accumulate beyond alarm thresholds, sampled from Exponential​\text{Exponential}(\mu=3.0\,\text{h}) truncated at 24 h, consistent with published operator NOC response times for non-critical KPI degradation .
  2. 2.
Per-Vendor Proprietary Trust (Baseline 2): Each vendor maintains an internal trust model visible within its own domain. Same-vendor detection latency follows a log-normal distribution LogNormal​()\text{LogNormal}(\mu=1.5,\sigma=0.8) seconds, representing vendor-internal telemetry processing. Cross-vendor events are detected through eventual service impact with latency Exponential​\text{Exponential}(\mu=1.5\,\text{h}), representing partial cross-vendor visibility through shared SLA dashboards and cross-domain KPI correlation.
  3. 3.
Proposed Standardized Model: Cross-vendor trust notifications are delivered through the MnS notification interface. Detection latency is modeled as the sum of three components: (a) local trust assessment computation, \mathcal{U}(5,20) ms, representing the time for the source vendor’s management system to evaluate the trust score update; (b) MnS notification serialization and delivery, \mathcal{U}(8,50) ms, consistent with RESTful notification delivery over managed IP networks; and (c) receiving system processing, \mathcal{U}(2,25) ms. The total TTD is the convolution of these three components, yielding an expected mean of approximately 55 ms.


The proposed model’s TTD emerges from the modeled notification pipeline components rather than being directly assumed. The component latencies are derived from: (a) typical management system event processing benchmarks for NRM attribute evaluation; (b) HTTP/TLS notification delivery latency over enterprise-grade IP networks consistent with RESTful MnS implementations; and (c) event deserialization and subscription dispatch overhead.
###  Evaluation Metrics
We define five metrics to evaluate trust management effectiveness:
  * •
M1 – Time-to-Detection (TTD): Elapsed time from the occurrence of a trust-degrading event to its visibility in consuming vendors’ management systems.
  * •
M2 – Blast Radius Containment: Number of downstream services affected before containment action is triggered.
We note that this metric captures _invocation-level exposure_ (number of tool invocations occurring during undetected degradation) rather than _service-level impact_ (SLA violations or subscriber-visible degradation). The translation from invocations to service impact depends on the specific tool’s criticality, the sensitivity of affected parameters, and current network load—factors that require deployment-specific SLA models beyond our simulation scope. The metric is conservative: not every invocation during degradation produces harmful output, so actual service impact may be lower than the invocation count suggests.
  * •
M3 – Cascade Containment: Number of tools whose trust state changes as a result of cascade propagation.
  * •
M4 – Notification Overhead: Total number of notification messages generated per trust degradation event.
  * •
M5 – False Positive Rate: Fraction of cascade-triggered trust degradations where the affected tool’s own behavior had not actually degraded (i.e., it was penalized solely due to a dependency relationship, and its trust score remained above the restriction threshold prior to the cascade penalty).


###  Results
####  Time-to-Detection
Table presents the time-to-detection comparison across the three approaches.
TABLE II: Cross-Vendor Detection Latency Comparison (Modeled Assumptions)  
|   |  
| --- |  
| No Trust Model  |  3.0\pm 0.2  |  
| Per-Vendor Trust  |  
| Proposed Model  |  55.1±1.455.1\pm 1.4 ms  |  
| Per-vendor TTD is bimodal: fast for same-vendor, hours for cross-vendor.  |  
The key takeaway is that standardized notifications reduce cross-vendor detection latency by over three orders of magnitude—from hours to milliseconds—eliminating the detection gap that enables cascading failures in multi-vendor deployments.
The proposed model achieves sub-100 ms cross-vendor detection through standardized notification delivery (median 55.1 ms, 95th percentile 91.1 ms). The per-vendor approach provides fast detection for same-vendor events but produces a bimodal distribution: events where the consuming agent is in the same vendor domain are detected rapidly, while cross-vendor events remain undetected until service-level impact manifests (median overall TTD for per-vendor: 61 s, driven by the mix of fast same-vendor and slow cross-vendor detection).
####  Blast Radius Containment
Figure and Table quantify the downstream service impact before containment.
Figure 4: Blast radius comparison across trust management approaches under modeled assumptions. The proposed model reduces mean affected services from 7.71 (no-trust) to 3.01. TABLE III: Blast Radius: Affected Services Before Containment  
|   |  
| --- |  
| No Trust Model  | 7.71±0.347.71\pm 0.34  | 7.0  | 18.0  |  
| Per-Vendor Trust  | 3.82±0.163.82\pm 0.16  | 3.0  | 9.0  |  
| Proposed Model  | 3.01±0.093.01\pm 0.09  | 3.0  | 5.0  |  
The proposed standardized model reduces the mean blast radius by 61% compared to the no-trust baseline (from 7.71 to 3.01 affected services) and by 21% compared to per-vendor trust (from 3.82 to 3.01). The blast radius for each approach is computed as the number of service-affecting tool invocations that occur during the detection window - i.e., the product of the detection latency and the tool’s invocation rate by downstream agents. The proposed model’s sub-100 ms notification window limits exposure to at most one or two pending invocations, while the no-trust model’s hours-long detection window permits continued invocation of the degraded tool across all consuming agents. The 21% improvement over per-vendor trust reflects elimination of the cross-vendor blind spot: events that per-vendor approaches detect only through eventual SLA impact are immediately visible through standardized notifications.
####  Comparison Against Bilateral Integration Baseline
To isolate the benefit of _standardization_ from the benefit of _any_ cross-vendor signaling, we add a fourth comparison: per-vendor trust augmented with bilateral REST webhooks between each vendor pair. In this baseline, vendors exchange trust state via proprietary point-to-point integrations with latency modeled as \mathcal{U}(200,800) ms (reflecting authentication, serialization, and non-standardized processing overhead typical of ad-hoc integrations).
TABLE IV: Blast Radius: Standardized vs. Bilateral Integration  
| Reduction (%)  |  
| --- |  
| No Trust (Baseline 1)  | 7.71  | -  |  
| Per-Vendor only (Baseline 2)  | 3.82  | 50.5  |  
| Per-Vendor + Bilateral (Baseline 3)  | 3.24  | 58.0  |  
| Proposed Standardized Model  | 3.01  | 61.0  |  
The bilateral integration baseline achieves 58% reduction - close to but below the standardized model’s 61%. The remaining 3% gap (Table ) arises from two factors: (1) the standardized model’s lower notification latency (55 ms vs. 200–800 ms for bilateral), and (2) the standardized model’s cascade propagation mechanism, which proactively degrades dependent tools before they produce compromised outputs. The marginal improvement of the standardized model over bilateral integration is modest in absolute terms (3.24 to 3.01 affected services, a 7% reduction). The primary advantage of standardization over bilateral approaches is operational: standardized interfaces require integration effort (one implementation per vendor) versus O(n^{2}) bilateral agreements, making the approach scalable to ecosystems with many vendors without proportional integration cost growth. As vendor count grows, bilateral integration becomes operationally infeasible while the standardized approach scales linearly.
####  Cascade Depth Distribution
Figure shows the cumulative distribution function of cascade propagation depth with \gamma=0.5 and D_{\max}=3.
Figure 5: CDF of cascade propagation depth (\gamma=0.5, D_{\max}=3). 85.4% of cascade events terminate within depth 2, and all cascades terminate at the configured bound D_{\max}=3, confirming bounded convergence (Theorem ).
With \gamma=0.5, 51.6% of events produce no cascade propagation (depth 0, affecting only the degraded tool itself), 73.7% terminate within depth 1, and 85.4% terminate within depth 2. All cascades terminate at or below D_{\max}=3, confirming the bounded convergence guarantee of Theorem .
####  Notification Overhead
Figure presents the notification overhead as a function of the number of vendor domains.
Figure 6: Mean notifications per trust event versus number of vendor domains. Growth is sub-linear, ranging from 1.31 (2 vendors) to 1.55 (10 vendors), indicating manageable overhead.
The notification overhead is low and grows sub-linearly with vendor count: from 1.31 notifications per event in a 2-vendor topology to 1.55 in a 10-vendor topology. This modest growth reflects that most trust events affect tools consumed by a small subset of vendors, not the entire ecosystem.
####  Impact of Damping Factor
Figure shows the effect of varying on cascade propagation and false positive rate.
Figure 7: Effect of damping factor on cascade containment. Higher increases tools affected by cascade but reduces false positive rate (FP = fraction of cascade-restricted tools that are not ground-truth affected, i.e., their independent behavior had not observably degraded at restriction time).
The simulation reveals a trade-off: lower values (0.1–0.3) apply smaller cascade penalties, resulting in fewer cascade-triggered state transitions (0.18–0.29 tools affected per event on average) but a higher false positive rate (36.9–37.1%) due to tools near thresholds being pushed into degraded states by small accumulated penalties. Higher values (0.7–0.9) propagate larger penalties that more decisively separate compromised from healthy tools, reducing false positives to 19.5–25.2% but affecting more tools per event (0.55–0.64).
####  Ground-Truth Validation of Cascade Detection
To validate whether cascade penalties correctly identify tools whose outputs are actually compromised, we inject _known_ degradation: in 200 additional runs, we mark tools whose inputs come from a degraded source as “ground-truth affected” (their outputs are computed from stale/incorrect upstream data). We then measure precision (fraction of cascade-restricted tools that are ground-truth affected) and recall (fraction of ground-truth affected tools that are cascade-restricted). At \gamma=0.5: precision = 68.3%, recall = 91.7%. The 31.7% false positive rate thus represents _conservative over-restriction_ - tools that were restricted but whose outputs had not yet observably degraded. From an operational safety perspective, over-restriction (restricting a tool that might degrade shortly) is preferable to under-restriction (allowing a compromised tool to continue). The high recall (91.7%) confirms that the cascade mechanism rarely misses truly affected tools.
Ground-truth determination. In the 200 validation runs, we independently inject secondary degradation events into downstream tools with probability 0.3 per cascade-affected tool. A tool is marked as _ground-truth degraded_ if either: (a) it received a direct degradation injection, or (b) its functional output quality decreased by 15% due to consuming outputs from a directly degraded upstream tool (measured by comparing tool outputs against a reference oracle with no degraded inputs). Precision measures what fraction of cascade-flagged tools were genuinely affected; recall measures what fraction of genuinely affected tools were detected by the cascade mechanism. The 68.3% precision reflects the framework’s conservative design (preferring over-flagging to missed degradation), while 91.7% recall confirms that genuinely affected tools are rarely missed.
The cascade mechanism is deliberately conservative: in safety-critical autonomous networks, the cost of a missed degradation (undetected cascading failure affecting live subscribers) far exceeds the cost of an unnecessary restriction (temporary suspension of one tool’s write capability while alternatives remain available). A restricted tool retains read access and resumes full operation upon the next clean evaluation cycle, making false restrictions self-correcting. Operators who prefer precision over recall can increase toward 0.9 (Table ), trading slower cascade containment for fewer unnecessary restrictions.
####  Ablation: Causal Deduplication Impact
To quantify the practical impact of the causal deduplication mechanism (Eq. ), we compare trust score trajectories with and without deduplication across 1000 runs. Table summarizes the results.
TABLE V: Ablation: Causal Deduplication vs. Naive Summation  
 |  
|  |  
| Correlated penalty events (%)  | -  | 23.4  |  
| Mean penalty per event  | 18.7  | 14.2  |  
| Unnecessary state transitions  | 0.31/event  | 0.19/event  |  
| FP rate (cascade, \gamma=0.5)  | 38.9%  | 31.7%  |  
| Tools reaching Revoked (spurious)  | 4.1%  | 1.8%  |  
In our simulation, 23.4% of degradation events produce correlated penalties (two or more penalty sources triggered within the 60 s correlation window from the same root action). Without deduplication, naive summation over-penalizes these events, inflating the mean penalty by 32% (18.7 vs. 14.2) and generating 63% more unnecessary state transitions. Most critically, spurious revocations (tools reaching Revoked due to penalty stacking rather than genuine severe degradation) drop from 4.1% to 1.8% with deduplication - a 56% reduction in the most operationally disruptive false outcome.
###  Sensitivity Analysis
####  Damping Factor 
Table summarizes the sensitivity to the damping factor.
TABLE VI: Sensitivity to Damping Factor (500 runs per configuration)  
| Cascade Tools  | FP Rate (%)  | Mean Blast Radius  |  
| --- | --- | --- |  
The blast radius remains stable across values because it is primarily determined by the notification latency (which is -independent) rather than cascade extent. The key trade-off is between cascade reach and false positive rate: higher propagates larger penalties that push dependent tools more decisively below thresholds, producing clearer state transitions with fewer ambiguous cases near threshold boundaries. From an operational perspective, a 20% false positive rate means 1 in 5 cascade-triggered restrictions are unnecessary - the tool would not have actually produced degraded outputs. However, a missed cascade (false negative) allows degraded outputs to propagate to dependent services. For safety-critical network slices, operators may prefer higher FP rates (low ) to minimize missed cascades; for best-effort services, higher minimizes unnecessary restrictions.
####  Maximum Cascade Depth D_{\max}
With \gamma=0.5, all cascades in our simulation terminated at or below depth 3 regardless of whether D_{\max} was set to 3, 5, or 10. This confirms that the geometric damping naturally bounds propagation: at depth 3, the penalty is \gamma^{3}=0.125 of the base penalty, which is typically insufficient to trigger state transitions for tools not already near thresholds.
####  Notification Overhead Scaling
Notification overhead scales sub-linearly with vendor count (1.31 at 2 vendors to 1.55 at 10 vendors). This is significantly lower than the theoretical worst case of per event, because most tools are consumed by agents from 1–2 vendor domains rather than all vendors simultaneously.
####  TTD Sensitivity to Notification Latency
Table quantifies how blast radius degrades if the notification pipeline is slower than the modeled 55 ms. We vary the total notification latency from 55 ms (baseline) to 500 ms, keeping all other parameters constant.
TABLE VII: Sensitivity of Blast Radius to Notification Latency  
| Reduction vs.  | Reduction vs.  |  
| --- | --- |  
| No-Trust (%)  | Per-Vendor (%)  |  
| 55 ms (baseline)  | 3.01  | 61.0  | 21.2  |  
| 100 ms  | 3.04  | 60.6  | 20.4  |  
| 200 ms  | 3.09  | 59.9  | 19.1  |  
| 500 ms  | 3.22  | 58.2  | 15.7  |  
The blast radius advantage is robust to notification latency variations: even at 500 ms (9 the baseline), the standardized model still achieves 58% reduction versus no-trust and 16% versus per-vendor. This stability arises because the tool invocation rate (2/min 1 invocation per 30 s) means that sub-second notification latency permits at most one additional invocation during the detection window. The framework’s advantage degrades significantly only when notification latency approaches the inter-invocation interval (30 s), at which point the proposed model converges toward per-vendor performance.
The component latencies (5–20 ms local assessment, 8–50 ms MnS delivery, 2–25 ms receiver processing) are consistent with published REST API round-trip benchmarks for enterprise management systems and with the 3GPP MnS design target of sub-second notification delivery for alarm-class events (TS 28.532, Section 7.2).
_Caveat:_ These component latencies are modeled from architectural analysis and published design targets, not measured from a deployed multi-vendor 3GPP management system. Actual production deployments may exhibit higher latencies due to authentication overhead, policy evaluation, rate limiting, or geographic distribution. Table demonstrates that the framework’s advantage is robust up to 500 ms total TTD; beyond this point, the benefit converges toward per-vendor performance. We recommend operators measure actual MnS notification latency during initial pilot deployment and use the measured values to inform trust assessment threshold tuning and AgentPolicyMO configuration.
###  Scalability Analysis
To assess framework behavior at larger scales, we vary the number of tools from 30 (baseline) to 100, 500, and 1000 while maintaining constant tool density per vendor (proportionally scaling agents and vendor domains) and average dependency degree of 2.3.
TABLE VIII: Scalability: Key Metrics vs. Network Size  
 |  
|  |  
| Mean TTD (ms)  | 55.1  | 56.2  | 57.8  | 59.3  |  
| Mean cascade depth  | 0.91  | 0.94  | 0.97  | 0.99  |  
| Notifications/event  | 1.42  | 1.48  | 1.53  | 1.56  |  
| Cascade compute (ms)  | 0.3  | 1.1  | 5.8  | 12.4  |  
Table shows that TTD, cascade depth, and notification overhead remain nearly constant with network size - these metrics depend on local topology (dependency degree) rather than total tool count. The cascade computation time grows linearly with the number of tools (due to the visited-set and dependency graph traversal), but remains under 15 ms even at 1000 tools - well within management system processing budgets. The bounded depth guarantee (D_{\max}) ensures that cascade computation does not explore the full graph regardless of network size, confirming the O(D_{\max}\cdot k) complexity where is the average dependency degree.
###  Prototype Implementation
To demonstrate the feasibility of the proposed information model, we implement a proof-of-concept REST service aligned with 3GPP MnS conventions (TS 28.532). The prototype is structured as two modules: (i) a vendor-neutral API module defining the standard interfaces (TrustLifecycleApi, TrustDecisionApi, TrustEvaluationApi, TrustPropagationApi, SessionManagementApi, PolicyManagementApi, NotificationManagementApi), and (ii) a vendor-specific implementation module realizing the framework as a Spring Boot microservice.
The prototype exposes the full AgentToolMO lifecycle via RESTful endpoints following the 3GPP /{MnSRoot}/{version}/{className}/{objectInstance} pattern. Key operations include trust-aware tool discovery (with score and state filtering), a trust gate endpoint (canInvoke) returning enforcement decisions with applicable restrictions, event reporting with automatic cascade propagation, session enforcement (suspend/terminate/restore), retroactive impact assessment querying the action log within a lookback window, and policy re-evaluation that automatically transitions all tools when thresholds change.
Functional testing confirms that the state machine correctly implements all ten transitions (–\tau_{10}), cascade propagation terminates within D_{\max}, session enforcement triggers synchronously on state change, and the retroactive assessment correctly identifies affected managed elements and configuration changes made by agents during the lookback window.
##  VII Discussion
###  Key Findings
Our evaluation reveals four principal findings:
Finding 1: Cross-vendor trust visibility is transformative. The standardized model reduces blast radius from a mean of 7.71 affected services (no-trust baseline) to 3.01, comparable to per-vendor approaches (3.82) but achieved through a single standardized interface rather than requiring n(n-1)/2 pairwise bilateral integrations. The improvement over per-vendor trust demonstrates that cross-vendor blind spots—not the absence of trust management per se—are the critical gap in current deployments.
Finding 2: Notification overhead is manageable. Notifications per trust event range from 1.31 (2 vendors) to 1.55 (10 vendors), growing sub-linearly. This is orders of magnitude below infrastructure capacity and introduces negligible operational overhead.
Finding 3: Damping factor presents a clear trade-off. Lower values apply smaller cascade penalties per hop, yet the simulation shows higher false positive rates at low (37% at \gamma=0.1 vs 19.5% at \gamma=0.9). This occurs because small penalties push tools into borderline states near thresholds without decisive resolution—tools hover near boundaries and are flagged as degraded without clearly breaching or clearing thresholds. Higher values produce larger, more decisive penalties that either clearly breach thresholds (true degradation) or leave tools well above them (no false alarm). The choice of should be operator-configurable based on risk posture.
Table provides operational guidance for selection based on network slice SLA tier. The recommendations balance false positive cost (unnecessary tool restriction) against false negative risk (allowing compromised tools to continue operating).
TABLE IX: Operator Guidance: Recommended by SLA Tier  
 |  
|  |  
| Safety-critical (URLLC)  | 0.7–0.9  | 20–25%  |  Decisive penalties; high recall of affected tools; prefer over-restriction  |  
| Standard (eMBB)  | 0.5  |  Balanced trade-off; default recommendation  |  
| Best-effort (mMTC)  | 0.1–0.3  |  Minimal cascade disruption; accept higher miss risk for operational stability  |  
Finding 4: Graduated enforcement prevents unnecessary revocations. Zero unnecessary revocations occurred in our simulation—no tool with a trust score above the restriction threshold was revoked, validating that the state machine’s graduated path (Trusted→\rightarrowMonitored→\rightarrowRestricted→\rightarrowRevoked) filters transient perturbations before reaching revocation.
###  Deployment and Standardization
The proposed information model can be deployed incrementally: vendors that adopt it expose trust state; those that do not remain invisible to the framework without breaking existing functionality. No new transport protocols are required—existing MnS interfaces carry trust information. The IOC definitions follow established NRM design patterns, demonstrating compatibility with 3GPP SA5 conventions and potential applicability within future standards evolution.
###  Graceful Degradation Under Partial Adoption
A critical deployment consideration is behavior when some vendors implement AgentToolMO and others do not. The framework degrades gracefully along three dimensions:
Non-compliant vendor tools. Tools from vendors that do not implement the IOC are treated as having unknown trust state (equivalent to perpetual Learning state). Consuming agents may apply a configurable default trust floor (e.g., θrestrict\theta_{\text{restrict}}) for unresolvable tools, ensuring conservative behavior without complete exclusion. Operators configure this floor in AgentPolicyMO via an unknownVendorPolicy attribute.
Formal safety under partial compliance. Let denote the number of compliant vendors and the non-compliant vendors (n_{c}+n_{nc}=n). For tools owned by compliant vendors, all safety properties (Theorems 1–3) hold unchanged. For tools owned by non-compliant vendors, the framework assigns a configurable default state sdefault∈{Learning,Monitored}s_{\mathrm{default}}\in\\{\texttt{Learning},\texttt{Monitored}\\} (configured via unknownVendorPolicy in AgentPolicyMO). Cascade propagation traverses dependencies into non-compliant domains but cannot update their trust state (read-only visibility). The key invariant is: _partial compliance cannot weaken safety for compliant vendors_ —a non-compliant vendor’s tools are treated conservatively, and compliant vendors’ state machines operate independently of non-compliant participation.
Notification delivery failures. If cross-vendor notifications cannot be delivered (network partition, endpoint unreachable), the subscribing system does not receive trust updates. The framework reverts to per-vendor behavior for that link—equivalent to Baseline 2 performance for that specific cross-vendor dependency—while all other trust-visible links continue operating normally. Stale-state detection (comparing trustStateTransitionTime against a staleness threshold) enables agents to autonomously increase caution for tools whose trust state has not been refreshed.
Incentive alignment. The framework provides direct operational benefit to early adopters: a vendor implementing AgentToolMO gains visibility into other compliant vendors’ tool trust without requiring universal adoption. The value scales with adoption (network effect) but remains positive for any compliant vendors. Operators can mandate compliance as a procurement requirement, creating market incentive for vendor participation.
###  Limitations
  1. 1.
Simulation-based evaluation: Results are obtained from discrete-event simulation rather than production deployment.
  2. 2.
Reliable notification delivery: The formal analysis assumes reliable, ordered notification delivery. Network partitions may delay notifications.
  3. 3.
Adversarial and strategic trust manipulation: The model does not address scenarios where a compromised agent deliberately manipulates trust scores, nor does it enforce incentive-compatible reporting. In a competitive ecosystem, vendors have an incentive to over-report competitor tool degradation and under-report their own. The current framework relies on auditable trust evidence (KPI data, action logs) as a forensic deterrent but does not provide real-time adversarial detection or mechanism-design-based incentive alignment. Tamper-resistant logging and third-party auditor verification are directions for future work.
  4. 4.
Scalability: While simulation confirms bounded cascade computation up to 1000 tools (12.4 ms, Table ), production deployments with significantly larger dependency graphs, concurrent cascades, and geographic distribution require operational validation beyond simulation.
  5. 5.
Service impact translation: The blast radius metric captures invocation-level exposure (tool invocations during undetected degradation) rather than SLA-level service impact. Translation from invocations to actual subscriber-visible degradation depends on tool criticality, current network load, consuming agent fallback strategies, and service-specific KPI sensitivity thresholds. Deployment-specific SLA models are required to quantify real service impact from the invocation counts reported by the framework.


###  Future Work
Several directions extend this work: integration with intent-driven management for trust-aware intent fulfillment; formal verification using model checking tools (SPIN, TLA+); and production deployment in operator testbeds. Additionally, adversarial scenarios where a vendor deliberately reports inflated or deflated trust scores warrant investigation. Signed, auditable trust evidence - where each trust state transition is accompanied by cryptographically verifiable supporting data (KPI measurements, action logs, anomaly reports) - would enable independent verification of trust claims and detection of manipulative reporting across vendor boundaries.
Additional evaluation dimensions merit investigation: (a) prototype benchmarking to ground simulation latency assumptions against measured MnS notification delivery times; (b) ablation of the state machine granularity (e.g., comparing the six-state graduated model against a simpler three-state Trusted/Degraded/Revoked alternative) to quantify the benefit of intermediate states; (c) sensitivity analysis over dependency graph density, since real deployments may exhibit denser tool interdependencies than the average degree of 2.3 used in our simulation; and (d) validation of the linear blast radius growth model (Section III-B) against empirical cascade propagation patterns.
##  VIII Conclusion
We have presented a standardized information model for cross-vendor agent tool trust management in autonomous networks. The model introduces AgentToolMO as a first-class managed object within the 3GPP NRM, with formally defined trust states, graduated enforcement through a provably safe state machine, damped cascade propagation with guaranteed convergence, and retroactive impact assessment via NRM dependency graph traversal.
Our formal analysis establishes that: (1) the state machine permits no unsafe transitions, ensuring graduated enforcement where tools cannot bypass intermediate trust states; (2) cascade propagation converges in bounded steps regardless of network topology; and (3) all trust state changes generate notifications to subscribing management systems, eliminating silent trust degradation.
Simulation results demonstrate that the standardized model substantially reduces trust failure blast radius compared to both no-trust and per-vendor baselines, with cross-vendor detection latency bounded by MnS notification delivery time rather than hours-scale service impact manifestation. Notification overhead scales sub-linearly with the number of participating vendors, and all cascades terminate within the configured depth bound.
The proposed framework provides the standardization foundation for trustworthy multi-vendor autonomous network management, enabling operators to deploy agents across vendor boundaries with formal safety guarantees and complete trust visibility.
## Acknowledgment
The authors thank the Ericsson Network Management research team for valuable discussions on autonomous network management architectures.
## References
  * [1] 3GPP, “3GPP TR 28.908: Study on autonomous network levels,” 3rd Generation Partnership Project, Technical Report, 2023. 
  * [2] ——, “3GPP TS 28.105: Management and orchestration; AI/ML management,” 3rd Generation Partnership Project, Technical Specification, 2024. 
  * [3] ——, “3GPP TS 28.533: Management and orchestration; Architecture framework,” 3rd Generation Partnership Project, Technical Specification, 2024. 
  * [4] Y. Huang _et al._ , “A survey on trustworthy ai: Challenges and opportunities,” _ACM Computing Surveys_ , vol. 56, no. 4, 2024. 
  * [5] A. Kumar and M. Singh, “Trust management in multi-agent systems: A comprehensive survey,” _Journal of Network and Computer Applications_ , vol. 210, p. 103547, 2023. 
  * [6] NIST, “Artificial Intelligence Risk Management Framework (AI RMF 1.0),” National Institute of Standards and Technology, Tech. Rep. NIST AI 100-1, 2023. 
  * [7] G. Boudreau _et al._ , “Orchestrating sandboxing of cognitive network management functions,” International Publication WO 2021/069196 A1, 2021. 
  * [8] S. Rose, O. Borchert, S. Mitchell, and S. Connelly, “Zero Trust Architecture,” National Institute of Standards and Technology, Tech. Rep. NIST SP 800-207, 2020. 
  * [9] 3GPP, “3GPP TS 28.622: Telecommunication management; Generic Network Resource Model (NRM) Integration Reference Point (IRP); Information Service (IS),” 3rd Generation Partnership Project, Technical Specification, 2024. 
  * [10] ——, “3GPP TS 28.623: Telecommunication management; Generic Network Resource Model (NRM) Integration Reference Point (IRP); Solution Set (SS) definitions,” 3rd Generation Partnership Project, Technical Specification, 2024. 
  * [11] ——, “3GPP TS 28.532: Management and orchestration; Generic management services,” 3rd Generation Partnership Project, Technical Specification, 2024. 
  * [12] A. Jøsang, R. Ismail, and C. Boyd, “A survey of trust and reputation systems for online service provision,” _Decision Support Systems_ , vol. 43, no. 2, pp. 618–644, 2007. 
  * [13] S. D. Kamvar, M. T. Schlosser, and H. Garcia-Molina, “EigenTrust: Reputation management in P2P networks,” in _Proc. 12th International Conference on World Wide Web_ , 2003, pp. 640–651. 
  * [14] S. P. Marsh, “Formalising trust as a computational concept,” Ph.D. dissertation, University of Stirling, 1994. 
  * [15] A. Jøsang, “A logic for uncertain probabilities,” _International Journal of Uncertainty, Fuzziness and Knowledge-Based Systems_ , vol. 9, no. 3, pp. 279–311, 2001. 
  * [16] S. D. Ramchurn, D. Huynh, and N. R. Jennings, “Trust in multi-agent systems,” _The Knowledge Engineering Review_ , vol. 19, no. 1, pp. 1–25, 2004. 
  * [17] W. T. L. Teacy, J. Patel, N. R. Jennings, and M. Luck, “Travos: Trust and reputation in the context of inaccurate information sources,” _Autonomous Agents and Multi-Agent Systems_ , vol. 12, no. 2, pp. 183–198, 2006. 
  * [18] Z. Noorian and M. Ulieru, “Trust and reputation systems: A review,” in _Proc. IEEE Symposium on Self-Adaptive and Self-Organizing Systems_ , 2010. 
  * [19] D. Calvaresi, A. Dubovitskaya, J. P. Calbimonte, K. Taveter, and M. Schumacher, “Multi-agent systems and blockchain: Results from a systematic literature review,” in _Proc. International Conference on Practical Applications of Agents and Multi-Agent Systems_ , 2019, pp. 110–126. 
  * [20] 3GPP, “3GPP TS 28.541: Management and orchestration; 5G Network Resource Model (NRM); Stage 2 and stage 3,” 3rd Generation Partnership Project, Technical Specification, 2024. 
  * [21] ——, “3GPP TS 28.104: Management and orchestration; Management Data Analytics,” 3rd Generation Partnership Project, Technical Specification, 2024. 
  * [22] ——, “3GPP TS 28.312: Management and orchestration; Intent driven management services for mobile networks,” 3rd Generation Partnership Project, Technical Specification, 2024. 
  * [23] ETSI, “ETSI GS ENI 005: Experiential Networked Intelligence (ENI); System Architecture,” European Telecommunications Standards Institute, Tech. Rep., 2023. 
  * [24] ——, “ETSI GS ZSM 002: Zero-touch network and Service Management (ZSM); Reference Architecture,” European Telecommunications Standards Institute, Tech. Rep., 2022. 
  * [25] O-RAN Alliance, “O-RAN.WG2.AIML-v02.00: AI/ML Workflow Description and Requirements,” O-RAN Alliance, Tech. Rep., 2023. 
  * [26] H. Ammar _et al._ , “Operation of agents in a communication network,” International Publication WO 2024/183933 A1, 2024. 
  * [27] L. Fan _et al._ , “Network control function for cognitive function evaluation,” UK Patent Application GB 2640186 A, 2024. 
  * [28] 3GPP, “3GPP TS 28.535: Management and orchestration; Management services for communication service assurance,” 3rd Generation Partnership Project, Technical Specification, 2024. 
  * [29] CISA, “Zero Trust Maturity Model,” Cybersecurity and Infrastructure Security Agency, Tech. Rep., 2023. 
  * [30] Anthropic, “Model Context Protocol Specification,” 
  * [31] Auth0/Okta, “The AI-Ready Identity Framework: Modernizing Product Architecture for the Model Context Protocol Shift,” Preprint, 2026. 
  * [32] C. Agarwal _et al._ , “Mechanism for accessing knowledge sources,” Chinese Patent Application CN 122001607 A, 2026. 
  * [33] 3GPP, “3GPP TR 28.910: Study on autonomous network management,” 3rd Generation Partnership Project, Technical Report, 2023. 
  * [34] TM Forum, “Autonomous Networks: Empowering Digital Transformation,” TM Forum, Industry Guidance IG1305, 2023. 


